# Audit evidence, 2026-09-13 (workflow wf_56935bfc-8b8)
Every finding below survived independent refutation (two refuters for a high, one for a medium) unless listed under *unverified* or *refuted*. Line numbers are as of commit 5d54626. Read with COMPONENTS.md.


## 1. Configuration & feeds
confirmed 11 · refuted 1 · unverified 52

### Confirmed

**[high/small] AdpSnapshot's projection archive is all-or-nothing: CBS flipping to weekly numbers on 2026-09-11 threw and the day's rows for all four sources were lost, then committed without them** — `src/main/java/AdpSnapshot.java:157`
```
for(String source : sources){
            for(Map.Entry<String, Double> entry : ProjectionSources.resolve(source).entrySet()){
                if(SleeperProjections.adpOf(entry.getKey()) > 250){
                    continue;
                }
                out.append(String.join(",", today, source, entry.getKey(),
                        String.format("%.1f", entry.getValue()))).append("\n");
                rows++;
            }
        }
        Files.createDirectories(PROJECTIONS_CSV.getParent());
        Files.writeString(PROJECTIONS_CSV, out.toString(), StandardCharsets.UTF_8,
         
```
- scenario: Real, today. `resolve("cbs")` reaches CbsProjections.leaguePointsBySleeperID, whose URL `.../season/projections/ppr/` now serves the current WEEK once the season starts: the cached cbsProjections_QB_20262026-09-11.txt has gp=1 rows (Herbert 254 pass yds) and the fixed-layout parse gives a best QB of 27.8, so the self-check at CbsProjections.java:65 threw IllegalStateException inside this loop. Because `out` is only written after every source has resolved, the sleeper/borischen/espn rows for 2026-09-11 were never appended. main() had already written the ADP CSV and printed "recorded 235 players" (line 112), so the run looked half-successful and commit 5d54626 ("adp snapshot 2026-09-11") landed with 235 ADP rows and zero projection rows: `cut -d, -f1,2 data/projection-snapshots.csv` ends at 2026-09-02 while adp-snapshots.csv has 2026-09-11. The archive is what `snapshot:<date>` pins and what the accuracy shootout is supposed to be built from; every in-season day CBS stays weekly, the whole archive silently stops. EspnProjections.fetch (line 49-63) has the same shape - no stale fallback, a throw kills the archive.
- fix: Resolve each source inside its own try/catch, append the sources that succeeded, and print/return the ones that failed by name ("cbs: FAILED - weekly page"); write the CSV per source rather than after the loop. Separately, point CBS at the season table or gate it on `gp` >= 10 rather than only on the QB total, and add a test that a source throwing does not blank the others (a fake resolver map is enough).
- siblings: src/main/java/CbsProjections.java:65, src/main/java/EspnProjections.java:49

**[high/small] CBS switched to a weekly page in-season; its guard now aborts AdpSnapshot before the projection archive is written, and the 2026-09-11 day is already lost** — `src/main/java/AdpSnapshot.java:157`
```
for(String source : sources){
    for(Map.Entry<String, Double> entry : ProjectionSources.resolve(source).entrySet()){
        ...
        out.append(String.join(",", today, source, entry.getKey(), ...
    }
}
Files.createDirectories(PROJECTIONS_CSV.getParent());
Files.writeString(PROJECTIONS_CSV, out.toString(), ...);

// CbsProjections.java:65-68
if(position.equals("QB") && best < 250){
    throw new IllegalStateException("CBS QB parse looks broken - best QB scored " + best ...
```
- scenario: Confirmed on disk. Replaying CbsProjections' regexes on the cached cbsProjections_QB_20262026-09-11.txt: the page now serves per-game rows (gp=1, 254 pass yds; first row Justin Herbert), best QB = 27.8 league points, versus 428.2 on the 09-02 cache. So resolve("cbs") throws, and because archiveProjections buffers sleeper/borischen/espn/cbs into one StringBuilder and writes only after the loop, nothing is written: data/projection-snapshots.csv ends at 2026-09-02 while data/adp-snapshots.csv (written earlier in main, line 110) has 2026-09-11, and commit 5d54626 'adp snapshot 2026-09-11' shipped only the ADP half. Root cache shows cbsProjections_QB for 09-11 but no RB/WR/TE - the loop died at the QB check. This will repeat every in-season day: the projection archive that DIAGNOSTIC #3/#7 and ProjectionSources.snapshot depend on gets no in-season rows at all.
- fix: Write each source's rows as it resolves (or catch per source and continue, printing which feed failed) so a broken fourth feed cannot lose the sleeper rows; and make CbsProjections point at a season/ROS table or drop it from automaticSources() in-season. Re-run AdpSnapshot after the fix - main's early-return path still calls archiveProjections, so today can be back-filled.
- siblings: src/main/java/CbsProjections.java:65, src/main/java/ProjectionSources.java:47

**[high/small] CBS 'season' URL serves the weekly page in-season; the gate throws and kills the daily four-feed archive after the ADP write** — `src/main/java/CbsProjections.java:21`
```
return "https://www.cbssports.com/fantasy/football/stats/" + position + "/" + season
        + "/season/projections/ppr/";
...
if(position.equals("QB") && best < 250){
    throw new IllegalStateException("CBS QB parse looks broken - best QB scored "
            + best + "; the page layout has probably changed");
}
```
- scenario: Verified on disk: cbsProjections_QB_20262026-09-11.txt has <title>Week 1 Proj Fantasy Football Stats - QB Points</title>, gp=1, best QB 27.0 - CBS now answers the /season/projections/ path with the current week's projections once the season starts. The gate fires (correctly) but with a message naming a cause that is false (the layout is unchanged; the population changed from 17 games to 1). AdpSnapshot.main writes the ADP CSV first, then archiveProjections builds sleeper, borischen, espn AND cbs into one StringBuilder and writes at the end (AdpSnapshot.java:157-169), so the exception loses every source's rows for the day. data/projection-snapshots.csv ends at 2026-09-02 while data/adp-snapshots.csv has 235 rows for 2026-09-11 (commit 5d54626 touched only the ADP file). Every in-season AdpSnapshot run ends this way; the RUNBOOK line for it still says 'fresh ADP + all four feeds'. -Pprojections=cbs and any blend:...,cbs also throw. Sleeper/ESPN/Chen day files for 09-11 exist locally (gitignored) so today's rows are still reconstructable; each further day without a fetch is not.
- fix: In archiveProjections, resolve each source in its own try/catch and archive the ones that succeeded, printing which failed; write the buffer per source. In CbsProjections, detect the in-season page by the title/gp column and say 'CBS serves week-N projections at the season URL from week 1; no season stat line is available in-season' rather than 'layout changed'; drop cbs from automaticSources() once LeagueWeek.week() >= 1, or find CBS's rest-of-season path. Add a test that the archive has a row for every date the ADP CSV has (FeedPinTest is the natural home).
- siblings: src/main/java/AdpSnapshot.java:157, src/main/java/AdpSnapshot.java:168, RUNBOOK.md:174

**[medium/small] CBS season URL now serves the WEEK page in-season; the self-check throws and the projection archive has recorded nothing since 2026-09-02 while the ADP half keeps being committed** — `src/main/java/CbsProjections.java:22`
```
return "https://www.cbssports.com/fantasy/football/stats/" + position + "/" + season
        + "/season/projections/ppr/";
...
if(position.equals("QB") && best < 250){
    throw new IllegalStateException("CBS QB parse looks broken - best QB scored "
            + best + "; the page layout has probably changed");
```
- scenario: Today's cache cbsProjections_QB_20262026-09-11.txt (759 KB, fetched 09:11) is titled "Week 1 Proj Fantasy Football Stats - QB Points": CBS now redirects the /season/ URL to the current week. All 68 rows parse, and the best QB under league scoring is 27.8, so line 65 throws. AdpSnapshot.main appends the ADP rows (line 110) BEFORE calling archiveProjections (line 122), which resolves sources in the order sleeper, borischen, espn, cbs and only writes at the end - so data/adp-snapshots.csv gained 235 rows for 2026-09-11 (commit 5d54626 "adp snapshot 2026-09-11") while data/projection-snapshots.csv's last date is still 2026-09-02, and no cbsProjections_RB/WR/TE_20262026-09-11 file exists because the loop died at QB. Every in-season day is missing from the archive that `snapshot:<date>` pins and the accuracy shootout depend on; `-Pprojections=snapshot:2026-09-11` fails with "no sleeper projection snapshot".
- fix: Write each source's rows as it resolves (or catch per source and print which halves landed), so a dead CBS cannot erase sleeper/borischen/espn for the day; give CbsProjections an in-season-safe URL or refuse cbs after week 1 with a message; have AdpSnapshot print a summary line naming both files and the dates written.
- siblings: src/main/java/AdpSnapshot.java:122, src/main/java/AdpSnapshot.java:158

**[medium/small] getTodaysWebPage pins an empty or error-wrapper payload for the day, and getCachedForever freezes an HTTP-200 error body forever** — `src/main/java/InOutUtilities.java:151`
```
if(!f.exists() || f.isDirectory()) {
    try {
        downloadTodaysWebPage(webURL, filepathStart);
    }
...
public static void downloadTodaysWebPage(String webURL, String filepathStart){
    String webContent = WebUrlUtility.urlToString(webURL);
    ...
    writeContentToFile(webContent, todaysFilePath);
```
- scenario: WebUrlUtility.urlToString rejects only blank bodies; getCachedForever additionally refuses '', '{}', '[]', 'null' (line 246) but getTodaysWebPage has no such check. A transient '[]' from /projections/nfl/2026 (the shape Sleeper returned for an unplayed week on 2026-09-04) is written as today's file and served to every tool for the rest of the day: parseTodaysWebPage returns an empty map, every projection reads 0, and the stale-fallback branch (line 165) never runs because nothing threw. The forever cache has the twin hole for wrapper bodies: ffcBoardhalfppr2010.txt..2017.txt each hold exactly {"status": "Error", "errors": "No ADP data found."} (52 bytes) - a 200 with an error object passes emptyPayload and is frozen; a one-off FFC error for a real season would be frozen the same way and NflverseBoards.all silently drops the season ('a season with no board is not a season with a bad board').
- fix: Apply emptyPayload to getTodaysWebPage/getRecentWebPage before writing (a day cache of nothing is not worth more than yesterday's copy), and extend emptyPayload to a top-level object whose 'status' is 'Error' or whose 'players'/'data' array is empty. Delete the eight 52-byte ffcBoardhalfppr files so the 2010-2017 half-PPR answer is 'not fetched' rather than 'fetched, empty, forever'.
- siblings: src/main/java/InOutUtilities.java:246, src/main/java/InOutUtilities.java:264, src/main/java/NflverseBoards.java:256

**[medium/trivial] mostRecentCached matches on prefix only, so the stale fallback for a week-1 file can serve week 14's file** — `src/main/java/InOutUtilities.java:202`
```
if(candidate.isFile() && name.startsWith(base) && name.endsWith(".txt")
        && name.length() > base.length() + 4
        && (best == null || name.compareTo(best) > 0)){
    best = name;
}
```
- scenario: SeasonOutlook.java:115-117 fetches matchups for weeks 1..14 under `"sleeperOutlook" + leagueID + "w" + week`. By week 12 the files sleeperOutlook<id>w1<date>.txt ... w14<date>.txt all exist. If the week-1 fetch fails that morning, getTodaysWebPage falls back to mostRecentCached("sleeperOutlook<id>w1"), which picks the lexicographically largest name starting with that prefix: "...w14..." beats "...w1<date>" because '4' > '2' at the character after "w1". Week 1's banked results are replaced by week 14's rows, the console prints one "STALE" line, and the playoff odds and the halfway buy/sell verdict run on wrong banked wins. Same family: sleeperLiveProjection2026w1 vs w10-w18, sleeperWeekProjection..., nflState is safe only because it has no week suffix.
- fix: Anchor the match to the date: require name.length() == base.length() + 14 and name.substring(base.length()).matches("\\d{4}-\\d{2}-\\d{2}\\.txt").
- siblings: src/main/java/SeasonOutlook.java:117, src/main/java/LeagueWeek.java:75

**[medium/small] The current league object is frozen forever with status pre_draft, so the 2026 season will never count as complete for the trade log, FAAB history, wire survival or the playoff-odds pool** — `src/main/java/LeagueTransactions.java:62`
```
static String leagueRaw(String leagueID){
    return InOutUtilities.getCachedForever(
            "https://api.sleeper.app/v1/league/" + leagueID,
            "sleeperLeagueMeta" + leagueID);
}
```
- scenario: sleeperLeagueMeta1390416723210952704.txt on disk (fetched 2026-08-31) reads status "pre_draft"; leagueChain1390416723210952704.txt (PlayoffOdds.java:56-58) reads the same, and rostersChain1390416723210952704.txt (PlayoffOdds.java:61-63) holds fpts 0 for all twelve teams. `completedSeasons` keeps only "complete", so 2026 is excluded - correct today. When the 2026 season completes on Sleeper in January the file still says pre_draft: TradePartners.records (the console's "He trades" column, LeagueConsole.java:585), FaabBid's contest harvest, WireSurvival and PlayoffOdds.history all silently omit the whole 2026 season until somebody deletes three files by hand. The chain's links (previous_league_id) are immutable; its head is not.
- fix: Read configuration.getLeagueID() through the day cache (AAAConfiguration.getLeagueJson) and only cache forever once status == complete; same for PlayoffOdds' leagueChain/rostersChain on the head league.
- siblings: src/main/java/PlayoffOdds.java:56, src/main/java/PlayoffOdds.java:61

**[medium/small] The projection archive files Sleeper's number under a rival source's label for every player that source never covered** — `src/main/java/ProjectionSources.java:121`
```
if(automatic != null){
    Map<String, Double> merged = new LinkedHashMap<>(
            SleeperProjections.parseTodaysWebPage());
    merged.putAll(automatic);
    return merged;
}
```
- scenario: resolve('espn'|'cbs'|'borischen') is Sleeper's full map overlaid with the feed, and AdpSnapshot.archiveProjections (AdpSnapshot.java:157-165) writes resolve(source) verbatim as that source's rows. Measured in data/projection-snapshots.csv: on every archived day the espn and cbs rows contain all 17 defences (ESPN's filterSlotIds and the CBS scrape are QB/RB/WR/TE only - e.g. '2026-09-02,cbs,BAL,95.0' is Sleeper's Ravens number) plus 3-26 skill players identical to the sleeper row, and borischen has 68-81 such rows. The class comment says this archive exists for 'a future accuracy shootout'; that shootout would credit ESPN and CBS with Sleeper's defence accuracy and shrink every cross-source disagreement, and nothing in the row says whether the feed actually covered the man.
- fix: Archive the raw automatic map (EspnProjections.leaguePointsBySleeperID() etc.), not the merged one - keep the merge for the planner's resolve() only - or add a 'covered' column. Backfilling is impossible for past days, so at least mark the existing rows: any espn/cbs row whose id is a defence, or equal to the sleeper row for the same date, is Sleeper's.
- siblings: src/main/java/AdpSnapshot.java:158

**[medium/small] Injury designations on the Sunday lineup tab come from the day-cached projections file, which can be many hours older than the roster it is paired with** — `src/main/java/SleeperProjections.java:34`
```
private static String getTodaysWebPage(){
    return InOutUtilities.getTodaysWebPage(getWebURL(), filepathStart + getSeason());
}
...
public static synchronized String injuryStatusOf(String sleeperID){
    if(cachedInjury == null){
        HashMap<String, String> status = new HashMap<>();
        for(JsonElement jsonPlayer : getTodaysProjections()){
```
- scenario: LeagueConsole.java:313 decides which starters get a break-even row with doubtful(SleeperProjections.injuryStatusOf(man.id())) and line 326 prints the Status column from the same call. The status is read from whichever sleeperProjections2026<today>.txt was fetched first that day. The roster feed was given a 20-minute TTL (AAAConfiguration.rosterFreshnessMinutes, line 97-104) precisely because a 00:35 copy answered a question at noon; the injury tag that gates the RUNBOOK's 'Sunday, ninety minutes before kickoff' decision has no such expiry. Scenario: ProjectionDrift or check runs at 08:00 Sunday, a starter is downgraded Questionable -> Out at 11:30 (routine for Sunday inactives), LeagueConsole is regenerated at 11:35 - he shows healthy, no break-even row, and the page reports the lineup optimal with an inactive man in it. The RUNBOOK's mitigation ('read the inactive report') is procedural, not in the page.
- fix: Fetch the projections feed through getRecentWebPage with a short TTL (the roster's 20 minutes, or -DprojectionMinutes) on game days, or at minimum refetch when LeagueWeek says the live week's games are within a day; stamp the fetch time (not just the date) in DataStamp so 'projections 2026-09-13 00:35' is visible on the page.
- siblings: src/main/java/LeagueConsole.java:313, src/main/java/LeagueConsole.java:326, src/main/java/DataStamp.java:28

**[medium/small] SeasonOutlook's roster_id-to-manager join reads the CURRENT league's rosters and users through TradePartners.managersOf, which caches them forever** — `src/main/java/TradePartners.java:77`
```
for(JsonElement element : JsonParser.parseString(InOutUtilities.getCachedForever(
        "https://api.sleeper.app/v1/league/" + leagueID + "/rosters",
        "sleeperRosters" + leagueID)).getAsJsonArray()){
```
- scenario: SeasonOutlook.java:99 calls `TradePartners.managersOf(leagueID)` with the 2026 league id. sleeperRosters1390416723210952704.txt and sleeperUsers1390416723210952704.txt were written 2026-09-07 07:35 and will never be refetched; today's seriousRostersForKeepers...2026-09-08 already differs (roster 11's players). The owner map is still identical, so today's odds are right by luck. The first co-owner swap or display-name change breaks it silently: `managerOf.getOrDefault(a, "?")` returns the stale name, banked wins are credited to a name that is not a key of `mean` (built from LeagueOwners.today, 20-minute cache), and every remaining game for that roster is simulated at `mean.getOrDefault(a, 100.0)` - a phantom 100-point team - while the real manager loses his banked wins. Nothing prints.
- fix: Give managersOf a policy: getCachedForever for a completed season, getRecentWebPage(...rosterFreshnessMinutes()) for configuration.getLeagueID(); or build roster_id->name in SeasonOutlook from the same roster JSON LeagueOwners.today reads, joining on user_id.
- siblings: src/main/java/SeasonOutlook.java:99, src/main/java/TradePartners.java:65

**[medium/small] The drift test compares the oldest and newest snapshot, so the preseason moves make it pass forever and it can never see the in-season freeze it says it exists for** — `src/test/java/ProjectionDriftTest.java:54`
```
ProjectionDrift.Drift whole = ProjectionDrift.between(
        archive.get(0), archive.get(archive.size() - 1), Map.of());
...
assertTrue(whole.moved() > 0,
        "NOT ONE of " + whole.shared() + " season projections has changed across the"
                + " whole archive. The feed is static, ...");
```
- scenario: archive() is every sleeperProjections2026<date>.txt in the working directory, oldest first; the 08-24 -> 09-07 preseason moves (30 of 585, RUNBOOK line 146) are permanently inside that span. If Sleeper's feed froze on 2026-09-02 and stayed frozen to January (the exact failure the javadoc and the RUNBOOK describe), whole.moved() stays 30 and the assertion passes every run; on a fresh clone with no day files it is skipped by assumeTrue. Measured today: 09-02 -> 09-11 changed one row (Reggie Gilliam), which this test cannot distinguish from thirty.
- fix: Assert on the newest pair (or the last N days) once LeagueWeek.week() >= 2, e.g. the drift between the newest two snapshots at least seven days apart after week 1 must be > 0; keep the whole-archive check as a separate, weaker assertion. ProjectionDrift.main already prints step rows - the test should read the same rows.

### Refuted
- **Unprojected men are written as a 0.0 projection, so blend: averages a real feed with a synthetic zero** — `src/main/java/SleeperProjections.java:66`
  - refuted: Code is quoted correctly and the mechanism is real: verified against sleeperProjections20260908.txt that 2561 rows have a stats object but no offensive-stat keys and a null pts_half_ppr, including the exact named players (Sam Howell, Kenny Pickett, Drew Lock, Hunter Long, Erick All), and traced that ProjectionSources.resolve('blend:...') would average a real ESPN number against this synthetic 0.0. But every load-bearing decision path named in the rubric (LeagueConsole:238, TuesdaySwap:221, TradeMarket:616, SeasonOutlook:90, TradeStability:84, KeeperPlan:41) hardcodes the literal 'sleeper' source, not 'blend:' — they never hit the diluting code path; plain Sleeper's own 0.0 for a deep-bench unprojected man isn't blend dilution. 'blend:' is reachable only via an explicit -Pprojections=blend:... override used in draft-simulation tools (DraftPlanner, RankDraft, PairwiseOdds, BoardSourceCheck, ObjectiveStability, Keepers16), and RUNBOOK.md:222 says outright 'DraftPlanner alone... is not a live tool' — the draft is already over (RUNBOOK.md calls itself 'the in-season runbook'). So no number Justin currently acts on changes because of this.

### Unverified (52; found by a lens, not yet refuted — verify before acting)
- [medium] README tells Justin to delete the root cache files to force a refresh; those files are the only history ProjectionDrift (weekly per RUNBOOK) and MarketMovers read, and DRAFT-READY already records the wildcard destroying it once — `README.md:92`: RUNBOOK.md:130/144 says to run ProjectionDrift once games are played to decide whether the feed is frozen and actuals must be blended in. Its entire time series is gitignored root files (/*.txt in .gitignore, whose own comment repeats 'dele
- [medium] The pre-draft fixture pin covers one of two cache names for the league object and one of two for rosters, so 'pinned' unit tests fetch the live league and TradeMarketTest runs on today's rosters while PreDraftFixtureTest runs on 2026-09-01's — `src/main/java/AAAConfiguration.java:119`: filepathStartLeague is "leagueSettings" (line 86); data/fixtures/2026-pre-draft holds seriousLeagueSleeper1390416723210952704.txt (SleeperLeague's name for the same URL) but no leagueSettings1390416723210952704.txt, so getLeagueJson() - and
- [medium] -PadpSnapshot=<date> is advertised for any planner tool but the archive it reads never contains a defence, so every DEF pins to Double.MAX_VALUE (100% survival) — `src/main/java/AdpSnapshot.java:93`: grep -c ',DEF,' data/adp-snapshots.csv = 0, while data/projection-snapshots.csv does carry SEA/CHI/LAC rows for the same days. README.md:58 says 'any planner tool also takes ... -PadpSnapshot=<date>' with no caveat, and RUNBOOK.md:344 says 
- [medium] AdpSnapshot's four-feed archive is all-or-nothing per day — `src/main/java/AdpSnapshot.java:157`: resolve() for any one source throwing (CBS since week 1; the Chen S3 bucket's SSL failure of 2026-08-29; ESPN returning non-200) discards the rows already built for the sources that succeeded, because the single write happens after the loop
- [medium] CbsProjections self-checks only the QB page; an RB/WR/TE layout change or weekly flip would pass weekly points off as season projections — `src/main/java/CbsProjections.java:65`: The class javadoc says the parse 'sanity-checks itself'; it checks one of four positions. On 2026-09-11 the season URL began serving week-1 lines for every position (QB cache: gp=1, Herbert 254 yards) and only the QB throw caught it. Had CB
- [medium] DataStamp omits the roster snapshot, so two artifacts built across a waiver claim carry identical stamps and the ladder comparison now asserts a 'ranking disagreement' that is really a roster change — `src/main/java/DataStamp.java:60`: The stamp names the newest sleeperProjections file date and the metadata file date only. Rosters expire every 20 minutes (AAAConfiguration.rosterFreshnessMinutes, getRecentWebPage) precisely because a claim on 2026-09-07 changed the team mi
- [medium] The 20-minute roster expiry (the 2026-09-07 waiver fix) has no test at all — `src/main/java/InOutUtilities.java:120`: `grep -rl 'getRecentWebPage\|rosterFreshnessMinutes' src/test/java` returns nothing. Every roster-derived number on the console (FAAB left, the wire's worth, every trade's 'he gains', keeper cost) and TuesdaySwap rests on this expiry. Chang
- [medium] mostRecentCached matches on a bare prefix, so the stale-fallback for week 1 serves week 14's file (verified against the files on disk) — `src/main/java/InOutUtilities.java:202`: SeasonOutlook.java:115-117 day-caches every week's matchups under `"sleeperOutlook" + leagueID + "w" + week` and the root already holds w1..w14 files from 2026-09-08. Emulating this loop on the working directory: mostRecentCached("sleeperOu
- [medium] getTodaysWebPage caches an empty 200 payload ('[]', '{}', 'null') for the rest of the day; the refusal exists only in getCachedForever — `src/main/java/InOutUtilities.java:273`: WebUrlUtility.urlToString (line 42) rejects only a blank body; Sleeper is documented in this repo answering "[]" for a whole endpoint on 2026-09-04. If the projections feed, the users feed or nflState answer 200 with "[]"/"{}" once, that an
- [medium] Player name index: a dead String-vs-enum guard and last-write-wins on same-name/same-position keys route feed rows to retired namesakes, bypassing the ambiguity guard the FantasyPros matcher built — `src/main/java/Player.java:254`: Replayed on the live sleeperDataPlayerAPI.json (file order = Gson insertion order): 26 same-name/same-position keys collide, 7 with an active man. 'kyle williamswr' resolves to id 638 (retired, no team) not 12547 (NE, in the 2026 feed at AD
- [medium] ProjectionDrift reads the gitignored root day-caches rather than the archive its docs name, and its test's first assertions are tautologies while the real one skips on a clean checkout — `src/main/java/ProjectionDrift.java:64`: The class comment (line 32) says "`AdpSnapshot` archives the feed daily; after real games have been played, run this", and RUNBOOK tells Justin to run it weekly to decide whether to blend actuals in. It reads `sleeperProjections<season><dat
- [medium] README's `ProjectionSources` command (and every -Pprojections=cbs / blend: run) now crashes in-season with no catch — `src/main/java/ProjectionSources.java:114`: README.md:32 lists `./gradlew run -Pmain=ProjectionSources # projection feeds: status, and where they disagree`. With CBS serving weekly rows (finding 1) resolve("cbs") throws IllegalStateException out of main after printing the slot table;
- [medium] ProjectionSources.resolve back-fills every automatic feed with Sleeper, so the projection archive labels Sleeper numbers as espn/cbs/borischen for every player those sites do not cover — `src/main/java/ProjectionSources.java:120`: AdpSnapshot.archiveProjections (line 158) writes `resolve(source)` for each automatic source as that source's rows. The merge means a man ESPN never projected is archived as `2026-09-02,espn,<id>,<Sleeper's number>`, indistinguishable from 
- [medium] WeeklyProjections writes LeagueWeek's 'immutable' cache name without the finished() gate; the week-1 file on disk was frozen four days before kickoff and will be served as week 1's final projection forever — `src/main/java/WeeklyProjections.java:31`: LeagueWeek.feed (line 56-60) promises `week < current` => getCachedForever("sleeperWeekProjection...") and otherwise the Live name, and its javadoc says the two "never share a cache name, so a week cannot be read through the wrong policy". 
- [medium] StartSit's flip curve is calibrated on WeeklyProjections' pts_half_ppr (4-pt TDs) against league-scored actuals, then applied live to LeagueWeek.projected's league-scored numbers — `src/main/java/WeeklyProjections.java:39`: Two readers of the same weekly feed return different units: WeeklyProjections hands back Sleeper's pts_half_ppr, LeagueWeek.projected (line 93) rescoring the stat line with scoreStatLine under the league's 6-point settings. StartSit.calibra
- [medium] The empty-payload refusal in getCachedForever has no behavioural test; the only test is a raw-source substring match that a comment would satisfy — `src/test/java/EmptyPayloadPolicyTest.java:22`: `grep -rl getCachedForever src/test/java` finds only this file, which never calls the method. LeagueWeekTest tests the `emptyPayload` predicate but nothing calls getCachedForever with a body of "{}" and asserts that no file is written and a
- [medium] The daily snapshot wrapper masks gradle's exit status behind a pipe and commits a half-written archive as a full 'adp snapshot' — `tools/adp_snapshot_daily.sh:24`: With the CBS failure above, gradle exits non-zero but the pipeline's status is grep's, there is no -e/pipefail, and adp-snapshots.csv did change - so the script commits the ADP rows alone and logs 'committed'. The header comment promises it
- [low] README's cache and configuration prose is stale: four cache lifetimes exist, not one, and the config file also carries a roster-cache prefix that gives TradeFinder a second 20-minute roster cache — `README.md:8`: README.md:92-94 says every response is reused 'for the rest of the day'; in code rosters expire in 20 minutes (rosterFreshnessMinutes), player metadata in 7 days (PlayerRawData.STALE_AFTER_DAYS), the user id monthly (InOutUtilities.getThisM
- [low] RUNBOOK's test count is two revisions stale and three documents disagree about how long `check` takes — `RUNBOOK.md:133`: Commit 548832e says '683 tests, none skipped' and 18fd956 says '692 tests, none skipped'; the 654 figure dates from 0a93dba. RUNBOOK tells Justin to read the count off check-wire.log against a number the build no longer produces, so a run t
- [low] The 'pre-draft' fixture's draft object was captured after the draft: status complete with last_picked set, contradicting the sibling fixture for the same URL — `data/fixtures/2026-pre-draft/draftObject1390416723227742208.txt:1`: draftObject1390416723227742208.txt (read by AAAConfiguration.getDraftJson) carries status "complete", last_picked 1788314883919 and start_time 1788309928458, while seriousDraftSleeper1390416723227742208.txt (SleeperDraftInfo, same URL) and 
- [low] The roster file is re-read on every call and can be refetched mid-run once it is 20 minutes old — `src/main/java/AAAConfiguration.java:101`: getTodaysRoster, getMyRosterPlayerIDs, getManagersWithoutKeepers and priceTodaysKeepers each re-read (and, past the TTL, re-download) the file with no in-JVM memo, unlike getLeagueJson/getDraftJson which are held. A LeagueConsole run that o
- [low] One league URL is read through five cache names and three policies; the test fixture pins only one of them — `src/main/java/AAAConfiguration.java:119`: The same https://api.sleeper.app/v1/league/<id> is cached as leagueSettings<id> (day, AAAConfiguration:119), seriousLeagueSleeper<id> (day, SleeperLeague.java:50-52), sleeperLeagueMeta<id> (forever, LeagueTransactions:62-64), leagueChain<id
- [low] getPreviousDraftID caches the previous league's JSON under the CURRENT league id, a second name for a file getPreviousDraftPicks caches under the previous id — `src/main/java/AAAConfiguration.java:162`: Line 202-203 caches the same URL as filepathStartPreviousLeague + previousLeagueID. The fixture carries previousLeagueSettings1249220546315423744.txt (the chain's form) and not previousLeagueSettings1390416723210952704.txt, so the only call
- [low] The same previous-league URL is cached under two different keys, and only one of them is in the test fixture — `src/main/java/AAAConfiguration.java:162`: getPreviousDraftID files the 2025 league object as previousLeagueSettings1390416723210952704 (keyed by the CURRENT league id) while the history walk files the identical document as previousLeagueSettings1249220546315423744. data/fixtures/20
- [low] 2027 keeper prices are read at each owner's 2026 draft slot without saying so — `src/main/java/AAAConfiguration.java:314`: Since ac62628 every keeper is priced at its owner's pick - but the only order that exists is the 2026 draft's, and the console's 'What you can sell' tab and the trade board's keeper cost are 2027 questions. If the commissioner sets the 2027
- [low] The one file edited each rollover mislabels the 2023 league id as 2024 — `src/main/java/AAAConfigurationSleeperLeague.java:10`: The fixture chain (data/fixtures/2026-pre-draft/previousLeagueSettings*.txt) reads: 1249220546315423744 = 2025, 1115743859591434240 = 2024, 980889732034994176 = 2023. README.md:6-8 calls this 'the only file that needs editing from one seaso
- [low] CSVProjectionsFP's 1.18 QB multiplier and sportRadarID keying are reachable only through IN_SEASON_FP_SITE, which nothing selects; listed as live, it is dead — `src/main/java/CSVProjectionsFP.java:88`: ScoredRoster.java:227-232 is the only caller and only under ProjectionSource.IN_SEASON_FP_SITE; no main class passes that enum value (grep finds none outside the enum, InSeasonProjectionsFP's error text and ScoredRoster). If anyone ever doe
- [low] EspnProjections fetches outside InOutUtilities: no stale-copy fallback, no empty-payload guard, and a bad body is cached for the day — `src/main/java/EspnProjections.java:39`: InOutUtilities.getTodaysWebPage:155-176 exists so 'a feed being down must not kill the tool on the clock' (its own comment) and getCachedForever refuses to freeze '{}' / '[]'. ESPN bypasses both: an outage throws straight through AdpSnapsho
- [low] ESPN cache is written before the body is validated and the client has no timeout — `src/main/java/EspnProjections.java:50`: A 200 whose body lacks 'players' (ESPN's error JSON, or an empty filter result) is cached for the day; leaguePointsBySleeperID returns an empty map (line 84-86), resolve('espn') then equals Sleeper exactly, and the archive writes a full set
- [low] EspnProjections keeps a private cache that ignores fixtureDir, has no stale fallback, and freezes any HTTP-200 body for the day — `src/main/java/EspnProjections.java:56`: Every other feed goes through InOutUtilities, which serves the fixture under -DfixtureDir and falls back to the newest cached copy when a fetch fails ("A feed being down must not kill the tool"). ESPN does neither: a 5xx throws out of AdpSn
- [low] Two FFC URLs share one day-cache key, so whichever runs first that day answers both — `src/main/java/FFCalculatorSD.java:19`: getWebURLSerious (with &position=all) and adpBySleeperID/centeredSpreadBySleeperID (without it) both cache under ffCalculatorSDSerious<season>; the static initialiser at line 27-29 fetches the first form on class load, so the second form is
- [low] FFCalculatorSD fetches in a static initializer, defeating its own go-dark try/catch; two URLs share one cache name and two different name joins read the same feed — `src/main/java/FFCalculatorSD.java:27`: adpBySleeperID/centeredSpreadBySleeperID promise "any fetch or parse trouble returns an empty map" (catch RuntimeException, line 68/105), but the first touch of the class runs initializeSeriousSDMap() in static{}; with FFC unreachable and n
- [low] FFCalculatorSD hides every fetch/parse failure as an empty map and fetches at class-load; two different URLs share one day-cache name — `src/main/java/FFCalculatorSD.java:68`: adpBySleeperID and centeredSpreadBySleeperID return an empty map on any RuntimeException (network, JSON shape, a renamed field) and the caller reads 'nobody has an ADP' / 'everyone is typical'; the class javadoc calls this 'the feature colu
- [low] Stray feed code: FantasyProsReports, WeeklyHarvest, EraExport have no callers; SleeperADP, FantasyProsADP and the two FantasyPros projection classes survive only through a test, a stray, and an enum branch nothing selects — `src/main/java/FantasyProsReports.java:15`: grep over src: FantasyProsReports, WeeklyHarvest and EraExport are referenced by nothing; SleeperADP only by StrategyBot (itself only DraftStrategyTest); FantasyProsADP only by FantasyProsReports and LeagueSmokeTest:137-158, so `smokeTest` 
- [low] Keepers.getDraftPicksTodaysWebPageSerious caches any draft's picks under one id-less name; the file on disk holds the 2026 draft although its only caller asks for the previous one — `src/main/java/Keepers.java:143`: seriousDraftIDPicks2026-09-01.txt on disk contains 24 rows with draft_id 1390416723227742208 - the 2026 draft's keeper picks - and the fixture copied it as seriousDraftIDPicks.txt, yet Keepers.getKeepersForUser passes getPreviousDraftID() (
- [low] LeagueActuals' class javadoc still says the league-scored path is opt-in and defaults off; enabled() has defaulted to true since 2026-09-04 — `src/main/java/LeagueActuals.java:31`: Lines 61-63 return `!"false".equalsIgnoreCase(System.getProperty(FLAG, "true"))` and the method's own javadoc (line 47) says ON. The class-level comment is the one quoted when someone asks what the grader does, and it states the opposite of
- [low] The console's "He trades" rate joins on display name across per-league user files cached forever, and a join miss reads as 0.00 a year — `src/main/java/LeagueConsole.java:808`: TradePartners.records keys by the display_name in sleeperUsers<pastLeagueId>.txt (getCachedForever, TradePartners.java:65-67, fetched Aug 2026); trade.withManager() is today's display name from LeagueOwners.today. I diffed all six sleeperUs
- [low] LeagueTransactions caches the CURRENT league's object forever, so its status is frozen at pre_draft and 2026 will never count as a completed season — `src/main/java/LeagueTransactions.java:61`: sleeperLeagueMeta1390416723210952704.txt was written 2026-08-31 and reads "status":"pre_draft". chain() (line 90-102) builds Year.status from it and completedSeasons()/FaabBid -Pfit (FaabBid.java:217-219 `if(!year.complete()) continue`) ski
- [low] LeagueWeek.state() day-caches /state/nfl, whose `week` flips during Tuesday - the day the runbook says to regenerate — `src/main/java/LeagueWeek.java:37`: nflState2026-09-07.txt was written 00:34 and pinned week 1 for that whole day. On Tuesday 2026-09-15, if the first tool runs before Sleeper advances week to 2, nflState2026-09-15.txt pins week 1: TuesdaySwap and LeagueConsole price the wire
- [low] NflverseWeekly.score and LeagueActuals.scoreSkill charge different category sets under the same settings object — `src/main/java/NflverseWeekly.java:158`: LeagueActuals.scoreSkill (line 167-170) also pays st_ff, st_fum_rec and fum_rec_td (6 points). data/nflverse/stats_player_week_2024.csv has a fumble_recovery_tds column (header field 90) that NflverseWeekly never reads, so a skill player's 
- [low] String compared to enum: the OTHER-position guard in the name index never fires — `src/main/java/Player.java:257`: pos is a String and Position.OTHER is an enum constant, so String.equals returns false for every player and every OTHER-position man (linemen, defenders, kickers) is indexed under name+'other'. Harmless today because the key carries the pos
- [low] Player.scorePlayer joins on sportradar id, which is empty for 24 feed rows, so every empty-id man scores as the first empty-id man in the list — `src/main/java/Player.java:433`: PlayerRawData.optionalString returns "" for a missing sportradar_id (line 79-85), and 24 non-defence rows in today's projections feed have none; one of them, Eric McAlister (WR, 13354), carries a 24.6 projection and sorts first among them b
- [low] A present-but-malformed external projection sheet is reported as 'not found' and the planner silently runs on Sleeper — `src/main/java/ProjectionBridge.java:84`: parseSource does Position.valueOf(parts[1].trim().toUpperCase()) (line 131) and Double.parseDouble on every cell; a single 'DST' row, a blank points cell, or a '#' comment with 'passTD=' misspelt throws, the catch-all returns null, and the 
- [low] ProjectionBridge swallows every parse exception and reports a malformed external CSV as 'not found - using sleeper' — `src/main/java/ProjectionBridge.java:85`: A subscriber export in data/external-projections/etr.csv with a 'DST' position (Position.valueOf throws), a stray thousands separator, or a bad '# passTD=' token makes externalSource return null; the planner prints that the file was not fou
- [low] ProjectionBridge.externalSource swallows every exception, so a malformed subscriber CSV silently becomes 'using sleeper' — `src/main/java/ProjectionBridge.java:88`: parseSource throws on one bad cell (Double.parseDouble at line 140/146, Position.valueOf at 131). externalSource returns null, pointsForSource (lines 165-169) prints "projection source 'etr' not found - using sleeper" and runs the whole too
- [low] Two comments in the feed layer describe the opposite of what build.gradle and the archive do — `src/main/java/ProjectionSources.java:53`: The suite does not run on snapshot:2026-09-01 - it runs on data/fixtures/2026-pre-draft/sleeperProjections2026.txt via fixtureDir, and the snapshot pin was abandoned for exactly the defence gap in finding 6. AdpSnapshot says its archive nev
- [low] resolve() back-fills every uncovered player with Sleeper's number and the archive labels those rows as espn/cbs/borischen — `src/main/java/ProjectionSources.java:121`: AdpSnapshot.archiveProjections (line 158) writes `ProjectionSources.resolve(source)` for every player with ADP <= 250. In data/projection-snapshots.csv for 2026-09-02, 32 of 247 "espn" rows, 43 of 247 "cbs" rows and 98 of 247 "borischen" ro
- [low] SleeperLeague and SleeperDraftInfo re-fetch the league and draft objects under second cache prefixes; the live path carries a '//todo these get___draft things are wrong' and an unused getDraft(String) — `src/main/java/SleeperLeague.java:48`: Every scoring path reaches league scoring through SleeperLeague.getSeriousLeague() (SleeperProjections.java:63) while num_teams, draft order and keepers come from AAAConfiguration.getLeagueJson()/getDraftJson(). They are two day-caches of o
- [low] scoreStatLine ignores `fum` and the DEF component line that the WEEK feed publishes, so LeagueWeek.projected is not in the league's points — `src/main/java/SleeperProjections.java:119`: The comment's premise ("the projection feed publishes no fumbles beyond fum_lost, and for a defence only a stub") is true of the season feed and false of the week feed LeagueWeek.projected scores at LeagueWeek.java:93: sleeperLiveProjection
- [low] TradeFinder reads the rosters feed under its own cache name; the fixture pins the other one — `src/main/java/TradeFinder.java:503`: AAAConfiguration.getTodaysRosterWebPageSerious (line 101-104) caches /rosters as seriousRostersForKeepers<id>; TradeFinder - the default mainClass - as leagueRostersCurrentSerious<id>. Both are 20-minute now, but they are two files fetched 
- [low] WeeklyProjections writes the LIVE week under LeagueWeek's immutable cache name, the very collision LeagueWeek's comment says cannot happen — `src/main/java/WeeklyProjections.java:31`: LeagueWeek.java:56-60 and :74-75 use "sleeperWeekProjection<season>w<week>" only when finished(week), otherwise "sleeperLiveProjection..."; WeeklyProjections uses the immutable name with getCachedForever regardless. No current caller passes
- [low] ProseDriftTest's literal scan reads raw source, so a flag name quoted in a comment satisfies 'the code reads it' — `src/test/java/ProseDriftTest.java:98`: everyFlagThePromiseMentionsExistsSomewhereInTheCode and theBuildForwardsNothingThatVanished treat any `"name"` in a .java file as evidence a knob is read. A comment such as `// used to read "window"` keeps a deleted knob 'alive' in both lin


## 2. Draft tool
confirmed 10 · refuted 2 · unverified 52

### Confirmed

**[high/small] Live board's RB/WR wire is zero: BoardValue.replacement indexes past the curve LiveBoard.thisYear builds** — `src/main/java/BoardValue.java:1227`
```
int rank = switch(position){
    case QB -> 21; case RB -> 61; case WR -> 81; case TE -> 19; default -> 13;
};
return rank < mean.length ? mean[rank] : mean[mean.length - 1];

// LiveBoard.thisYear, lines 1461-1465:
int cap = CAP.get(entry.getKey());          // RB 60, WR 72
double[] out = new double[cap + 2];
for(int rank = 1; rank <= cap && rank <= values.size(); rank++){
    out[rank] = values.get(rank - 1);
}
```
- scenario: LiveSetup.forTonight hands LiveBoard.answer the curve from thisYear(): RB is double[62] filled 1..60, WR is double[74] filled 1..72. replacement(RB) reads mean[61] (never filled -> 0.0); replacement(WR) reads mean[73] (never filled -> 0.0). QB/TE/DEF read real values (QB21, TE19, DEF13). oneSeason() then fills every unfilled RB/WR slot and both FLEX slots at 0 instead of the wire (BoardValue.java:1100 and :1112). On the 2026-09-11 Sleeper board the wire that should be there is RB61 = 54.2 and WR81 = 76.5 half-PPR. Consequences on the Draft2026 screen: (a) at pick 7 with held = [Purdy, Tuten], 'ADDS NOW' for a WR or RB is his full drawn season (~250-280) while TE's is over-the-wire (TE1 - TE19 ~ 90) - the printed column is on a different basis per position; (b) END TEAM's whole bench channel (a body is worth what he adds when the man ahead is LOST, wireWhenAllLost=true) insures RB/WR bodies against 0 rather than against the wire, systematically favouring extra RB/WR bodies over TE/QB depth in the paired comparisons the SEPARATED/TIE verdict is drawn from. The backtest that calibrated lostBelow=0.55 (BenchCalibration.ownOverWire, whose comment says 'the wire line is BoardValue.replacement, which is QB21 RB61 WR81') uses RankDraft.pointsByRank curves sized cap+1, where the same call falls to mean[mean.length-1] = RB60/WR72, nonzero - so the live tool and its calibration disagree about the wire, and neither reads the rank the comment claims.
- fix: Derive the replacement rank from InsuranceTest.replacementRanks (the source the comment cites) and clamp to the last FILLED rank of the curve (or size thisYear's array so rank 61/81 exists and is filled from the projection pool, which has 137 RBs and 214 WRs). Add a test that BoardValue.replacement(LiveBoard.thisYear(planner, kept), RB) and (…, WR) are > 0 and equal the projection at replacementRanks, and that the live and backtest curves give the same wire for the same rank.
- siblings: src/main/java/BoardValue.java:1100, src/main/java/BoardValue.java:1112, src/main/java/BenchCalibration.java:468

**[high/small] Roster attribution by state.takenAtOf survives in five siblings after 66d630b replaced it with minePicks in LiveBoard.answer only - and the 2026 draft had three off-board picks** — `src/main/java/DraftNight.java:107`
```
List<String> roster = new ArrayList<>(planner.myKeeperIDs());
for(String id : taken){
    Integer at = state.takenAtOf(id);
    if(at != null && simulator.slotAt(at) != null
            && planner.me().equals(simulator.slotAt(at).manager())){
        roster.add(id);
    }
}
```
- scenario: Commit 66d630b ('a man past ADP 250 broke both the roster and the board') added LiveBoard.minePicks - 'the nth entry of taken is the nth LIVE slot' - and changed only LiveBoard.java. The same loop still builds Model A's roster in DraftNight.answer (the bottom half of Draft2026), LiveLateRounds.main:60-67, LiveCommittee.main:58-65, LiveDraft.main:76-83 and LiveBoard.stillNeeds:1770-1778 (the 'still needs %s' line Draft2026 prints from round 8). On the real 2026 draft (draftData...2026-09-02.txt, checked with python3) the room made three live picks the board does not carry: pick 165 RB Malik Davis ADP 685, 188 RB Brooks ADP 695, 192 RB Ekeler ADP 632. From pick 165 stateAfter no longer advances, so every takenAtOf caller files each later pick one seat early: Justin's pick 175 (Bo Nix) lands in slot 174 (the slot-6 manager) and pick 176's man is credited to Justin. LiveLateRounds - the RUNBOOK's rounds-8+ tool and fallback-ladder step 1 - then priced pick 186 on a roster missing Nix and holding a stranger, and it prints no SCHEDULE DRIFT warning (grep: no scheduleDrift call in LiveLateRounds/LiveCommittee/WaitCheck). In Draft2026 the two halves of the same frozen screen show different rosters.
- fix: Make LiveBoard.minePicks the single attribution: call it from DraftNight.answer, LiveLateRounds.main, LiveCommittee.main, LiveDraft.main and LiveBoard.stillNeeds (all already hold planner, simulator and taken). Have LiveLateRounds print DraftNight.scheduleDrift/scheduleOwnerDrift like the other two live tools. Extend OffBoardPickTest to assert DraftNight's roster equals LiveBoard's on a board containing an off-board pick.
- siblings: src/main/java/LiveLateRounds.java:60, src/main/java/LiveCommittee.java:58, src/main/java/LiveDraft.java:76, src/main/java/LiveBoard.java:1770

**[high/small] LiveLateRounds never prints the schedule-drift detector; on the real 2026 draft it was one seat behind from pick 165 and would have told Justin his pick 175 was not his** — `src/main/java/LiveLateRounds.java:53`
```
List<String> taken = LiveDraft.livePicks(draftID);
DraftSimulator.SimState state = simulator.stateAfter(taken);
DraftSimulator.Slot slot = simulator.slotOf(state);
if(slot == null){
    System.out.println("no picks of mine left on the board");
    return;
}
...
int nextPick = nextPickAfter(simulator, state, planner, slot.pickNumber());
```
- scenario: stateAfter advances the schedule only for a man on the 250-ADP board (DraftSimulator.java:643-646). DraftNight.answer and LiveBoard.answer both call DraftNight.scheduleDrift and print '*** SCHEDULE DRIFT'; this tool - the one the runbook names for rounds 8-16 - does not. In the cached 2026 draft (livePicks1390416723227742208 2026-09-04) pick 165 was Malik Davis, RB, ADP 685.1, taken by another manager, so from there stateAfter sat one seat behind. At Justin's real pick 175 this tool would have reported 'pick 174 (round 15) belongs to someone else / (not my pick - this is the read for when it is)', taken nextPickAfter(...,174) = 175 as his next pick, simulated ONE pick for survival (174 -> 175) and printed ~95-100% SURVIVES and 'wait - he keeps' on every row - while he was on the clock and the real wait was eleven picks to 186. Pick 188 (Chris Brooks, ADP 695) put it two behind for pick 192.
- fix: Print DraftNight.scheduleDrift(simulator, state, taken.size()) and DraftNight.scheduleOwnerDrift(...) at the top of main, as LiveBoard.answer does at lines 166-177, and either refuse to print verdicts or re-sync the state (advance the schedule one live slot per off-board id) while it fires. Add a test replaying the 2026 picks through pick 165 and asserting the tool reports pick 175 as MINE.
- siblings: src/main/java/LiveCommittee.java:51, src/main/java/WaitCheck.java:48, src/main/java/LiveDraft.java:59, src/main/java/Tomorrow.java:64, src/main/java/LateSurvival.java:76

**[high/small] LiveLateRounds still builds Justin's roster by takenAtOf and has no schedule-drift detector - the two faults fixed in LiveBoard on 2026-09-01 were never swept here** — `src/main/java/LiveLateRounds.java:62`
```
List<String> mine = new ArrayList<>(planner.myKeeperIDs());
        for(String id : taken){
            Integer at = state.takenAtOf(id);
            if(at != null && simulator.slotAt(at) != null
                    && planner.me().equals(simulator.slotAt(at).manager())){
                mine.add(id);
            }
        }
```
- scenario: Commit 66d630b ("a man past ADP 250 broke both the roster and the board") replaced exactly this loop in LiveBoard with LiveBoard.minePicks (seat-walk attribution) and LiveBoard.answer/DraftNight.answer both print DraftNight.scheduleDrift; LiveLateRounds got neither (grep: no scheduleDrift/scheduleOwnerDrift/minePicks in the file). RUNBOOK line 323 says "At each pick from round 8 on, run the live late-rounds tool" and the fallback ladder names it for rounds 8+. On the real 2026 board somebody drafted RB Malik Davis (ADP 686, not on the 250-cut board) at pick 175 (DRAFT-READY item 8). After any such pick DraftSimulator.stateAfter stops advancing, so every later takenAtOf lands one seat early: LiveLateRounds prints 'pick 185 (round 16) belongs to someone else' at Justin's 186, prices survival over one extra pick, and had the off-board man been drafted before 162 Justin's own 162/175 picks would be attributed to the neighbouring seat and dropped from `mine` - base = value.of(mine) at line 84 and every ADDS/LOSS IF WAIT row are then priced on the wrong roster (e.g. a second TE recommended because the first was forgotten), with nothing on screen saying so.
- fix: Replace the loop with `List<String> mine = LiveBoard.minePicks(planner, simulator, taken);` and print `DraftNight.scheduleDrift(simulator, state, taken.size())` and `DraftNight.scheduleOwnerDrift(...)` before the table, exactly as DraftNight.answer does. Add a test that freezes a board containing an off-board id via LiveDraft.freezeWith and asserts the roster and the drift warning (the same shape as OffBoardPickTest).
- siblings: src/main/java/LiveCommittee.java:60-66 (same takenAtOf loop, no drift detector), src/main/java/LiveDraft.java:76-83 (same loop, no drift detector)

**[high/small] LiveLateRounds.survival lets the room model spend MY pick, deflating SURVIVES for the very man being priced (WaitCheck fixed this; the copy did not get it)** — `src/main/java/LiveLateRounds.java:215`
```
static double survival(DraftSimulator simulator, DraftSimulator.SimState state,
                       DraftPlanner planner, String id, int nextPick, int trials){
    int survived = 0;
    for(int trial = 0; trial < trials; trial++){
        Random random = new Random(661_000L + 7919L * trial);
        DraftSimulator.SimState branch = state.copy();
        while(simulator.slotOf(branch) != null
                && simulator.slotOf(branch).pickNumber() < nextPick){
            simulator.simulateOneFrom(branch, random);
        }
        if(branch.takenAtOf(id) == null){
            survived++;
 
```
- scenario: When it is Justin's pick (slot.pickNumber() < nextPick), the first simulateOneFrom fills HIS seat with roomChoice() - the shared room model, since managerModels is empty - and the room's favourite at his seat is very often the best available man at the position being priced. He is then counted 'gone' in a world where Justin waited. WaitCheck.report (WaitCheck.java:82-110) names exactly this fault and fixes it with branchWith(state, spendOn.get(position)); LiveLateRounds (written 2026-08-30, two days after that fix) starts from state.copy() instead. On the 2026 board at pick 90 the RUNBOOK example row 'TE Kelce SURVIVES 48% / LOSS IF WAIT 7.9 / lean take' is biased low on SURVIVES and high on LOSS by however often the room-model-Justin takes Kelce first, so every 'TAKE NOW' verdict in rounds 8-16 is inflated. Smaller sibling: LiveBoard.drain (LiveBoard.java:1608-1612) prices VS WAIT as expectedGone(next) - expectedGone(pick), which includes the room model's expected consumption at Justin's own seat.
- fix: Branch with the pick spent elsewhere, as WaitCheck does: DraftSimulator.SimState branch = simulator.branchWith(state, spendOn) where spendOn is the best man at a different position (or, if it is not Justin's seat, state.copy()); then loop while slotOf(branch).pickNumber() < nextPick. Consider a MyPolicy that skips `id` at Justin's seats for both this and LiveBoard.drain, and pin with a test that a target is never taken by Justin's own simulated seat.
- siblings: src/main/java/LiveBoard.java:1608, src/main/java/WaitCheck.java:109

**[high/small] LiveLateRounds.survival lets the room model spend MY pick, so the best man is counted 'gone' in worlds where I waited** — `src/main/java/LiveLateRounds.java:221`
```
DraftSimulator.SimState branch = state.copy();
while(simulator.slotOf(branch) != null
        && simulator.slotOf(branch).pickNumber() < nextPick){
    simulator.simulateOneFrom(branch, random);
}
if(branch.takenAtOf(id) == null){
    survived++;
}
```
- scenario: When it is Justin's pick, slotOf(state) is his own slot and nextPick is his pick after it (nextPickAfter requires pickNumber() > after), so the first simulateOneFrom advances HIS slot through roomChoice(state, slot, random) - the fitted room model picks for him - and whenever it takes the very man being priced he is counted gone. WaitCheck fixed exactly this ('My own pick must NOT be left to the generic manager model … simulated me would sometimes take the very player whose survival is being measured') by branching with branchWith(state, spendOn.get(position)); this tool never got the sweep. SURVIVES is understated by the room model's probability of taking the top man at that position from Justin's seat, LOSS IF WAIT = (1 - survives) x drop is overstated, and 'lean take'/'TAKE NOW' verdicts fire spuriously. RUNBOOK 'At each pick from round 8 on, run the live late-rounds tool' names this as the rounds-8+ decision tool and its fallback ladder item 1; the sample table in the runbook (SURVIVES 0% / 16% / 48%) was produced with the self-pick included.
- fix: Before the loop, when slotOf(state).manager() equals planner.me(), branch with a spend on the best man at a different position (as WaitCheck.report does with spendOn) or advance the schedule index past my slot without applying a pick; then simulate to nextPick. Pin with a test on a fixture state where the room model's top choice for my seat is the man being priced: survival must not depend on my own seat's model.
- siblings: src/main/java/WaitCheck.java:109, src/main/java/LateSurvival.java:88

**[high/trivial] Training computes the pair/wait features on a nine-round pick list while the simulator computes them on the sixteen-round schedule - train/serve skew for every round-9-to-16 observation the 16-round fit now includes** — `src/main/java/SelectionModel.java:583`
```
Map<String, List<Integer>> livePickNumbers = new HashMap<>();
Set<String> allManagers = new HashSet<>();
for(JsonObject pick : picks){
    JsonElement by = pick.get("picked_by");
    if(by != null && !by.isJsonNull() && pick.get("round").getAsInt() <= GAME_ROUNDS){
        livePickNumbers.computeIfAbsent(by.getAsString(), u -> new ArrayList<>())
                .add(pick.get("pick_no").getAsInt());
        allManagers.add(by.getAsString());
    }
}
...
int next = slot >= 0 && slot + 1 < mine.size() ? mine.get(slot + 1) : Integer.MAX_VALUE;
int previous = slot > 0 ? mine.get(slot - 1) : Integer
```
- scenario: Under LiveSetup (scheduleRounds=16) SelectionModel.trainRounds() returns 16 (line 175-177) and BoostedSelectionModel.fitShipped trains on rounds 1-16 with all 30 features, including f11-f13 = firstOfPair, secondOfPair, waitFraction times f0 (lines 820-822). But livePickNumbers is still filtered at `round <= GAME_ROUNDS` (9), so for a round-9 pick `next` is MAX_VALUE (waitFraction 1.0, no pair flag) and for rounds 10-16 `slot` is -1 (waitFraction 1.0, both flags false). DraftSimulator's constructor (lines 142-161) builds turnShape over every live slot of the 16-round schedule: Justin's round-9 pick 103 has next=114, waitFraction 11/24=0.46; a slot-12 manager's round-9/10 pair is flagged. So the trees learn 'waitFraction is always 1.0 from round 9 on' and are then asked about 0.46 at exactly the rounds the survival table (LiveBoard.warmSurvival), LiveLateRounds.survival and LateSurvival simulate. The 2021-2025 fit is the room model under every rounds-8-16 number on the screen.
- fix: Filter livePickNumbers at `round <= maxRound` (the trainRounds() the caller passed), not GAME_ROUNDS; keep `teams` from the same population. Add a test that seasonObservations' waitFraction for a round-9 pick equals DraftSimulator's turnShape for the same pick when maxRound is 16.
- siblings: src/main/java/DraftSimulator.java:150

**[medium/small] RUNBOOK's fallback 'DraftNight -Pkeepers=Tuten,Purdy' silently ignores -Pkeepers and warms a different Model A than Draft2026 (9-round schedule, 13-round fit, Sleeper-declared keepers only) - today that is zero keepers** — `src/main/java/DraftNight.java:41`
```
int last = Integer.parseInt(configuration.getSeason()) - 1;
Map<String, Double> earliness = SelectionModel.qbEarliness(configuration, last);
ChoiceModel model = BoostedSelectionModel.fitShipped(configuration, last, earliness);
DraftPlanner planner = DraftPlanner.forCurrentSeason(configuration, List.of(),
        model, earliness);
```
- scenario: LiveSetup.forTonight (the 'ONE PLACE THE LIVE PATH IS WARMED') sets scheduleRounds=16 before fitting - so SelectionModel.trainRounds() returns 16 - and passes DraftPlanner.keepersFromProperty(configuration). DraftNight.main, WaitCheck.main:38-45, LiveCommittee.main:40-47 and LiveDraft.main:45-54 do neither: trainRounds() is 13, the schedule is nine rounds with no defences, and the keepers are List.of() = whatever Sleeper's roster keepers field says. RUNBOOK.md:220 tells Justin to run `DraftNight -Pkeepers=Tuten,Purdy -q` as fallback step 1; the flag is accepted by build.gradle and never read. On draft night the declared list coincided with the flag; today seriousRostersForKeepers...2026-09-08.txt holds 0 declared keepers (Sleeper emptied the field on 2026-09-02, per InOutUtilities.java:97-102), so the fallback DraftNight builds a world in which Justin holds nobody, needs a QB and an RB, and the other eleven seats have no keeper slots either - while Draft2026 with the flag still holds Tuten and Purdy. Two different Model As under one runbook line.
- fix: Route DraftNight.main, WaitCheck.main, LiveCommittee.main and LiveDraft.main through LiveSetup.forTonight() (as PreFlight and LiveBoard.main already are), or at minimum pass DraftPlanner.keepersFromProperty(configuration) and set scheduleRounds the same way. Add a source test in the OneWarmUpTest family asserting no live main constructs DraftPlanner.forCurrentSeason with List.of().
- siblings: src/main/java/WaitCheck.java:41, src/main/java/LiveCommittee.java:43, src/main/java/LiveDraft.java:50, src/main/java/LiveSetup.java:64

**[medium/small] LiveBoard.rolloutRoster types Justin's fourteen pick numbers - the seats END TEAM is priced on - while RosterRules and the simulator compute them; seven copies of the literal exist** — `src/main/java/LiveBoard.java:962`
```
int[] picks = {7, 18, 31, 42, 55, 66, 79, 90, 103, 114, 127, 162, 175, 186};

// THE TAIL MUST END WITH A LEGAL LINEUP.
```
- scenario: rolloutRoster fills the tail of the draft at these seats and BoardValue.empirical of the result is the END TEAM column, 'what the verdict ranks on'. RosterRules.livePicks(justinsKeepers()) derives the same list ('computed here, not typed', RosterRules.java:315-317) and DraftSimulator.pickNumbersOf(planner.me()) has the live schedule's answer; neither is consulted. Right on the 2026 draft (slot 7, keepers at r12/r13, verified against draftObject...2026-09-08.txt). Wrong, silently, for any `-PdraftId=<mock>` rehearsal where Justin sits elsewhere or the mock carries different keeper rounds - the rehearsal RUNBOOK.md:175/182 says must always be run - and for every future season: the tail then prices seats Justin does not own while the wait table above it uses the real schedule. Same literal in BoardValue.java:137, PlanBacktest.java:86, DefenceTiming.java:21, PairwiseOdds.java:450, RankDraft.java:48 and BenchCalibration.java:87 (first seven).
- fix: Replace with planner.simulator().pickNumbersOf(planner.me()) (already computed for DraftPlanner.myPickNumbers) or RosterRules.live().livePicks(...); add a test asserting rolloutRoster's seats equal the simulator's live seats for Justin on the fixture, and grep-pin that no main-source file carries the literal.
- siblings: src/main/java/BoardValue.java:137, src/main/java/PlanBacktest.java:86, src/main/java/DefenceTiming.java:21, src/main/java/PairwiseOdds.java:450, src/main/java/RankDraft.java:48

**[medium/small] LiveLateRounds has no legality gate: it prices a second TE, third QB or second DEF that LiveBoard refuses through RosterRules and MOST** — `src/main/java/LiveLateRounds.java:90`
```
for(Position position : new Position[]{Position.RB, Position.WR, Position.TE,
        Position.QB, Position.DEF}){
    ...
    String candidate = bestAvailable(simulator, state, planner, position);
    if(candidate == null && position == Position.DEF){
        candidate = bestDefence(planner, taken);
    }
    if(candidate == null){
        continue;
    }
    List<String> trial = new ArrayList<>(mine);
    trial.add(candidate);
    best.put(position, candidate);
    adds.put(position, value.of(trial) - base);
}
```
- scenario: LiveBoard.answer offers every candidate to RosterRules.whyNotDraft and BoardValue.MOST before pricing (lines 392-409: 'REFUSED: already hold 1, which is all this roster wants'). LiveLateRounds - the RUNBOOK's rounds-8+ tool (RUNBOOK.md:323-325) and fallback-ladder step 1 - references neither (grep: no RosterRules/MOST/ceiling in the file). WeeklyStarterValue credits a bench man's promotion weeks, so with Fannin held it will print a positive ADDS for a second tight end in rounds 10-16, the pick RUNBOOK rule 1 ('Never take a SECOND tight end from round 10 on') forbids, and with Purdy plus a stash it will price a third quarterback and, after the Ravens at 162, a second defence. Tomorrow.java:133-134 admits it in prose ('LiveLateRounds has no cap and will price one anyway'), which is the TRAPS A1 standard - 'a model that COULD draft three quarterbacks and happens not to is not fixed' - failed by the tool the runbook names.
- fix: Build the rules roster via LiveBoard.rulesRoster(planner, simulator, state, mine, declined) and skip (or print REFUSED for) any position where roster.whyNotDraft(position, slot.round()) != null or mine already holds BoardValue.MOST at it, exactly as LiveBoard.answer does.
- siblings: src/main/java/LiveBoard.java:398, src/main/java/Tomorrow.java:133

### Refuted
- **Tomorrow.java still scans for 'a slot that is mine' without keeperSlot() - the fault ab7ba3e fixed in LiveBoard and Draft2026 - so at pick 127 it prices the wait to 138, not 162** — `src/main/java/Tomorrow.java:69`
  - refuted: Part (a) confirms the code: Tomorrow.java's two scans at lines 69-73 and 82-87 use `planner.me().equals(mine.manager())` on `DraftSimulator.Slot` with no `!keeperSlot()` guard — exactly the pre-fix pattern ab7ba3e removed from LiveBoard/Draft2026. Tomorrow does set scheduleRounds=16 at startup, so keeper slots (Tuten at 138, Purdy at 151) are live and reachable by this scan; the failure scenario is real in isolation.

However part (b) fails: the finding's premise that Tomorrow is "listed live" is false. RUNBOOK.md explicitly says "The live tool is `./gradlew run -Pmain=Draft2026 -Pkeepers=Tuten,Purdy -q`" and lists DraftNight/LiveLateRounds as the two live-draft commands for rounds 1-7 and 8+. README.md likewise marks Draft2026 as "the live tool" and lists other tools (including one explicitly marked "superseded"). Tomorrow.java is not mentioned in RUNBOOK.md, README.md, or DRAFT-READY.md at all, is not called from any other source file, and is not referenced by any test. It has no wiring into the documented draft-night workflow Justin actually follows — there is no path by which its WAITING-until-pick-X number or MY PICK header reaches a decision Justin acts on. Since the artifact Justin actually reads during a draft is Draft2026/LiveLateRounds/DraftNight (already fixed by ab7ba3e), and Tomorrow is an orphaned/unreferenced tool, the bug does not currently change any number Justin acts on.
- **Three definitions of 'kept': LiveBoard.kept() reads only Sleeper's declared list, the planner adds -Pkeepers extras, RosterRules types Tuten r12/Purdy r13 - and today Sleeper's list is empty** — `src/main/java/LiveBoard.java:1435`
  - refuted: Part (a) checks out: the quoted kept() is verbatim at LiveBoard.java:1435, and tracing LiveSetup.forTonight() confirms the mechanism — planner = DraftPlanner.forCurrentSeason(configuration, keepersFromProperty(...), ...) folds -Pkeepers extras into the board/simulator, while `Set<String> kept = LiveBoard.kept(configuration)` (declared-Sleeper-only) feeds thisYear()/bestAvailable()/PreFlight's count. If both were run today with -Pkeepers set, the described divergence would occur. But part (b) fails: RUNBOOK.md's own first line is "The draft is done. This is what the rest of 2026 looks like," and its weekly-loop instructions name only TuesdaySwap and LeagueConsole ("Regenerate BOTH... In that order, back to back"). Neither of those files references LiveBoard, DraftPlanner, or -Pkeepers at all (grep found zero hits), and the most recent seriousRostersForKeepers snapshot (2026-09-08) already carries keepers:null, consistent with the draft having concluded. LiveBoard/PreFlight/-Pkeepers are draft-night-only tooling — exactly the scenario the finding frames around ("keepers priced by -Pkeepers before the commissioner enters them") — and that scenario no longer arises because the draft it was for has already happened. No number on the console page, TuesdaySwap, or any report Justin actually reads this week passes through LiveBoard.kept() or the -Pkeepers path, so no decision-facing number changes today.

### Unverified (52; found by a lens, not yet refuted — verify before acting)
- [medium] RUNBOOK tells him to read the test count and '0 skipped' from check-wire.log; the `test` task has no testLogging so the log prints neither, and the quoted '654 tests' is stale (688 non-smoke @Test now, 652 when the line was written) — `RUNBOOK.md:153`: The committed check-wire.log (2026-09-08) contains no test count and no skipped count, so the instruction cannot be followed as written. A fixture-dependent test that starts hitting an Assumptions.assumeTrue (the pattern TRAPS #43-45 catalo
- [medium] RUNBOOK says `smokeTest -PdraftId` exercises 'the entire live-draft path, which is where every draft-night defect lived'; the seven MockDraftSmokeTest checks exercise SleeperLiveDraft/DraftProgress/LiveDraftInfo - the tool README marks superseded - and never touch LiveDraft.livePicks, DraftSimulator.stateAfter, LiveBoard or Draft2026 — `RUNBOOK.md:182`: Every 2026 draft-night defect (drift detector firing 137/168, keeper-slot scan answering 'nothing legal' from pick 113, branchWith spending into a keeper slot, Malik Davis named after being drafted) lived in LiveDraft/DraftSimulator/LiveBoa
- [medium] RUNBOOK's 'Before the draft' and 'During the draft' sections tell him to start and hold DraftNight open all night, describe Draft2026's timings and board model under that name, and give a reading rule ('All four engines agree + KN says PROVEN') that the code can no longer produce; the fallback ladder 25 lines later, README and DRAFT-READY all say Draft2026 is the live tool — `RUNBOOK.md:189`: Lines 177/189-191 date from 2026-08-28/29 (git blame), before Draft2026 existed (created 2026-09-01, 30c8b11). Followed literally, he starts DraftNight standalone: DraftNight.main never sets scheduleRounds=16, never calls LiveBoard.warmSurv
- [medium] build.gradle declares the five .md files as test inputs but not the data artifacts and fixture directory the draft tests read - a changed pin or artifact leaves `test` UP-TO-DATE (TRAPS #45) — `build.gradle:58`: The comment above these lines says 'a test that reads a file gradle does not know about is UP-TO-DATE when that file changes, and passes with a planted fault in it'. HindsightRegressionTest reads data/wire-rate-stress-2026-09-04.txt (line 4
- [medium] Inside Draft2026, Model A's roster (DraftNight.answer) and the round-8+ 'still needs' line (LiveBoard.stillNeeds) still attribute picks by takenAtOf, not by seat — `src/main/java/DraftNight.java:107`: Draft2026 calls DraftNight.answer for rounds 1-7 and LiveBoard.stillNeeds (LiveBoard.java:1770-1778, identical loop) for rounds 8-16. The board-model half of the same screen uses LiveBoard.minePicks. If Justin spends pick 114 or 127 on a ti
- [medium] Roster attribution fix (DRAFT-READY #8) landed only in LiveBoard.minePicks; DraftNight, LiveLateRounds, LiveCommittee, LiveBoard.stillNeeds and LiveDraft still build 'my roster' by takenAtOf and drop any man past ADP 250 — `src/main/java/DraftNight.java:107`: Real 2026 draft: Justin took RB Malik Davis (ADP 686) at pick 175 (DRAFT-READY.md:388-390). From that moment LiveBoard.answer (fixed) knows he holds him, but Draft2026's round-16 'still needs' line (LiveBoard.stillNeeds:1770-1778, same take
- [medium] LateSurvival's quoted stash table lets simulated-Justin take the targets, so survival to his later picks is a lower bound — `src/main/java/LateSurvival.java:88`: The runbook's 'Take Nix or Dart at 114, or at 127 at the latest' rests on this table (74%/42%/6% at 114/127/162). Every slot including Justin's own is played by roomChoice, so a target simulated-Justin himself took at 103, 114 or 127 is cou
- [medium] LiveBoard.answer ranks a candidate by depth() (men gone + 1) on the branch that skips simulation, against projection rank everywhere else - the rule its own comment rejects — `src/main/java/LiveBoard.java:410`: The simulation block runs only `if(taken.size() < pick - 1)` (line 259) - a pick COUNT against a pick NUMBER, the TRAPS #30 shape - so on Justin's own turn before any keeper slot has passed (picks 7 and 18) ranksAt is empty and `rank` falls
- [medium] LiveBoard.rolloutRoster hard-codes Justin's 2026 seats and the 16-man roster; END TEAM runs the wrong tail on any other draft or slot — `src/main/java/LiveBoard.java:962`: END TEAM - the column the runbook says to rank on - is the value of the roster this tail finishes, and the tail walks a typed copy of slot 7's fourteen live picks with keepers at rounds 12 and 13 rather than simulator.pickNumbersOf(planner.
- [medium] The board model's verdict chain - drain(), tiers()/nextValley() (NEXT CLIFF), tooFragile() (REFUSED fragile) and answer() itself - has no test; only source scans touch it — `src/main/java/LiveBoard.java:1574`: Grep of src/test for `drain(`, `nextValley`, `tiers(`, `tooFragile`, `summarise(`, `fragilityBar` finds no caller; `LiveBoard.answer(` appears only in CycleSurvivesAFailureTest and SurvivalDependentToolsTest as a string. These produce, per 
- [medium] LiveBoard.stillNeeds and every Model A roster loop still attribute Justin's picks by takenAtOf, the rule minePicks replaced — `src/main/java/LiveBoard.java:1771`: LiveBoard.minePicks (1800-1815) exists because 'stateAfter only places a man it carries - so if Justin spent a pick on anyone past ADP 250 … his own man vanished from his own roster … and stayed one short for the rest of the night'. stillNe
- [medium] Model A's committee (vote/lookahead/hindsight/greedy/voteRecord), WaitCheck.report and TimingPlanner.bestAvailable/vorpPosition have no test - only topOf and fitRollouts are covered — `src/main/java/LiveCommittee.java:94`: Grep of src/test for `LiveCommittee.vote`, `lookahead(`, `hindsight(`, `voteRecord`, `WaitCheck.report`, `vorpPosition`, `bestAvailable(` returns nothing. vote() produces the 'N of 4 engines say RB -> <name>' line, the SPLIT VOTE / 'one voi
- [medium] LiveLateRounds' verdict bars (3 and 10 points) were never compared with the objective's own seed-to-seed spread; the RUNBOOK's sample rows 'lean take (4)' sit inside the 6.8-point floor ObjectiveStability measured for a WeeklyStarterValue marginal — `src/main/java/LiveLateRounds.java:123`: ADDS is V(roster+him) - V(roster) on the same WeeklyStarterValue the console uses, whose marginal moves up to 6.8 points between seeds at 480 scenarios (about 4.3 if it scales as 1/sqrt(n) at 1200). LOSS IF WAIT = P(gone) x max(0, ADDS - fa
- [medium] Tomorrow's 'my pick' and 'next pick' scans never ask keeperSlot() - the ab7ba3e fault, unswept - so the wait table is priced to Tuten's keeper slot (138) instead of pick 162 — `src/main/java/Tomorrow.java:69`: Commit ab7ba3e fixed three scans (LiveBoard next-pick, LiveBoard.nextPickAfter, Draft2026.roundNow) and KeeperSlotScanTest pins that scanning forward from 130 must land on 162, not 138. Tomorrow was not touched. On the 2026 schedule (slot 7
- [medium] Tomorrow.java locates 'my pick' and 'my next pick' by scanning slotAt without keeperSlot() - the exact fault fixed in Draft2026.roundNow and LiveBoard on 2026-09-01; Tomorrow (written 2026-08-31) was not swept and KeeperSlotScanTest re-implements the loop instead of calling the tool — `src/main/java/Tomorrow.java:69`: Tomorrow forces scheduleRounds=16 (line 53), so Tuten's keeper slot 138 and Purdy's 151 carry Justin's manager id (DraftPlanner.java:674-680 builds every slot with its manager, keeperSlot=true). On the clock at pick 127 - the seat the RUNBO
- [medium] Tomorrow's seat scans do not skip keeper slots: at pick 127 it prices the wait to Tuten's slot 138, not to 162 — `src/main/java/Tomorrow.java:71`: LiveBoard.answer (lines 189-190) and Draft2026.roundNow (203-204) were fixed to require !keeperSlot() ('A KEEPER SLOT IS NOT A PICK … Tuten sits in pick 138 and Purdy in 151'); Tomorrow was not swept. On the 2026 schedule, when Justin is on
- [medium] AppetiteCapTripwireTest cannot fail: adaptiveDraft enforces MOST inside its candidate loop, so `used <= cap` and `quarterbacks <= 2` hold by construction — `src/test/java/AppetiteCapTripwireTest.java:76`: BoardValue.adaptiveDraft (BoardValue.java:314-317) reads `if(have.getOrDefault(position, 0) >= MOST.get(position) || ... ){ continue; }` and increments `have` at line 401, so no roster it returns can ever exceed a MOST entry; the second tes
- [medium] The pinned 'hindsight premium' (0.98) compares the artifact's ADP-band reimplementation (9.01) against honest; the number that actually ships (wireRates = 8.74, per the same artifact) is never compared to anything — `src/test/java/HindsightRegressionTest.java:353`: measuredShippedRatePerWeek() reads the row `SHIPPED pooled top quartile 9.01` (data/wire-rate-stress-2026-09-04.txt line 20), but line 4 of the same file says `SHIPPED  WeeklyStarterValue.wireRates(DEF) = 8.74 a week` and line 14 explains t
- [medium] HindsightRegressionTest.theHonestWireIsOptInSoTheFrozenToolsDoNotMove asserts a system property the test JVM never sets - it passes whatever WeeklyStarterValue does — `src/test/java/HindsightRegressionTest.java:371`: build.gradle's `test` task forwards no -P knobs into the test JVM (only the `run` task does), so Boolean.getBoolean("honestWire") is false regardless of what WeeklyStarterValue.wireRates reads. If wireRates were changed to `Boolean.parseBoo
- [medium] KeeperSlotScanTest re-implements the next-pick scan instead of calling the three production scans it was written to guard — `src/test/java/KeeperSlotScanTest.java:59`: The loop under test lives in the test. The production scans - the inline loop in LiveBoard.answer (LiveBoard.java:180-192), LiveBoard.nextPickAfter (1653-1665) and Draft2026.roundNow (190-209) - are never invoked, so deleting `&& !mineAt.ke
- [medium] TwoObjectivesTest.theDifferenceIsWhyModelAIsSilentAfterRoundSeven asserts Draft2026.java contains the word 'only' - it cannot fail — `src/test/java/TwoObjectivesTest.java:54`: Draft2026.java uses 'only' in its class comment, in 'the only one that speaks after round 7', in '(Model A is silent from round 8 - it is only proven in rounds 1-7' and elsewhere. Delete the whole round-8 explanation block (lines 146-168) a
- [low] DRAFT-CARD.md (2026-08-28) contradicts RUNBOOK on the command to run, the round-7/8 plan, the Tuten story, the kicker, the pick list and the first fallback - and no test scans it — `DRAFT-CARD.md:10`: The one-page card - the artifact designed to be printed for the table - names a superseded command, nine picks instead of fourteen, the TE-at-79 plan the RUNBOOK measured at -96.5, a kicker the league does not start, a Tuten origin the RUNB
- [low] Warm-up and cycle timings disagree across DRAFT-READY, RUNBOOK, DIAGNOSTIC and CycleTiming's own javadoc: 30s/25s at the top of DRAFT-READY versus its own 27.4s correction, ~40s in RUNBOOK, '25s of 60' in DIAGNOSTIC, and a '4-11s' quote that no document still makes — `DRAFT-READY.md:7`: The first eleven lines of the document he reads at the table quote the number its own 'The clock, measured' section retracts (TRAPS #64). The direction is the dangerous one CycleTiming's javadoc names: an underquoted cycle and a press at :4
- [low] RUNBOOK's LateSurvival table has no committed artifact and no pin; the StashValue/StarterRisk/BenchValue artifacts it quotes predate the TRAPS #80/#84 re-grades and are pinned by nothing — `RUNBOOK.md:377`: `ls data` shows stash-value-2026-08-29.txt, starter-risk-2026-08-29.txt, bench-value-2026-08-29.txt (all matching the RUNBOOK's 101.9/0.90x/44.0-32.8-31.2 today) but no late-survival-*.txt at all, so the 74%/42%/6% Nix table was pasted from
- [low] RUNBOOK and the DraftNight screen call Justin's keeper pair 'the league's weakest (+27 against JakeSK's +118)'; DIAGNOSTIC's Keepers16 result puts it at +67 with BHier weakest at +11 and says Tuten + Purdy 'ARE the best pair his roster offered' — `RUNBOOK.md:405`: Two yardsticks (the nine-round ledger and the sixteen-round Keepers16 objective) give opposite rankings of the same pair; the runbook and the on-screen text assert one as fact without naming the yardstick, and the runbook uses it to explain
- [low] BenchValue's next-season wire falls back to rank 24 for every position while this season's falls back to 2x the starter rank — `src/main/java/BenchValue.java:225`: Only bites when InsuranceTest.replacementRanks lacks a position (it derives ranks from previous drafts, so a missing position means no history); then the keeper term (StashValue.keeperSurplus) is measured against WR24 while the same season'
- [low] Draft2026 and DraftNight compute BenchValue.overWireByPosition at warm and hand it to DraftNight.answer, which never uses it - lateRounds() ignores its parameter — `src/main/java/Draft2026.java:64`: DraftNight.answer receives benchBaseRate and uses it only in `lateRounds(benchBaseRate)` (DraftNight.java:104), whose body (lines 254-265) is fixed prose that never reads `baseRate`; the round-8+ branch re-derives the rates per cycle with `
- [low] DraftNight, WaitCheck, LiveCommittee and LiveDraft mains ignore -Pkeepers and DraftNight runs Model A on a 9-round schedule, unlike Draft2026 — `src/main/java/DraftNight.java:44`: The runbook's fallback ladder is `./gradlew run -Pmain=DraftNight -Pkeepers=Tuten,Purdy -q`; the property is never read here (LiveSetup.forTonight reads it via DraftPlanner.keepersFromProperty), so before the commissioner enters the keepers
- [low] `-Pkeepers` is silently ignored by DraftNight, LiveCommittee, WaitCheck and LiveDraft (they pass List.of()), yet RUNBOOK's ladder and DRAFT-READY pass it to DraftNight; and README's claim that every live-path tool warms through LiveSetup with a test to enforce it is false for these six hand-assembled tools — `src/main/java/DraftNight.java:44`: Harmless in 2026 because both keepers were declared on Sleeper (DRAFT-READY.md:393), but next August - before the commissioner enters keepers - the documented fallback plans Justin's seat with no keepers while Draft2026 plans it with Tuten 
- [low] DraftNight.benchGuidance still asserts 'THE STARTING NINE IS FULL' at any round >= 8 (TRAPS #32, corrected in Draft2026), and the on-screen routing text in DraftNight, LiveCommittee and LiveLateRounds still carries the retired 8-9/10+ split and sends him to DraftNight where the RUNBOOK says Draft2026 — `src/main/java/DraftNight.java:239`: Running the fallback DraftNight at round 8 on the RUNBOOK's own plan (TE deferred to 90) prints 'THE STARTING NINE IS FULL' while he still owes a tight end - the sentence Draft2026's comment says 'could cost him the tight end'. The other th
- [low] DraftNight prints a hardcoded 'StarterRisk says my nine are 0.90x as fragile' at every round-8+ pick - a literal measured on a hypothetical pre-draft nine, not the roster actually drafted — `src/main/java/DraftNight.java:247`: The 0.90 was computed for the projected nine (Henry, Nabers, ...) before the draft; the drafted roster is whatever the board gave. The screen states the pre-draft number as a fact about the live roster and would keep doing so in 2027 with a
- [low] DraftNight.benchGuidance carries a hard-coded StarterRisk figure and ignores the base rates computed at warm — `src/main/java/DraftNight.java:248`: '0.90x' is a number copied out of StarterRisk's output into prose printed on the live screen; it does not move with the roster he actually drafted (StarterRisk measured a nine with Henry and Nabers, which he did draft, but the line would pr
- [low] DraftNight prints typed numbers from other tools - '162, 175 and 186', 'QB stashes hit 41%', 'StarterRisk says 0.90x' - and Draft2026 quotes the simulated-only survival error TRAPS #42 superseded — `src/main/java/DraftNight.java:254`: benchGuidance (line 247-249) hard-codes 'StarterRisk says my nine are 0.90x as fragile as average' for whatever roster is on the table; lateRounds hard-codes the seat numbers RosterRules computes and LateRoundValue's 41%; Draft2026.java:69-
- [low] DraftNight prints '41%' and '0.90x' as live guidance; the 41% comes from a 2026-08-28 artifact the current LateRoundValue no longer produces, and LateRoundValue's committed output predates its own constants — `src/main/java/DraftNight.java:261`: data/late-round-value-2026-08-28.txt line 4 reads `QB 39 41%` (hit rate); the current tool's committed output data/late-round-value-2026-08-31.txt has no hit-rate table at all and instead says 'the bands run 135.8 / 129.5' and '-13' while L
- [low] The league's named starting slots are typed in three places (DraftSimulator.REQUIRED, StartingLineup.FIXED, BoardValue.lineup) that RosterRules derives from roster_positions and claims are never typed twice — `src/main/java/DraftSimulator.java:281`: RosterRules.java:31-33 - 'every positional ceiling fall out of those. No count in this file is typed twice' - reads the lineup from Sleeper. DraftSimulator.mustFill (the room's end-legal rule), StartingLineup.FIXED (lines 29-34, Model A's o
- [low] Stray and superseded draft tools still in the tree with mains: TightEndWait (no references anywhere), SeatPlan (only in OneWarmUpTest's list), LateWaitOrTake (RUNBOOK 'Superseded'), LateRoundTargets (DraftNight tells him NOT to run it), WaitOrTake (README lists it unmarked though its AvailabilityModel odds were replaced by the survival table), SleeperLiveDraft (README 'superseded' but it is what smokeTest exercises) — `src/main/java/LateRoundTargets.java:28`: README still recommends WaitOrTake without a superseded mark, so a reader gets ADP-bias survival odds that DRAFT-READY's 'The one real modelling change of the night' section replaced with the survival table (3.35 vs 1.19 men error on real d
- [low] LateSurvival and DraftPlanner still say the choice model is fitted on rounds 1-13 (TRAIN_ROUNDS) while their own fits train to 16 through trainRounds() — `src/main/java/LateSurvival.java:119`: LateSurvival sets scheduleRounds=16 at line 34 before BoostedSelectionModel.fitShipped, so SelectionModel.trainRounds() (lines 175-177) returns 16 and the fit covers rounds 1-16; the printed note under the table RUNBOOK.md:380-386 quotes sa
- [low] LiveBoard prints a simulated-pick count that mixes live pick count with pick number — `src/main/java/LiveBoard.java:259`: taken excludes keeper picks (LiveDraft.livePicks drops is_keeper) while pick is a slot number that counts them, so the guard is true on Justin's own turn whenever a keeper slot precedes it (from pick 32 on this season) and the line reads e.
- [low] LiveBoard.answer guards the pre-pick simulation with a live-pick COUNT against a slot NUMBER (the TRAPS #30 confusion) so on Justin's own pick after any keeper slot it still runs 200 zero-step trials and prints 'the N picks before mine simulated' with N = keeper slots passed — `src/main/java/LiveBoard.java:259`: On the clock at pick 162 in the 2026 draft, taken.size() is the live-pick count (about 139 with ~22 keeper slots before 162), pick - 1 is 161, so the branch runs and prints '139 men actually gone; the 22 picks before mine simulated 200 time
- [low] Dead code inside live-path classes: LiveBoard.valleysHistorical is defined and never called; LiveDraft.main/recommend is the superseded Component F single-shot (only freeze/livePicks/livePickOwners are used by Draft2026); HumanStrategy.main prints nothing while the class itself is load-bearing for KeeperChooser — `src/main/java/LiveBoard.java:810`: No numeric consequence. The component list marks HumanStrategy 'stray'; deleting it would break KeeperChooser.draftPlan and the SimulationDraft path KeeperChooser.simulate still runs. LiveDraft's javadoc (lines 17-34) still advertises `-Pma
- [low] LiveLateRounds prices every position with no RosterRules ceiling - a third quarterback or second defence gets an ADDS/SURVIVES row and a verdict where LiveBoard prints REFUSED; Tomorrow.java says so on screen instead of fixing it — `src/main/java/LiveLateRounds.java:90`: With Purdy kept and Nix drafted at 114, the fallback tool at 127 still prints a QB row (third quarterback) and a DEF row after a defence is held. ADDS for those is near zero so the verdict usually reads 'wait - he keeps', but the row is pre
- [low] LiveLateRounds' LOSS IF WAIT uses today's second-best man as the fallback, not the man expected at the next pick — `src/main/java/LiveLateRounds.java:120`: fallback() (186-212) returns the marginal of the best man at the position on the CURRENT board other than the candidate; by the next pick the room has taken some of that depth too, so the cliff behind him is understated and loss with it. It
- [low] PreFlight says 'read from Sleeper just now' but its status, settings, start time and draft order come from AAAConfiguration.getDraftJson, a day cache; only the picks count is live — `src/main/java/PreFlight.java:27`: AAAConfiguration.getDraftJson (lines 358-364) goes through InOutUtilities.getTodaysWebPage, so a PreFlight run in the evening reports whatever draftObject<id><today>.txt held when it was first fetched that day - the morning's status ('pre_d
- [low] RosterRules.justinsKeepers() is a typed Tuten r12 / Purdy r13 that the live rules roster uses regardless of the planner's actual keepers — `src/main/java/RosterRules.java:70`: LiveBoard.answer prints 'the rules allow here', 'still needed for a legal lineup' and every REFUSED reason from a roster seeded with this constant, while the board, curve and my roster come from planner.myKeeperIDs() (Sleeper's declared kee
- [low] StashValue's quoted table averages 'this season' over picks with a following season while the decay quoted beside it comes from all five seasons — `src/main/java/StashValue.java:110`: RUNBOOK 'Rounds 8-16' states the decay as '44.0 / 32.8 / 31.2 points over the wire' (BenchValue.overWireByBand, every season including 2025) and directly below it the table's 'this season' column (StashValue, 2021-2024 only, since 2025 has 
- [low] WaitCheck.report branches with a null 'spendOn' when only one position has a best-available man, which reaches DraftSimulator.applyPick and NPEs on Player.getPlayerFromSIDV2(null) — `src/main/java/WaitCheck.java:87`: On a late board where timing.bestAvailable returns a single position, spendOn maps it to null; branchWith calls applyPick(branch, slot, null) and DraftSimulator.java:821 dereferences Player.getPlayerFromSIDV2(null).position. DraftNight.answ
- [low] AppetiteCapTripwireTest's board is whichever fp-adp-halfppr CSV File.listFiles() happens to return first - an unordered listing — `src/test/java/AppetiteCapTripwireTest.java:34`: File.listFiles() order is unspecified (APFS and ext4 differ); with several seasons of fp-adp-halfppr CSVs in data/ the 'real board' this test drafts on differs by machine, so a failure on one laptop is not reproducible on another and the te
- [low] HindsightRegressionTest reads data/wire-rate-stress-2026-09-04.txt at test time but build.gradle declares only the five .md files as test inputs - the TRAPS #45 'test that cannot see its own input' pattern recurring — `src/test/java/HindsightRegressionTest.java:426`: Re-running WireRateStress and committing a new rate into that file (the objective LiveLateRounds runs on, per the test's line 162) leaves `./gradlew check` UP-TO-DATE for the test task if no source changed, so the regression against the com
- [low] smokeTest silently skips the seven live-draft checks when -PdraftId is absent - a documented skip that could be a failure — `src/test/java/MockDraftSmokeTest.java:36`: build.gradle forwards `draftId` only `if(project.hasProperty('draftId'))`, and RUNBOOK line 183 has to warn 'Without one, seven MockDraftSmokeTest checks silently SKIP - the entire live-draft path, which is where every draft-night defect th
- [low] RosterRulesTest.theSixteenComesFromTheLeagueNotFromThisConstant returns green when the league cannot be read - the one condition it exists to catch — `src/test/java/RosterRulesTest.java:413`: The javadoc says this test 'is what stops [FALLBACK_SLOTS] drifting in silence'; on the branch where the fallback is actually in use it prints a line and passes. With fixtureDir set the fixture always serves the league, so the branch never 
- [low] Seven draft-path source-scanning tests use bare String.contains and would be satisfied by a comment mentioning the call — `src/test/java/SurvivalDependentToolsTest.java:73`: A new harness whose javadoc says 'unlike FragilityBinding this does not call warmSurvival' passes the very check written because FragilityBinding's numbers reached DRAFT-READY without a survival table. Today every match is in code (verified
- [low] SurvivalRankTest's 'truth' is drawn from the same simulator the survival table is fitted on - it proves survival predicts the simulator, not the room — `src/test/java/SurvivalRankTest.java:57`: Different seeds, same DraftSimulator, same fitted choice model: the held-out draws are the population the table was built from, so `survivalError < cutoffError` is close to guaranteed by construction and says nothing about the 2.69 -> 0.08 


## 3. Draft research & diagnostics
confirmed 11 · refuted 1 · unverified 50

### Confirmed

**[high/small] replacementRanks prices the in-season wire off 2021-2025 drafts, not the finished 2026 draft, and has no test** — `src/main/java/InsuranceTest.java:47`
```
public static Map<Position, Integer> replacementRanks(AAAConfiguration configuration){
    Map<Position, Integer> drafted = new EnumMap<>(Position.class);
    int seasonsCounted = 0;
    for(com.google.gson.JsonArray picks : configuration.getPreviousDraftPicks()){
        seasonsCounted++;
        ...
    for(Map.Entry<Position, Integer> entry : drafted.entrySet()){
        ranks.put(entry.getKey(),
                entry.getValue() / Math.max(seasonsCounted, 1) + 1);
```
- scenario: AAAConfiguration.getPreviousDraftPicks (line 193-217) starts at getPreviousLeagueID(), so it walks 2021-2025 and never the 2026 draft. On the live cache (draftData1390416723210952704 2026-09-08, 192 picks) the 2026 room took QB 18 / RB 65 / WR 76 / TE 21, so the real first free men are QB19 / RB66 / WR77 / TE22; the five-season truncated mean gives QB21 / RB61 / WR81 / TE19. WeeklyStarterValue.forCurrentBoard (line 358-372) turns those ranks into the wire projection that LeagueConsole (240/414/642), TuesdaySwap (222), TradeMarket (617) and TradeStability (132/165) price every drop, add and trade against: RB depth is credited five ranks too rich and TE three too rich, WR four and QB two too poor, in every 'roster with him and without him' number on the console page. grep shows no test in src/test/java mentions replacementRanks; the only pinned arithmetic is ReplacementRanks.ranksFrom (ObjectiveAuditTest 24-69), a diagnostic twin, and the two truncate (60.8 -> 61, 18.6 -> 19) without a test saying so.
- fix: Once the current season's draft is complete (configuration.getDraftPicks() holds teams x rounds picks), count that draft; before it is complete fall back to the historical mean as now, and print which basis was used beside the ranks. Add a unit test that builds two fixture drafts (one with a full 2026 board, one pre-draft with 24 keeper picks) and pins both the basis choice and the truncation-plus-one arithmetic, so the number the console prices against is asserted rather than remembered.
- siblings: src/main/java/WeeklyStarterValue.java:358, src/main/java/BustBoomValue.java:290, src/main/java/StartSit.java:325, src/main/java/BenchValue.java:170

**[medium/trivial] DefenceRound slides the defence through a duplicated copy of the sequence PlanBacktest now calls a strawman, so RUNBOOK's defence-round table is measured on a plan with two dead picks** — `src/main/java/DefenceRound.java:26`
```
/** The committed plan with its defence removed - thirteen fixed picks. */
static final String[] WITHOUT_DEFENCE =
        "RB RB RB WR WR WR WR TE WR QB TE QB RB".split("\\s+");
```
- scenario: This is PlanBacktest's "RUNBOOK committed" string minus DEF. PlanBacktest.java:95-108 has since annotated that string: 'A STRAWMAN ... it drafts two quarterbacks - three with Purdy kept, on a one-quarterback lineup - and a second tight end', and added "RUNBOOK as written" (`RB RB RB WR WR WR WR TE WR QB WR RB RB DEF`, line 113-114) as what the document actually prescribes. DefenceRound was not updated, so RUNBOOK:433-442 ('one plan, thirteen picks held fixed ... spread from round 1 to round 16 is 215 points') is the cost of an early defence on a roster whose r11 tight end and r14 quarterback contribute nothing, and every slide of the defence displaces those dead picks rather than live ones. The table (data/defence-round-2026-08-29.txt) is also pre-#84 feed points.
- fix: Derive WITHOUT_DEFENCE from PlanBacktest.STRATEGIES.get("RUNBOOK as written") with the DEF token removed (one home), rerun, and replace RUNBOOK:437 with the new row.
- siblings: src/main/java/PlanBacktest.java:109, src/main/java/PlanBacktest.java:113

**[medium/small] DefenceVersusDepth and LateRoundValue compare pre-flip feed-point skill literals (44.0/32.8/31.2) against post-flip league-point defence bands (140.3/131.9): 'both halves on one scale' are on two** — `src/main/java/DefenceVersusDepth.java:27`
```
/** From BenchValue, measured on this league's own rounds 8-16 picks. */
static final double SKILL_ROUNDS_8_9 = 44.0;
static final double SKILL_ROUNDS_13_16 = 31.2;
```
- scenario: 44.0/32.8/31.2 were read off data/bench-value-2026-08-29.txt, graded in the feed's pts_half_ppr. On 2026-09-04 LeagueActuals.enabled() flipped ON (6-pt pass TDs, -1 per fumble, pts_allow_14_20 for defences) and BenchValue.gather already grades through it (BenchValue.java:190 `actuals = LeagueActuals.seasonPoints(season);`), so re-running BenchValue today no longer prints 44.0 - the 8-9 band holds 10 quarterbacks of 111 whose seasons gain ~50 each under 6-pt TDs. LateRoundValue's own comment (lines 45-53) records that the DEF bands moved three times that day to 140.3/131.9 'once outcomes were graded in the league's own points', while its BANDS at lines 39-43 (`new Band("8-9", 8, 9, 44.0, 0.29)` ...) stayed on the old scale. DefenceVersusDepth therefore prints '+12.8 (skill, feed points) vs +8.4 (defence, league points), depth wins by 4.4' in data/defence-versus-depth-2026-09-04.txt - a 4.4-point verdict built from two units, in a tool whose whole heading is THE TRADE, BOTH HALVES ON ONE SCALE. BenchValue.overWireByBand (line 78) exists precisely so nothing retypes these, and neither tool calls it. BandRegressionTest reads the DEF side back from the file; the skill side is pinned only as fixture literals in BenchCalibrationTest:347-349 and :389, which cannot fail when the tool's output moves.
- fix: Have DefenceVersusDepth and LateRoundValue call BenchValue.overWireByBand(configuration) (means only) instead of holding constants; commit a post-flip bench-value output and extend BandRegressionTest to read the three skill bands from it the way it reads the DEF bands; update RUNBOOK:301-317 and DRAFT-READY.md:244,261 from the rerun.
- siblings: src/main/java/LateRoundValue.java:39, src/main/java/BenchValue.java:78, src/test/java/BenchCalibrationTest.java:347

**[medium/trivial] LateRoundValue prices a streamed defence over 17 weeks at a rate that is season/18, then subtracts it from an 18-week drafted-defence total** — `src/main/java/LateRoundValue.java:62`
```
static final int WEEKS = 17;
...
double defenceWire = PlanBacktest.streamedDefencePerWeek();
double streamed = defenceWire * WEEKS;
...
System.out.printf("   so drafting the best defence beats streaming by %+.0f.%n%n",
        DEF_BEST_BAND - streamed);
```
- scenario: streamedDefencePerWeek comes from WeeklyStarterValue.wireRates, whose rate is `season.meanWhenPlaying() * season.games() / 18.0` (WeeklyStarterValue.java:412) - an 18-week denominator, which the HONEST_WIRE comment at lines 108-117 says 'has to match' and warns was once double-converted. DEF_BEST_BAND (140.3) is DefenceVersusDepth's sum over `board.weekly()` (lines 64-66), and PlanBacktest.board builds weekly over `week <= WeeklyActuals.WEEKS` = 18 (PlanBacktest.java:347, WeeklyActuals.java:29). So 'streaming pays 8.7 a week, 149 a season' (data/late-round-value-2026-08-31.txt) is 17 x (season/18): the streamed side is short one week, ~8.3 points at today's 8.8/week, and every 'STARTING DEF' cell and the pooled-mean sentence are that much too kind to drafting a defence. WireRateStress.java:99-100 prints the same `shipped * 17` beside the 18-denominator rate. The verdict ('take the skill man') survives, the number does not.
- fix: Use WeeklyActuals.WEEKS (or divide the DEF band by 18 and compare per week) so both sides of the subtraction carry the same week count; fix WireRateStress's printed season figure the same way.
- siblings: src/main/java/WireRateStress.java:99, src/main/java/WeeklyStarterValue.java:412, src/main/java/DefenceVersusDepth.java:64

**[medium/small] Two board loaders join FantasyPros names with TightEndTiming.normalise, which drops nicknamed men the repo's own Player alias table resolves (Ken Walker III, Gabe Davis, Hollywood Brown); PlanBacktest keeps their rank but cannot draft them** — `src/main/java/PlanBacktest.java:328`
```
String id = idByName.get(TightEndTiming.normalise(cells[nameCol]));
rows.add(new Row(id, position, Double.parseDouble(cells[adpCol])));
...
if(row.id() != null && !positionOf.containsKey(row.id())){
    ids.add(row.id());
```
- scenario: normalise() only strips punctuation and suffixes (TightEndTiming.java:690-695). Player.java:40-45 carries FANTASY_PROS_NAME_ALIASES (`"hollywoodbrown" -> "marquisebrown"`) and getPlayerFromNameAndPos falls back to getPlayerFromFantasyPros because 'the name/team/position matcher handles renames and nicknames' (Player.java:195-196); neither loader uses it. Replaying the join on the committed files: 2022 loses Ken Walker III (RB, ADP 103.5 - Sleeper has 'Kenneth Walker') and Gabriel Davis (WR, ADP 66.5 - 'Gabe Davis'); 2024 loses Hollywood Brown (ADP 84.3) despite the alias; 2021 loses Robby Anderson, Gabriel Davis, Kenneth Gainwell. In PlanBacktest.board the TRAPS #80 fix keeps an unjoined man's RANK (line 339) but he is never added to `ids`, so on the 2022 board no strategy - and none of the other eleven managers in draft() - can take Walker at 103 or Davis at 66: the men are simply absent from the board RUNBOOK:349's '1921 against 1998' and the defence-round table are scored on. In TightEndTiming.join (line 678-681, `if(id == null){ continue; }`) the man vanishes entirely and bestAt(season, RB, 103) hands the option the next back.
- fix: Route both loaders' name lookup through Player.getPlayerFromNameAndPos (which already applies the alias table and the fuzzy matcher), or at least apply FANTASY_PROS_NAME_ALIASES before normalise; print the per-season unjoined list the way OutcomeDistributions.joinReport does so a new miss is visible.
- siblings: src/main/java/TightEndTiming.java:678, src/main/java/PlanBacktest.java:290, src/main/java/Player.java:45

**[medium/medium] DefenceRound's 'flattens after round 14' plateau is an artifact: in PlanBacktest.draft the other eleven managers never draft a defence, so Justin lands preseason DEF1 whichever round he takes it** — `src/main/java/PlanBacktest.java:542`
```
else {
    // the other eleven never draft a defence early either
    String other = bestAvailableSkill(board, gone);          // :542-547
...
public static String bestAvailableSkill(Board board, Set<String> gone){
    for(String id : board.ids()){
        if(!gone.contains(id) && board.positionOf().get(id) != Position.DEF){   // :562-565
            return id;
```
- scenario: The comment says 'early' but the code says never: no opponent takes a defence in any of 200 picks, so the plan's defence at pick 162, 175 or 186 is always the board's DEF1 and the three rows differ only by which bench man was displaced. That is exactly why data/defence-round-2026-08-29.txt reads 1981/1984/1984 for rounds 14/15/16 (and 2020/2020/2020 in 2021), which RUNBOOK.md and MODEL.md:3223 quote as a finding ('rounds 14, 15 and 16 are worth the same'). In the real league 10-13 defences go every year, all in rounds 10-16 (previousSeasonDraftPicks: DEF 11/10/12/12/13), so at pick 186 the top dozen are gone. DefenceVersusDepth says preseason DEF rank predicts little (140.3 vs 131.9), so the level moves by under ten points - but the plateau itself is by construction, not measurement, and the same assumption sits under every PlanBacktest strategy row that ends 'DEF'.
- fix: Let the other eleven take defences at the rounds the league's own drafts show (e.g. draw each opponent's defence round from the measured distribution, or at least take DEF1..DEF11 across picks 109-192), rerun DefenceRound, and reword the RUNBOOK/MODEL.md plateau sentence to what the corrected run shows. Fix the comment either way.
- siblings: src/main/java/DefenceRound.java:52, RUNBOOK.md:427

**[medium/medium] PlanBacktest.seasonPoints scores an unfilled skill slot at zero while the objective it backtests, and TightEndTiming's weekly scorer, floor every slot at the wire; only the defence is streamed** — `src/main/java/PlanBacktest.java:674`
```
static double fill(List<String> available, int slots, List<String> flex,
                   Map<String, Double> points){
    int size = available == null ? 0 : available.size();
    double scored = 0;
    for(int slot = 0; slot < slots && slot < size; slot++){
        scored += points.getOrDefault(available.get(slot), 0.0);
    }
```
- scenario: WeeklyStarterValue.fill (lines 291-307) starts the wire whenever a slot is empty or the rostered man is expected below it (FREE_WIRE, documented at lines 79-98: 'the bench is FUNGIBLE ... a rostered man is worth exactly what he EXCEEDS the wire by'), and TightEndTiming.fill (553-566) does the same. PlanBacktest.seasonPoints gives a bye-week or injury-emptied QB/RB/WR/TE slot 0.0 (line 654 `fill(up.get(Position.QB), 1, null, points)`) and streams only the DEF slot (663-665). So the RUNBOOK:349 verdict that every model built on the objective 'has lost a five-season backtest ... 1921 against 1998' is scored by a grader that charges the objective's plans full price for the wire reliance the objective assumes is free, and a thin plan (one TE, one QB) loses whole weeks the objective would have filled at 8-12 points. The two artifacts claim to compute the same V(R) = sum of the best legal lineup and disagree on what an empty slot is worth.
- fix: Either give seasonPoints a wire floor per position from WeeklyStarterValue.wireRates (charged a roster spot the way the defence is), or state in PlanBacktest's header and RUNBOOK:349 that the backtest scores empty skill slots at zero and is therefore a harsher grader than the objective, and quantify the gap once with ScorerHonestyAudit's machinery.
- siblings: src/main/java/WeeklyStarterValue.java:291, src/main/java/TightEndTiming.java:553, src/main/java/PlanBacktest.java:654

**[medium/small] TightEndTiming and StarterContribution price the wire as a season total divided by 17 but rostered men at their per-game rate when playing - a unit mismatch that under-credits streaming** — `src/main/java/TightEndTiming.java:428`
```
wire.put(position, wireLevel(season, position) / 17.0);          // :428 (StarterContribution :407, :486)
...
static double perGame(Seen player){
    return player.games() > 0 ? player.points() / player.games() : 0;   // :573-575
}
// wireLevel :608-609 - a MEAN OF SEASON TOTALS of men who play 13-15 games
return ranked.subList(drafted, Math.min(ranked.size(), drafted + 12)).stream()
        .mapToDouble(Seen::points).average().orElse(0);
```
- scenario: The tool's own curve table shows the TE19-21 band averaging 15.0 games and the WR bands 13-15, so a wire man's season total / 17 is 12-20% below his per-game rate, while every rostered man in fill() is scored at points/games. Option C ('stream TE') is paid wireTe/17 every week - about 4.5 a week from a 77-point band instead of roughly 5.4 - which is ~15 points a season, most of the B-C gap the RUNBOOK quotes as 'waiting beats streaming' (1451.9 vs 1402.8, +49). The same understated wire lets weak rostered men clear the fill() bar (perGame >= wireRate) that a correctly priced wire would bench. The 'TE10 - wire' streaming table (+53.2 +/- 30.6) is unaffected since it compares totals to totals.
- fix: Price the wire per game the way the roster is priced: mean of the band's points/games (or points over the league's 17-week window with missed weeks scattered), in wireLevel or at the three call sites. Re-run tight-end-timing and starter-contribution and update the RUNBOOK numbers.
- siblings: src/main/java/StarterContribution.java:407, src/main/java/StarterContribution.java:486

**[medium/small] TightEndTiming.wireLevel's RB wire literal (80) is deeper than the FP boards go, so the RB wire is 0.0 in 2022 and a one-man 'band' in 2023; the measured league wire is RB61** — `src/main/java/TightEndTiming.java:601`
```
int drafted = position == Position.TE ? 18 : 80;
...
if(ranked.size() <= drafted){
    return 0;
}
return ranked.subList(drafted, Math.min(ranked.size(), drafted + 12)).stream()
        .mapToDouble(Seen::points).average().orElse(0);
```
- scenario: InsuranceTest.replacementRanks (the function the whole repo shares 'so the two never disagree about how rich the wire is') measures this league at QB21 RB61 WR81 TE19 from its five cached drafts (RB 59+63+58+63+61 = 304, /5 = 60, +1), and BenchValue prints exactly that wire line. TightEndTiming's literal happens to match for TE (18) and WR (80) but uses 80 for RB too. Replaying join() on the committed fp-adp-halfppr files: 2021 joins 105 RBs, 2022 joins 69, 2023 81, 2024 82, 2025 85. So `ranked.size() <= 80` returns a ZERO RB wire for 2022, and the '12-man mean' the javadoc promises is one man (RB81, 95.3 pts) in 2023, two men in 2024 and five in 2025. StarterContribution inherits it verbatim at lines 407 and 486 (`wire.put(position, TightEndTiming.wireLevel(season, position) / 17.0)`), and its RB column is the 'best alt' in the 1.0x rows of data/starter-contribution-2026-08-29.txt (RB 89.5 vs WR 85.0), so the RUNBOOK:289-292 claims ('the tight end loses in all 30 worlds', '-36.3 at 79 -> -16.9 at 127') are measured against an RB alternative whose marginal is taken over a zero or one-man wire in two of five seasons. At the measured rank the 2021 RB wire is 44.9 pts (2.64/g) against 29.6 (1.74/g) at rank 81. The same zero feeds injuryAwareSwap (line 428) for the RUNBOOK:273 A/B/C table; verdict() guards `wireTe == 0` (line 260-261) but nothing guards the RB or WR wire, so the zero is silently included (pattern 3).
- fix: Replace the literal with InsuranceTest.replacementRanks(configuration) minus one per position (RB 60, WR 80, TE 18 today), and make wireLevel refuse (throw or skip the season, as verdict() does for TE) rather than return 0 when the board is shallower than the wire rank; re-run TightEndTiming and StarterContribution and update RUNBOOK:273-292 from the new outputs.
- siblings: src/main/java/StarterContribution.java:407, src/main/java/StarterContribution.java:486, src/main/java/TightEndTiming.java:428, src/main/java/InsuranceTest.java:44

**[medium/trivial] TightEndTiming.join still grades outcomes in the feed's pts_half_ppr after the 2026-09-04 flip moved every sibling loader to LeagueActuals; StarterContribution inherits it** — `src/main/java/TightEndTiming.java:635`
```
static List<Seen> join(File adpFile, String season) throws Exception {
    Map<String, Double> points = HistoricalActuals.pointsBySleeperID(season);
    Map<String, Integer> games = HistoricalActuals.gamesPlayedBySleeperID(season);
```
- scenario: LeagueActuals.java:31-40 says 'Anything that grades outcomes ... should call the dispatchers rather than the raw feed, so one switch moves all of it at once', and PlanBacktest.board (line 284), OutcomeDistributions.byProjection, BenchValue.gather (line 190) and WireRateStress were all moved. TightEndTiming.join was not, and StarterContribution.main gets its whole history through TightEndTiming.load() (StarterContribution.java:182). The league charges -1 for every fumble (LeagueActuals.java:20-21, feed charges nothing from 2023), so the RUNBOOK:273 A/B/C table (1355.4 / 1451.9 / 1402.8), the +53.2 +/- 30.6 streaming margin at RUNBOOK:282 and the 30-world sweep at :289 are on the retired scale while the defence-round and plan-backtest tables printed beside them are on the new one. The per-player effect for RB/WR/TE is a few points a season (fumbles only), so the ordering is unlikely to move - but the two tools now claim to score the same league on different rules and nothing says so.
- fix: Change join() to LeagueActuals.seasonPoints(season) (and keep gamesPlayed from the raw feed), rerun both tools, and refresh the three RUNBOOK numbers; add TightEndTiming to ProseDriftTest.everyLeagueActualsDispatcherConsultsTheFlag's sweep or to the objectiveBearing list so the next flip reaches it.
- siblings: src/main/java/StarterContribution.java:182, src/main/java/PlanBacktest.java:284, src/main/java/LeagueActuals.java:70

**[medium/medium] The outcome pool and every weekly scorer include NFL week 18, a week this league has never scored (last_scored_leg = 17 in all five seasons)** — `src/main/java/WeeklyStarterValue.java:223`
```
boolean up = u < games / 18.0;          // WeeklyStarterValue.java:223
return WEEKS * total / scenarios;      // :245, WEEKS = 17
// WeeklyActuals.java:29
public static final int WEEKS = 18;   // "The regular season has run 18 weeks since 2021"
// OutcomeDistributions.byProjection :131-136
for(int week = 0; week < WeeklyActuals.WEEKS; week++){ if(weeklyPlayed.get(week).contains(row.id())){ games++; ...
```
- scenario: Every previousLeagueSettings file 2021-2025 has playoff_week_start 15 and last_scored_leg 17, so the league never fields a lineup in NFL week 18, when playoff-bound teams rest starters. The cached actuals show it: 2025 top-12 QBs played 8/12 in week 18 vs 11.2/12 on weeks 1-17 and averaged 15.6 vs 19.9; top-24 RBs 10.1 vs 14.5. In the pool a rested QB1 reads games=16 so the console's objective marks him up 16/18 = 0.889 while a deep man who plays week 18 reads 17/18 = 0.944; in the league's own window both are 16/17. The tier-0 cells therefore carry a ~1-2% availability haircut plus week-18's depressed scoring, and tier-2+ cells carry none. This flows through WeeklyStarterValue.forCurrentBoard into LeagueConsole (wire tab, trades), TuesdaySwap and TradeMarket - a few points shaved off top men relative to deep men, on the same order as the 'inside the noise' bar. PlanBacktest (:631) and DefenceVersusDepth also sum an 18th week nobody plays.
- fix: Score and count over the league's scoring window: read last_scored_leg (17) from the league settings (or make WeeklyActuals.WEEKS the league's, not the NFL's), loop weeks 1..17 in OutcomeDistributions, PlanBacktest.seasonPoints/board and DefenceVersusDepth, and use the same 17 in the availability and wireRates denominators (:223, :412). Re-run outcome-distributions, plan-backtest and defence-versus-depth outputs and note the moves in TRAPS.
- siblings: src/main/java/WeeklyActuals.java:29, src/main/java/OutcomeDistributions.java:107, src/main/java/OutcomeDistributions.java:131, src/main/java/PlanBacktest.java:631, src/main/java/WeeklyStarterValue.java:412, src/main/java/DefenceVersusDepth.java:64

### Refuted
- **FogFit's bust mass is counted twice by every sampler that uses it: the normal component is fitted on ratios that already include the busts** — `src/main/java/InsuranceTest.java:122`
  - refuted: Part (a) checks out: FogFit.fit (src/main/java/FogFit.java:37-44) does compute mean/var over all `values` including the sub-0.5 bust cases, and then computes `bust` as the fraction of those same values under 0.5 — so the normal component is indeed fitted on a population that includes the bust mass. InsuranceTest.java:120-123 (verified, matches quote) does then draw a mixture: with probability c[2] (bust rate) draws from a separate bust distribution, else from N(c[0], c[1]) — so mathematically the bust mass is represented in both the mixture weight and inflated into the normal component, which is a genuine double-counting of bust mass in the mixture model.

However this fails part (b). InsuranceTest, LiveInsurance, DecisionSensitivity and FogFit are exclusively draft-time preparation tools (MODEL.md: 'Implementation: FogFit + DecisionSensitivity + InsuranceTest', used for pre-draft insurance/sensitivity analysis, LiveInsurance for in-draft rounds 8-9 decisions). RUNBOOK.md line 1 states plainly: 'The draft is done. This is what the rest of 2026 looks like' — the weekly loop (TuesdaySwap, LeagueConsole) is the only thing currently feeding decisions Justin acts on, and neither of those tools touches FogFit/InsuranceTest/LiveInsurance/DecisionSensitivity (confirmed via grep — no references in TuesdaySwap/LeagueConsole call graphs or RUNBOOK's weekly section). So no number in a report or page Justin currently reads or acts on is affected by this bug this season; it would only resurface if/when a future draft-prep pass reruns these tools, at which point it should be caught before use.

### Unverified (50; found by a lens, not yet refuted — verify before acting)
- [medium] The weekly `check` step tells him to read a test count and '0 skipped' from check-wire.log, which the test task never writes - and the count quoted is stale — `RUNBOOK.md:153`: RUNBOOK.md:19-20 says two artifacts built from different snapshots are flagged by `check` 'skipping the ladder comparison rather than pretending'. Gradle's Test task logs nothing on success without testLogging, so a skipped LeagueConsoleTes
- [medium] build.gradle declares no data/ or fixture inputs, so tests that read committed artifacts stay UP-TO-DATE when the artifact changes — `build.gradle:58`: The comment names the fault and then declares only five markdown files. HindsightRegressionTest (data/wire-rate-stress-2026-09-04.txt, line 407/426), WireStressRegressionTest (line 23), BandRegressionTest (data/defence-versus-depth-2026-09-
- [medium] DefenceVersusDepth hard-codes BenchValue's feed-unit 44.0/31.2 while its own defence half is now league-graded - 'both halves on one scale' is two scales — `src/main/java/DefenceVersusDepth.java:27`: BenchValue grades through LeagueActuals.seasonPoints (BenchValue.java:190), so with the 2026-09-04 default flip it prints 45.4 / 33.8 / 32.2 (DRAFT-READY.md:264-265 gives exactly those under -PleagueScoredActuals=true), while DefenceVersusD
- [medium] InsuranceTest.main and LiveInsurance index the wire one man deeper than WeeklyStarterValue for the same replacement rank — `src/main/java/InsuranceTest.java:135`: replacementRanks returns a 1-based rank (QB21 = 'the 21st best is the first free man'). WeeklyStarterValue line 364 and BustBoomValue line 296 read `int index = Math.min(Math.max(0, rank - 1), ids.size() - 1)`, the 21st man. InsuranceTest l
- [medium] InsuranceTest and LiveInsurance index the wire one man deeper than the live objective for the same replacement rank — `src/main/java/InsuranceTest.java:135`: On the five cached drafts (data/fixtures/2026-pre-draft, censused: 192 picks x 16 rounds x 12 teams each season) replacementRanks() = {QB 21, RB 61, WR 81, TE 19}; WeeklyStarterValue.java:400-405 documents 21 as 'QB21 - the first quarterbac
- [medium] OutcomeDistributions.CACHE is keyed only on poolKey, so the streamed-defence memo keyed on the scoring flag is fed the other arm's pool — `src/main/java/OutcomeDistributions.java:57`: ScoringImpactReport.java:39-47 sets the flag false, runs every strategy (the first bye week calls streamedDefencePerWeek, which builds and caches the feed-graded pool under key 'projection'), then sets the flag true and runs again: computeI
- [medium] TightEndTiming.wireLevel hardcodes RB wire depth 80 while the league's measured wire is RB61; TE and WR literals happen to match the measurement — `src/main/java/TightEndTiming.java:601`: The league drafts 58-63 running backs a season (previousSeasonDraftPicks), so the RB wire is RB61-72; this literal prices it as the RB81-92 band, roughly half the points. StarterContribution.marginalOf/marginalsSampled put that wire under t
- [medium] TightEndTiming.wireLevel hardcodes the wire at 18/80 for every position, 20 ranks too deep for RB, and its bust bar uses a third set of literals — `src/main/java/TightEndTiming.java:601`: The league drafts about 61 running backs (58-63 in 2021-2025, 65 in 2026), so the RB wire is RB62-73; wireLevel averages RB81-92 instead. TightEndTiming.injuryAwareSwap (line 428) and StarterContribution (lines 407, 486) both take `wireLeve
- [medium] TightEndTiming (and StarterContribution through load()) still grades in the feed's points, bypassing LeagueActuals — `src/main/java/TightEndTiming.java:635`: LeagueActuals.enabled() has defaulted to true since 2026-09-04 (LeagueActuals.java:61-63) and DIAGNOSTIC.md:405-407 says every table in the repo now grades in league points (TRAPS #84). TightEndTiming - tagged live, and the source of RUNBOO
- [medium] 'honestWire must default off' asserts a JVM property the build never sets, not the code's default — `src/test/java/HindsightRegressionTest.java:369`: build.gradle's test task forwards no honestWire property, so Boolean.getBoolean("honestWire") is false in every test JVM regardless of what WeeklyStarterValue does with it. Flip WeeklyStarterValue.wireRates to use the honest rate unless -Ps
- [medium] RosterRulesTest's live cross-check passes green when the league cannot be read - a guard that skips on the very failure it exists to catch — `src/test/java/RosterRulesTest.java:411`: This is the only test that checks FALLBACK_SLOTS / FALLBACK_TEAMS / FALLBACK_SLOT against Sleeper's roster_positions, and every other test in the file builds its league from those constants. If the fixture league file is renamed, the fixtur
- [medium] ShapeSensitivityTest pins the 2026-08-29 season vector and a 125-point bar that PlanBacktest and PowerBacktest no longer produce, and only tests arithmetic on its own literal — `src/test/java/ShapeSensitivityTest.java:143`: The name says these are the seasons PlanBacktest prints; nothing in the test calls PlanBacktest. data/plan-backtest-2026-08-29.txt prints 2035/1654/2191/1960/2148 (mean 1998); data/plan-backtest-2026-09-04.txt, after LeagueActuals flipped t
- [medium] TwoObjectivesTest asserts Draft2026.java contains the word 'only' - a source-scan that cannot fail — `src/test/java/TwoObjectivesTest.java:50`: Any Java file of that size contains 'only'; deleting the whole explanation from Draft2026 leaves the test green. The sibling at line 44 (`source.contains("fill(pool, Position.DEF, 1, curve, flex, false)")`) is the pattern that found a bug t
- [low] RUNBOOK's 'this league has taken 0 of 58' defences before round 10 is contradicted by DIAGNOSTIC's '2 of 70' after the 2026 draft — `RUNBOOK.md:225`: DefenceReality computes the count from the league chain, and with sleeperProjectionsFinal2026.txt present the current season counts as history (DIAGNOSTIC.md:443-447), so the tool now prints 2 of 70 while the runbook still states 0 of 58 as
- [low] RUNBOOK says every objective-built model lost the backtest (1921 vs 1998) and reproduced 'RB RB RB WR WR WR'; DRAFT-READY, DIAGNOSTIC and PlanBacktest.STRATEGIES say otherwise — `RUNBOOK.md:347`: The runbook's standing instruction 'prefer the plan unless it shows a large LOSS IF WAIT' rests on a 1998-vs-1921 gap that PlanBacktest.java:95-110 now labels a strawman row and DRAFT-READY.md:127-131 calls unmeasurable; neither committed s
- [low] RUNBOOK still cites the committed plan at 1998 while PlanBacktest has printed 2033 since the league-scoring flip — `RUNBOOK.md:349`: Anyone re-running PlanBacktest to check the RUNBOOK sentence sees 2033 and a 1921 that was measured in the old unit; the comparison the sentence makes (model 1921 vs plan 1998) mixes a feed-scored number with what the tool now prints and ca
- [low] RUNBOOK's DefenceRound table ('monotone, later is better, 215 points') and DRAFT-READY's DefenceTiming table ('peak at pick 79, spread 107.5, not measurable') answer the same question differently and neither document mentions the other — `RUNBOOK.md:432`: DefenceRound scores real outcomes with the strawman plan held fixed; DefenceTiming scores rollouts on the 2026 board. Both are quoted as the answer to 'where does the defence go', one says round 16 beats round 7 by 60 points, the other says
- [low] RUNBOOK quotes defence and plan numbers from 2026-08-29 tools that the 2026-09-04 artifacts no longer reproduce — `RUNBOOK.md:445`: The -52.7 / 59.5 / 44.4 come from data/defence-timing-2026-08-29.txt, measured before defences were ranked in the source's order (TRAPS #80) and before league scoring (TRAPS #84). The same question re-measured on 2026-09-04 (data/defence-ve
- [low] RUNBOOK's 'do not reach' defence numbers (3 in 20, 6.9, 59.5, -52.7 +/- 44.4) come from a DefenceTiming that was rewritten to measure something else — `RUNBOOK.md:445`: The runbook attributes the paragraph to a measurement 'over five seasons' without naming the tool; the tool that produced it (commit 3e3a95f) was replaced on 2026-09-01 by a rollout-based sweep, so nobody can regenerate or check the quoted 
- [low] RUNBOOK's defence and backtest prose quotes numbers no current tool produces (6.9 / 59.5 / -52.7 +/- 44.4 / 3 of 20; 1998 vs 1921) while the committed 09-04 outputs say +8.4, 3 of 5, and 2033 — `RUNBOOK.md:446`: Those figures are MODEL.md:2946-2965 (2026-08-29, feed points). The shipped DefenceVersusDepth prints '+8.4 points a season, top band to bottom', 'DEF1-3 beat DEF10-12 in 3 of 5 seasons' and 'depth wins by 4.4' (data/defence-versus-depth-20
- [low] The RUNBOOK's defence-instinct numbers (3 of 20, +6.9, 59.5, -52.7 +/- 44.4) come from a DefenceTiming that commit 8486580 deleted; no committed tool can reproduce them — `RUNBOOK.md:446`: The numbers survive only in data/defence-timing-2026-08-29.txt (feed-scored, pre-TRAPS #80/#84 join and grading) and in MODEL.md:2955-2972; DefenceReality prints neither the 3-of-20 table nor the pick-value trade. Anyone re-running 'Defence
- [low] DefenceRound re-types the strawman strategy string instead of reading PlanBacktest.STRATEGIES — `src/main/java/DefenceRound.java:26`: The RUNBOOK's defence-round evidence (:436-437) is the strawman plan with the defence slid through it, not the plan 'as written'; MODEL.md:4416 uses ShapeSensitivity reproducing DefenceRound 'exactly' as a cross-check, which only holds whil
- [low] Thirty-one tools in this component are referenced by nothing - no source, no test, no document — `src/main/java/DefenceSurvey.java:15`: These compile, are forwarded knobs by build.gradle (e.g. 'me' and 'sources' were added on 2026-08-31 specifically so RiskDial and SourceSensitivity could be steered), and several encode literals that will silently go stale (AutodraftImpact.
- [low] DefenceVersusDepth and LateRoundValue hardcode BenchValue's 2026-08-29 feed-scored bands (44.0/32.8/31.2) that BenchValue no longer produces under league scoring, with no read-back test — `src/main/java/DefenceVersusDepth.java:27`: BenchValue's rounds 8-9 band is 10 quarterbacks out of 111 picks, and league scoring pays 6 per passing TD, so a rerun moves the 44.0 (BenchValue's own footer says the raw-points column 'flatters QB'; the 6-pt regrade only widens it). The c
- [low] EraKeepers reads today's FFC board with no post-draft freeze, so PlanBacktest.keeperIDs/EraSlate transplant a different Tuten rank on different days, and DefenceTiming hardcodes a third answer — `src/main/java/EraKeepers.java:48`: The cached ffCalculatorSDSerious2026 files give Tuten RB24 on 2026-08-24, RB23 on 08-30 (the FALLBACK at line 34, 'Measured 2026-08-30') and RB22 on 09-06; the draft is over but the ADP feed keeps moving with in-season mocks. PlanBacktest.k
- [low] EraSlate.on() and PlanBacktest.keeperIDs() place keepers by joined-count rank while rankOn/tiersOf use the source rank - TRAPS #80 not swept here — `src/main/java/EraSlate.java:254`: board.ids() holds only men whose name joined an actuals id; a man who failed the join (TRAPS #80 lists Aaron Rodgers 2023 at QB11, Gabriel Davis 2022 at WR25, Joe Mixon 2025 at RB31) is absent, so every keeper slot below him lands one man d
- [low] FogFit hardcodes the season list that its siblings compute from the league, and tiers by 1-12/13-36/37+ while the objective tiers by twelves — `src/main/java/FogFit.java:61`: OutcomeDistributions.all (line 69), TrustCoefficient (97, 312) and StartSit.calibrate (307) all use `for(int year = current - 5; year < current; year++)`. Identical today; when the league rolls to 2027 the fog constants InsuranceTest and Li
- [low] InsuranceTest's fixed rounds 1-7 are the retired WR-heavy shape with the tight end at 79, not the plan the RUNBOOK commits to — `src/main/java/InsuranceTest.java:28`: The javadoc says 'Rounds 1-7 are held at the shipped plan's starting-nine fill'. The shipped plan is now RB RB RB WR WR WR with the tight end moved to round 8 (RUNBOOK:196-207, PlanShowdown's +1.8 on 3,000 paired trials), so the 'insurance'
- [low] InsuranceTest's own main and LiveInsurance index the wire man one deeper than the rank replacementRanks exports and WeeklyStarterValue/BenchValue consume — `src/main/java/InsuranceTest.java:135`: replacementRanks returns a 1-based rank (drafted/seasons + 1 = 'the first undrafted man', WR81). WeeklyStarterValue.forCurrentBoard takes `Math.max(0, rank - 1)` (line 364) and BenchValue.atRank takes `get(rank - 1)` (line 390) - the 81st r
- [low] InsuranceTest and LiveInsurance read the wire one man deeper than replacementRanks names: a 1-based rank used as a 0-based index — `src/main/java/InsuranceTest.java:135`: replacementRanks returns drafted/seasons + 1, the 1-based 'next man up' (QB21 when 20 are drafted, printed as such by BenchValue). pool is sorted descending from index 0, so pool.get(21) is QB22 and LiveInsurance's three-man wire is QB22-24
- [low] LateRoundValue multiplies an 18-week-denominated wire rate by 17 and subtracts it from 18-week realised band totals — `src/main/java/LateRoundValue.java:59`: data/late-round-value-2026-08-31.txt: 'streaming a defence pays 8.7 a week, 149 a season ... drafting the best defence beats streaming by -13'. 8.74 x 17 = 149 but the 135.8/140.3 it is compared to is summed over WeeklyActuals.WEEKS = 18, s
- [low] Four places still say -PleagueScoredActuals defaults OFF; enabled() defaults it ON — `src/main/java/LeagueActuals.java:29`: The class header, build.gradle's knob comment ('grade outcomes under the league's own scoring instead of Sleeper's pts_half_ppr. Off by default on purpose.'), PlanBacktest.java:280-283 ('With the flag off these are exactly HistoricalActuals
- [low] Justin's pick numbers {7, 18, 31, ... 186} are literals in nine files with no test against the configuration's own pickNumberFor/keeperOccupiedPickNumbers — `src/main/java/PlanBacktest.java:86`: The schedule is slot 7 with keepers at rounds 12 and 13; AAAConfiguration.pickNumberFor(round) and keeperOccupiedPickNumbers() already compute it from the draft order. Next season's slot or keeper rounds change and every backtest, DefenceRo
- [low] The 2026 pick schedule {7, 18, 31, ... 186} is typed as a literal in a dozen files while the configuration computes it — `src/main/java/PlanBacktest.java:86`: Every copy agrees with PreFlight's [7, 18, ..., 186] today (DRAFT-READY.md:61-62). The 2027 draft assigns a new slot and possibly different keeper rounds; each literal has to be found and edited by hand (TightEndTiming.java:61-62,384; Defen
- [low] PlanBacktest still cites the runbook by bare line numbers (':78', ':79') that the citation lint does not match and that now point at other rows — `src/main/java/PlanBacktest.java:96`: Commit 0a93dba converted the leading citation to a heading and left the two bare ':78' / ':79' references; the round-11 and round-14 rows are now RUNBOOK.md:255-256, and because the lint anchors on the literal 'RUNBOOK.md:' these escape it,
- [low] Board.poolRanks ranks only the board's joined men by projection while the pool ranks every projected man, so deep cells are named by one order and filled from another — `src/main/java/PlanBacktest.java:193`: A projected man missing from the FantasyPros file, or one with no actuals row (never played), is absent from ids, so every board man below him is promoted one projection rank and tiersOf() looks him up in a cell the pool computed over bette
- [low] Dangling javadoc in PlanBacktest describes draft() but is attached to nothing — `src/main/java/PlanBacktest.java:357`: Two consecutive javadoc blocks; javac attaches only the second to keeperIDs, so the description of draft() (which lives at :501 with no javadoc) is orphaned and the javadoc reference check that `check` runs cannot see it.
- [low] PlanShowdown's labels are reversed against the recorded Model A shape and its 'live engine' is not the engine that ran; it is the unnamed source of the runbook's '+1.8 on 3,000 paired trials' — `src/main/java/PlanShowdown.java:27`: RUNBOOK.md:239 ('RB-heavy beat WR-heavy by +1.8 on 3,000 paired trials, so round 2 is an RB') is this tool's paired delta with no standard error quoted (default trials is 4000, the tool prints +/-SE); the row it calls Model A is the WR-heav
- [low] Dead locals in the TE tools: an unused 'representative season' and a swap candidate computed only to be null-checked — `src/main/java/StarterContribution.java:219`: No numeric consequence today; wrLate2's null check can skip a season for a second late receiver that option C never uses, which would silently shrink the swap table's season count without saying so.
- [low] TightEndTiming.load() duplicates main()'s season scan instead of main calling load() — `src/main/java/TightEndTiming.java:45`: StarterContribution.java:182 uses load(); TightEndTiming's own tables use the copy in main. A change to the season filter or the join in one path (for example routing through LeagueActuals, or raising the >100 threshold) does not reach the 
- [low] The runbook's injury-aware TE table is built on Model A's RB WR RB WR WR WR opening, not the committed RB RB RB WR WR WR it is presented as evidence for — `src/main/java/TightEndTiming.java:384`: RUNBOOK.md:266-276 quotes this table under 'Round 7 is no longer the tight end' as the reason the committed plan (RUNBOOK.md:245-250: RB RB RB WR WR WR) takes TE at 90; the front six that were scored hold four receivers, which is precisely 
- [low] TightEndTiming hard-codes three different wire/replacement depths (TE 18, RB/WR 80; TE 19, RB 48, WR 60) beside the measured replacementRanks — `src/main/java/TightEndTiming.java:600`: The RB wire band in injuryAwareSwap (TightEndTiming.java:426-429, wireLevel/17) is RB81-92, twenty men deeper than the league's real RB wire (RB61+), and StarterContribution.java:407,486 inherit it; the swap's B-A and C-A differences are un
- [low] TightEndTiming.replacement() bust bar (TE19/RB48/WR60) disagrees with the wire the same file and the league measure (TE19/RB61/WR81) — `src/main/java/TightEndTiming.java:627`: Used only for the 'bust' column of curve(): a back is a bust if he finished below RB48 by realised points, while wireLevel two methods later (and InsuranceTest.replacementRanks) put the league's replacement at RB61/WR81. The 'bust' percenta
- [low] TightEndTiming (and through it StarterContribution) grades outcomes on the feed's pts_half_ppr while the rest of the repo grades in league points since 2026-09-04 — `src/main/java/TightEndTiming.java:635`: BenchValue, PlanBacktest, OutcomeDistributions and DefenceVersusDepth all moved to LeagueActuals.seasonPoints; this join did not. No quarterback is involved so the 6-pt TD rule does not bite, but the -1 per fumble the feed stops charging in
- [low] TightEndTiming grades outcomes with the raw feed (pts_half_ppr) rather than the LeagueActuals dispatcher, and nothing lints graders outside the switch — `src/main/java/TightEndTiming.java:635`: LeagueActuals' own comment says 'anything that grades outcomes should call the dispatchers rather than the raw feed, so one switch moves all of it at once', and the switch flipped to league scoring on 2026-09-04. TightEndTiming.join (and th
- [low] The defence wire rank is a `DEF ? 13 : 24` literal in seven files because replacementRanks never returns DEF, and 13 follows WireRateStress's rounding, not replacementRanks' truncation — `src/main/java/WeeklyStarterValue.java:362`: InsuranceTest.replacementRanks counts only StartingLineup.isSkillPosition (line 52), so every consumer reaches its fallback for DEF: WeeklyStarterValue:363 and :401, RiskDiscountedValue:201, BustBoomValue:295, StarterSumCheck:61, ObjectiveA
- [low] AppetiteCapTripwireTest runs on whichever ADP file the filesystem lists first, and its 'declines a third quarterback' assertion allows two drafted QBs — `src/test/java/AppetiteCapTripwireTest.java:36`: File.listFiles() order is unspecified; on APFS it is usually 2021 first, on another filesystem it may be 2025, so the tripwire that must 'never bind' is exercised on one season chosen by the OS, and a cap that binds on 2024's board only is 
- [low] ModelDisciplineTest derives the 94-point bar from itself before asserting it — `src/test/java/ModelDisciplineTest.java:113`: The first assertion is x == (x / t) * t and cannot fail. The second checks only that 125 is what the formula gives for 94, so both published figures are typed into the test rather than read from data/power-backtest-2026-08-30.txt ('signific
- [low] RunbookCitationTest checks a heading exists and is over 40 characters, not that it 'still holds the rule'; line-number citations survive in tests and in ':78' form — `src/test/java/RunbookCitationTest.java:48`: RosterRules cites 'RUNBOOK.md "The committed plan (laptop-dies fallback)"' when it refuses a second quarterback; replace that section's table with a paragraph saying take a QB in round 3 and the citation still passes. Meanwhile RosterRulesT
- [low] SurvivalDependentToolsTest and LateRoundToolTest are satisfied by a comment that names the warm-up — `src/test/java/SurvivalDependentToolsTest.java:73`: A tool whose javadoc says 'unlike the others this does not call warmSurvival' passes the lint that exists to catch tools that do not call warmSurvival. I stripped comments from every main with a LiveBoard rollout call (CycleTiming, DefenceT


## 4. Keeper selector
confirmed 10 · refuted 2 · unverified 52

### Confirmed

**[high/small] Console KEEP flag is the top two by surplus, position-blind, while keeperValue prices the best LEGAL pair (one starter per QB/TE/DEF)** — `src/main/java/LeagueConsole.java:939`
```
keeperRows < 2 && worth != null && worth > 0,
// versus, four hundred lines earlier on the same page:
ids -> value.of(ids) + TradeMarket.keeperValue(ids, surplus, everyPosition);   // line 531
```
- scenario: myMen is sorted by surplus (line 923) and the first two positive rows get keep=true regardless of position. TradeMarket.keeperValue (the position-aware fix landed today) refuses two men at a ONE_STARTER position. Today the top two on the live feed are Tuten RB +33.8 and Nix QB +32.8, so both agree. The RUNBOOK's own example ('Without Tuten the fallback is Nix plus the Ravens, not Nix plus Purdy') is exactly where they split: after a Tuten trade - which the trade tab proposes ('Bhayshul Tuten -> Josh Allen +22.7') - the next console build marks Nix AND Purdy (two QBs) as the 2027 keepers on the 'What you can sell' tab, while every trade on the same page is priced on Nix + Ravens. LeagueConsoleTest.exactlyTheBestTwoKeepersAreMarked cannot see it: it checks <=2 marked and surplus>0, not the one-starter rule.
- fix: Derive the keep set from the pair keeperValue actually chose: add a TradeMarket.bestKeeperPair(roster, surplus, positionOf) returning the two ids, have keeperValue sum that pair, and set keep = pair.contains(id). Then extend exactlyTheBestTwoKeepersAreMarked to assert no two KEEP rows share a ONE_STARTER position.
- siblings: src/test/java/LeagueConsoleTest.java:689

**[high/trivial] TradeMarket prices every rival's keeper loss position-blind while the console prices it position-aware** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);

// lines 375-378:
static Side lossAverseOnKeepers(java.util.function.ToDoubleFunction<List<String>> season,
                                Map<String, Double> surplus){
    return lossAverseOnKeepers(season, surplus, Map.of());
}

// LeagueConsole.java:537-538 (the page):
TradeMarket.Side theirSide = withKeepers
        ? TradeMarket.lossAverseOnKeepers(seasonOnly, surplus, everyPosition)
```
- scenario: Commit ac62628 ('position-aware keeperValue') fixed Justin's own side in TradeMarket (line 668 now passes everyPosition) and the page's rival side, but the terminal board's rival side still goes through the two-argument overload, which hands keeperValue an empty positionOf so `first != null && first == second && ONE_STARTER.contains(first)` never fires. On the 2026-09-08 rosters tommyrads holds Caleb Williams (2027 cost r13) and Jared Goff (r16); replicating keeperPoints on the projection feed both carry positive surplus (~20.8 and ~12.5 on pts_half_ppr, directional). The terminal TradeMarket charges tommyrads Goff's whole surplus as keeper loss for any trade that asks for Goff; the console charges the drop in his best LEGAL pair, which is ~0 because two QBs cannot both be kept. Same shape for jerem9604 (Mahomes r16 + Hurts r5) and Renteez (Love r16 + Lamar r3) whenever both go positive. The 'him' column and the mutual filter on the terminal board disagree with the page on the same roster, the sixth instance of this week's pattern.
- fix: Pass everyPosition at line 672: `lossAverseOnKeepers(season, surplus, everyPosition)`, and delete the two-argument overload at 375-378 so nothing can reach the position-blind path again (the same treatment ac62628 gave the keeperValue overload). Add a TradeMarketTest that builds a rival with two positive-surplus QBs and asserts the loss-averse side charges only the pair drop.
- siblings: src/main/java/TradeMarket.java:375

**[medium/medium] KeeperDriftCheck's drift is a hand-typed 11.7 measured over the first eight QBs, applied to a QB taken 175th** — `src/main/java/KeeperDriftCheck.java:43`
```
return Double.parseDouble(System.getProperty("qbDrift", "11.7"));
...
// QbMarketGap.java:109
int compared = Math.min(Math.min(realQbPicks.size(), droneQbPicks.size()), 8);
```
- scenario: The class doc says `-PqbDrift=12` and 'mean about twelve', RUNBOOK 137-139 says 'about twelve picks', the code says 11.7, and there is no QbMarketGap artifact under data/ or any test pinning the literal to the measurement (the only other 11.7 in the repo is an unrelated TE constant in SelectionModel). Separately, QbMarketGap's drone gap averages the first eight quarterbacks taken each season - the QB1-QB8 market - and KeeperDriftCheck shifts the curve at pick 175 for Bo Nix and 138 for Purdy, roughly QB15-QB18, a population the gap never sampled (TRAPS #79/#81/#101 pattern). The runbook then acts on the shifted numbers ('halved Bo Nix (+32.8 to +15.2) and erased Brock Purdy').
- fix: Have KeeperDriftCheck compute the drift from QbMarketGap's per-season gaps (commit the artifact and read it, or expose a static method), and either measure the gap on the QB rank band the keeper sits in or state on the report that the shift is extrapolated from QB1-8.
- siblings: src/main/java/QbMarketGap.java:109, RUNBOOK.md:137

**[medium/small] KeeperOrigin's 'who drafted him' join stops at the keeper pick, so the runbook's 0.88 capture rate is really 0.71** — `src/main/java/KeeperOrigin.java:72`
```
// player id -> who drafted him, in the most recent draft that has him
...
for(JsonArray draft : drafts){
    for(JsonElement element : draft){
        ...
        draftedBy.putIfAbsent(id.getAsString(), by.getAsString());
```
- scenario: A keeper pick carries `is_keeper` and `picked_by` = the keeping manager, so for any repeat keeper the 'most recent draft that has him' is last year's keeper pick, and the man is counted as self-drafted whoever actually drafted him. KeeperPricing.originalDraftRound (lines 219-230) does this join correctly by walking past `is_keeper` picks. Recomputed on the 2026 board's 24 keepers with the cached previous drafts: KeeperOrigin's join gives 21/24 = 0.88 (the number RUNBOOK lines 401-412 quote, 'one of only three keepers in twenty-four'); walking to the original drafter gives 17/24 = 0.71 - Jaxon Smith-Njigba (drafted by KevinDA, kept by itsabust), Jayden Daniels (tommyrads -> BHier) and Chase Brown (JFMarino -> Hamrliks) are misfiled as self-drafted, and Kyren Williams was never drafted. StashValue scales its keeper term by this, so the runbook's 'How much of this is next year: 30%' rests on an overstated 0.88.
- fix: Walk each keeper back to the first pick without `is_keeper` (reuse KeeperPricing's walk, made package-visible) before reading `picked_by`; regenerate the KeeperOrigin numbers and correct RUNBOOK 401-412.
- siblings: src/main/java/KeeperPricing.java:219, src/main/java/StashValue.java:44, RUNBOOK.md:401

**[medium/small] Console keeper panel names the top two surpluses, not the legal pair that keeperValue prices** — `src/main/java/LeagueConsole.java:939`
```
myMen.sort(Comparator.comparingDouble((String id) -> -surplus.getOrDefault(id, 0.0)));
...
    keeperRows < 2 && worth != null && worth > 0,
...
const kept = D.keepers2027.filter(r=>r.keep);
... worth <b>${f1(kept.reduce((a,r)=>a+r.surplus,0))}</b> together.
```
- scenario: The position-aware fix landed in TradeMarket.keeperValue (lines 1116-1141: a pair may not put two men at a ONE_STARTER position) and in the trades column, but the 'What you can sell' panel's keep flag is still 'first two rows sorted by surplus' and the note sums them. Today the top two are Tuten (RB, 33.8) and Nix (QB, 32.8), a legal pair, so the page happens to be right. The moment Tuten is traded - the exact scenario RUNBOOK lines 94-97 describe ('Without Tuten the fallback is Nix plus the Ravens, not Nix plus Purdy') - the panel names Bo Nix + Brock Purdy 'worth 52.5 together' while every trade row on the same page charges the Nix + Ravens pair (35.8). LeagueConsoleTest.exactlyTheBestTwoKeepersAreMarked (line 657) checks only surplus>0, round non-null and count<=2, so it cannot see this.
- fix: Derive the keep flags from the same pair keeperValue chooses: expose a `bestPair(roster, surplus, positionOf)` in TradeMarket returning the two ids, use it for `keep` and for the note's total, and add a fixture test where the top two surpluses are both QBs.
- siblings: src/main/java/TradeMarket.java:1116, src/main/java/LeagueConsole.java:1254

**[medium/small] NextYearKeepers applies the first-two-rounds rule to the escalated keeper cost, KeeperPricing to the original draft round** — `src/main/java/NextYearKeepers.java:123`
```
if(round <= KeeperPricing.HIGHEST_KEEPABLE_DRAFT_ROUND){
    costs.put(id, new Cost(id, name, round,
            "taken in round " + round + "; the first "
                    + KeeperPricing.HIGHEST_KEEPABLE_DRAFT_ROUND
                    + " rounds cannot be kept"));
```
- scenario: KeeperPricing.cost (178-181) checks `originalDraftRound`, walking back past kept years, and KeeperPricingTest.theFirstTwoRoundsRuleFollowsTheOriginalDraftNotTheKeeperCost (line 229) pins that reading: 'Drafted in the 5th, kept twice, now costing a 3rd. Still legal: the rule is about where they were drafted.' NextYearKeepers reads `round` off the current pick, which for a kept man is his escalated cost. On the 2026 board no keeper sits at r1-r2, so all 24 agree today (verified against the cached board). Trey McBride (drafted r4 2024, kept r4 2025, r3 2026) costs r2 in 2027; in the 2027 season this code refuses him for 2028 ('taken in round 2') while the commissioner's rules and KeeperPricing allow him at r1. KeeperEligibility.java:116 and :132 carry the same misreading.
- fix: Give NextYearKeepers.from the earlier boards it already receives for consecutiveYears and apply the round-2 test to the first non-keeper pick, mirroring KeeperPricing.originalDraftRound; add a test with a kept man sitting at r2.
- siblings: src/main/java/KeeperPricing.java:178, src/main/java/KeeperEligibility.java:116, src/test/java/KeeperPricingTest.java:229

**[medium/small] Waiver pickups cost a tenth on the page and nothing in TradeMarket, TradeStability and KeeperDriftCheck** — `src/main/java/TradeMarket.java:663`
```
Map<String, Integer> keeperRound = NextYearKeepers.roundsForThisLeague(configuration);
...
for(String id : keeperRound.keySet()){
    surplus.put(id, keeperPoints(...));
```
- scenario: NextYearKeepers.from only prices men who appear on the draft board, and LeagueConsole.java:516-521 adds `keeperRound.put(id, Keeper.UNDRAFTED_ROUND_COST)` for every rostered man it did not see - correct per the ruleset. TradeMarket (663), TradeStability (119) and KeeperDriftCheck (99-101, `if(round == null) continue`) never do, so a waiver man has no round and a surplus of 0 there. Seven rostered men are undrafted on 2026-09-08 (Dalton Schultz on Justin's roster - r10, surplus 0.00, margin -13.6 on the page; Roschon Johnson, Vele, Justice Hill, Holani, Saylors, Caleb Douglas elsewhere). All price at 0 today so no number moves yet; the first breakout pickup with a positive surplus at r10 will be charged to a rival on the page and free in the terminal board. NextYearKeepers.undrafted (line 192) exists with a test (NextYearKeepersTest 71-76) and no caller.
- fix: Move the undrafted default into NextYearKeepers.roundsForThisLeague (take the roster ids, fill UNDRAFTED_ROUND_COST for absentees) so all four callers get it, and delete or use `undrafted`.
- siblings: src/main/java/LeagueConsole.java:516, src/main/java/TradeStability.java:119, src/main/java/KeeperDriftCheck.java:99, src/main/java/NextYearKeepers.java:192

**[medium/trivial] TradeMarket prices the rival's keeper loss position-blind; the console prices it position-aware** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);
...
static Side lossAverseOnKeepers(... ToDoubleFunction<List<String>> season, Map<String, Double> surplus){
    return lossAverseOnKeepers(season, surplus, Map.of());
}
```
- scenario: 81e616c added the positionOf overload so keeperValue refuses two men at a ONE_STARTER slot, and LeagueConsole.java:538 calls `lossAverseOnKeepers(seasonOnly, surplus, everyPosition)`. TradeMarket.main kept the two-argument form, whose `Map.of()` makes `first == second` never true, so a rival holding two keepable quarterbacks is charged for both when asked for one. The page and the terminal board therefore disagree on 'him' for any rival with two positive QB (or TE/DEF) surpluses, and TradeMarketTest line 201 tests the two-argument form only.
- fix: Pass `everyPosition` at TradeMarket.java:672 (it is already built at 641-647), delete the two-argument overloads of lossAverseOnKeepers and keeperValue, and fix the two tests that use them.
- siblings: src/main/java/LeagueConsole.java:538, src/test/java/TradeMarketTest.java:201

**[medium/trivial] TradeStability still carries pool 6 while the board it audits searches DEFAULT_POOL 8** — `src/main/java/TradeStability.java:80`
```
int pool = Integer.getInteger("pool", 6);
```
- scenario: TRAPS #128 fixed 'pool 6 vs 8' for LeagueConsole and says 'The fix is the constant, not the number: TradeMarket.DEFAULT_POOL, read by both, with a test that fails if either main grows a literal again' - but SearchWidthTest.bothMainsTakeTheirPoolFromTheOneConstant (line 33) lists only LeagueConsole.java and TradeMarket.java. TradeStability's stated method is 'take the board's best trades, re-value those SAME trades' (class doc), yet it re-searches its own board at 6, so its 'top 8' is drawn from 81-trade-per-rival board rather than the 182 the console shows. The SEND-tier three-for-three TRAPS #128 says raising the pool surfaced is exactly the kind of row this weekly noise check never measures.
- fix: Replace the literal with `TradeMarket.DEFAULT_POOL` and add "TradeStability.java" to SearchWidthTest's list.
- siblings: src/test/java/SearchWidthTest.java:33, src/main/java/LeagueConsole.java:234, src/main/java/TradeMarket.java:605

**[medium/small] TradeStability builds the keeper curve from rostered men only - the defect KeeperDriftCheck documents fixing** — `src/main/java/TradeStability.java:116`
```
Map<String, Position> positionOf = new HashMap<>();
for(Map.Entry<String, String> entry : ownerOf.entrySet()){
    ...
    positionOf.put(entry.getKey(), player == null ? null : player.position);
}
...
Map<Position, java.util.TreeMap<Double, Double>> bestByAdp =
        TradeMarket.bestStillAvailable(points, positionOf);
```
- scenario: KeeperDriftCheck.java:65-71 and LeagueConsole.java:482-490 both build `everyPosition` over the whole projection feed because, built from the 192 rostered men, 'the deepest quarterback it knew had an ADP under 175, so ceilingEntry(175) returned nothing ... Bo Nix's keeper surplus came out as his ENTIRE season projection: 347.7'. TradeStability was not swept. On the 2026-09-08 rosters the deepest rostered QB is Jordan Love at ADP 164.9; Nix's 2027 keeper pick is 175 (r15, slot 7), so this tool prices him at his full projection instead of the console's 32.8. A running max over a subset is never larger than over the full pool, so every keeper's surplus here is >= the console's, and any man whose true surplus is under the 25 marker threshold but over it on the thin curve gets a false [KEEPER] on a board that claims to audit the console.
- fix: Build `everyPosition` from `points.keySet()` exactly as LeagueConsole 482-488 does (or lift that loop into TradeMarket next to bestStillAvailable) and pass it to bestStillAvailable and keeperPoints; add TradeStability to a source test that forbids `bestStillAvailable(points, positionOf)`.
- siblings: src/main/java/KeeperDriftCheck.java:72, src/main/java/LeagueConsole.java:482, src/main/java/TradeMarket.java:641

### Refuted
- **captureRate silently becomes 1.0 whenever the roster keepers field is empty, which it is all season** — `src/main/java/KeeperOrigin.java:37`
  - refuted: Part (a) checks out: KeeperOrigin.java:37 is exactly as quoted, and the scenario is real — seriousRostersForKeepers...2026-09-08.txt confirms all 12 rosters currently have keepers: null (matching TRAPS #65's documented finding that Sleeper empties the roster keepers field right after the draft, and "feeds still float"). So captureRate() does return 1.0 right now via the counts.total()==0 branch. But part (b) fails: the only production consumer of KeeperOrigin.captureRate()/StashValue.keeperTermFor is LateWaitOrTake (grep confirms no other caller), and RUNBOOK.md:357 explicitly says "**Superseded:** LateWaitOrTake — same idea, weaker value model," directing use of LiveLateRounds instead for the actual live rounds 8-16 decision — and LiveLateRounds contains zero references to StashValue, KeeperOrigin, captureRate, or keeperTermFor. The POS/ROUNDS table in RUNBOOK.md that StashValue.main() prints is a static reference table, not something Justin recomputes live. So no number in a tool Justin actually runs (LiveLateRounds, or anything else in RUNBOOK's weekly/draft workflow) is affected by this capture-rate flaw today. Downgrading to low: the underlying bug is real and would bite if StashValue/LateWaitOrTake or the static table were resurrected/regenerated live, but it currently reaches no decision or weekly tool per the rubric.
- **KeeperAudit reads the draft board through the per-day cache on the one day the board is being edited** — `src/main/java/AAAConfiguration.java:111`
  - refuted: Part (a) is technically accurate: AAAConfiguration.java:111 getTodaysDraftPicks() calls InOutUtilities.getTodaysWebPage, which is a once-per-calendar-day cache (confirmed by the actual daily draftData*.txt files, one per date), and KeeperAudit.java:61 does read the board through that call. But the finding's supporting evidence for reachability collapses under inspection: (1) the cited "Jerry Jeudy/Jayden Daniels...hand edit" case is not a commissioner board edit at all — KeeperPricing.java:245-249 shows it's a historical (2025 season) ADP tie-break rule dispute unrelated to KeeperAudit or draft-day editing. The finding fabricates a connection between an ADP tie-break anecdote and a "hand edit" of the draft board. (2) The "picks file changing size" evidence (10301 bytes through 09-01, 82167 on 09-02) is not evidence of an intra-day edit — these are two different calendar days' cache files (one snapshot per day, per the actual filenames draftData...2026-09-01.txt and ...2026-09-02.txt), and the jump corresponds to the draft itself happening on 09-01 night filling the board, not a same-day commissioner edit being missed. There is no single day in the repo's cached files showing two different sizes, which is the only evidence that would actually support "the board moves within the day it is audited." (3) RUNBOOK.md's actual KeeperAudit instruction (line ~176-179) has it run exactly once, Monday evening before the draft, as one step in a linear pre-draft checklist (AdpSnapshot -> smokeTest -> KeeperAudit -> DraftNight) — it does not document or imply a same-day rerun-after-edit workflow, so the specific failure path (morning run caches, commissioner edits midday, rerun same day silently reports stale 24/24) is not shown to be a real, reachable usage pattern for this tool as documented. Given the misattributed supporting evidence and unconfirmed reachability of the exact repro, this does not clear the bar to stand as a live finding.

### Unverified (52; found by a lens, not yet refuted — verify before acting)
- [medium] KeeperAudit reports 'no keepers on the board yet' when Sleeper has cleared the declarations, not the board — `src/main/java/KeeperAudit.java:53`: The audit iterates configuration.getTodaysKeepers() (line 62), which reads each roster's `keepers` array. On the 2026-09-08 rosters cache that field is null for all twelve teams - Sleeper drops it after the draft - so agreed == 0 with no di
- [medium] KeeperAudit reports 'no keepers placed on the board' when the board has 24 and the roster declarations are what is empty — `src/main/java/KeeperAudit.java:54`: agreed and disagreements are only ever incremented while iterating getTodaysKeepers(), which is empty whenever the rosters' `keepers` arrays are null - the state Sleeper leaves after the draft (TRAPS #65) and the state today (all 12 rosters
- [medium] KeeperAudit reports 'no keepers have been placed on the draft board yet' when the board has 24 and it is the rosters' keepers field that Sleeper cleared — `src/main/java/KeeperAudit.java:101`: boardIsEmpty() is `agreed == 0 && disagreements.isEmpty()`, which is also true when getTodaysKeepers() is empty. Every cached rosters feed from 2026-09-02 onward carries `keepers: null` on all twelve rosters (build.gradle records it: 'every
- [medium] KeeperDriftCheck's QB drift is a hand-typed 11.7 with no committed measurement behind it — `src/main/java/KeeperDriftCheck.java:43`: The runbook's verdicts (Nix +32.8 -> +15.2, Purdy erased) rest on this constant. QbMarketGap prints its drone gap to stdout and writes nothing to data/; no file in the repo contains the per-season gaps or a mean of 11.7 (RUNBOOK says 'about
- [medium] KeeperOrigin counts a previous keeper pick as 'drafted by the keeping manager'; captureRate is 0.71, not the 0.88 the runbook quotes — `src/main/java/KeeperOrigin.java:72`: A keeper pick carries picked_by = the manager who KEPT him, and the newest-first putIfAbsent takes it as the draft. Walking the same cached drafts back to the first non-keeper pick: Jaxon Smith-Njigba (kept by itsabust, drafted by KevinDA),
- [medium] The page's KEEP marks are the top two surpluses, position-blind, while its trade pricing uses the best legal pair — `src/main/java/LeagueConsole.java:939`: TradeMarket.keeperValue (1116-1141) was made position-aware on 2026-09-08 precisely because 'keeping purdy and nix would nerf one of the two', and every trade on the page is charged the drop in the best LEGAL pair. The keeper panel Justin r
- [medium] Every keeper tool allows a round-2 keeper cost; the league's own history says 0-for-4 on that — `src/main/java/NextYearKeepers.java:140`: Three implementations of the first-two-rounds rule put the check in three different places, and all three let a man kept at r3 be kept again at r2. The cached drafts say otherwise: every player ever kept at r3 was released and drafted in ro
- [medium] NextYearKeepers never applies the same-round bump that KeeperPricing applies and the league applied on the 2026 board — `src/main/java/NextYearKeepers.java:140`: Replaying NextYearKeepers' rules on the 2025 board against the 24 keeper rounds the commissioner entered for 2026 reproduces 23 of 24; the miss is Javonte Williams, drafted r6 in 2025 and kept at r5 in 2026 because Renteez's other keeper (M
- [medium] The three-year cap in NextYearKeepers (consecutiveYears and the two-arg from) has no behavioural test, and no unit test ever runs from() on a full board — `src/main/java/NextYearKeepers.java:159`: NextYearKeepersTest calls only the one-arg from() (priorYears = Map.of()), so the fix that removed '472 points of keeper surplus that cannot exist' for Taylor, Achane, Collins, K. Williams, LaPorta, Nacua and Chase Brown is guarded only by 
- [medium] The QB drift is measured over the first eight QBs and applied to the fifteenth — `src/main/java/QbMarketGap.java:109`: The drone gap is the mean displacement of QB1-QB8 per season. KeeperDriftCheck subtracts it from the pick number of Bo Nix at r15 (pick 175, ~QB15-18 by ADP) and Brock Purdy at r12 (pick 138), quarterbacks that were never in the population 
- [medium] Waiver pickups are priced at a tenth on the page but silently dropped by TradeMarket, TradeStability and KeeperDriftCheck — `src/main/java/TradeMarket.java:660`: Seven rostered men were not in the 2026 draft (Justin: Dalton Schultz; Hamrliks: Caleb Douglas, Jacob Saylors; Renteez: Vele, Roschon Johnson; itsabust: Justice Hill; tommyrads: Holani). The console prices each at r10; TradeMarket.main, Tra
- [medium] Undrafted men are priced at a tenth on the console page and silently omitted by TradeMarket, KeeperDriftCheck and TradeStability — `src/main/java/TradeMarket.java:660`: NextYearKeepers.from iterates the draft board only, so a man claimed off waivers has no entry. LeagueConsole.java:517-522 adds the ruleset's tenth for him (`if(!nextYear.containsKey(id)) keeperRound.put(id, Keeper.UNDRAFTED_ROUND_COST)`); T
- [medium] Terminal keeper tools omit waiver pickups that the console prices at a tenth — `src/main/java/TradeMarket.java:663`: NextYearKeepers.roundsForThisLeague only knows men who were in the 2026 draft. LeagueConsole adds every rostered man who was not, at Keeper.UNDRAFTED_ROUND_COST; TradeMarket (663), TradeStability (119) and KeeperDriftCheck (100: `if(round =
- [medium] TradeStability still searches at a literal pool of 6 while the page it audits now searches DEFAULT_POOL 8 — `src/main/java/TradeStability.java:80`: Commit 18fd956 widened the page to 8 and added SearchWidthTest, which checks only LeagueConsole.java and TradeMarket.java. TradeStability re-values 'the board under the first seed, which is the board the page shows' (line 131) but builds th
- [medium] TradeStability builds its keeper curve from rostered men only, the exact failure KeeperDriftCheck documents — `src/main/java/TradeStability.java:115`: LeagueConsole (490) and TradeMarket (658) build `everyPosition` over the whole projection feed before calling bestStillAvailable; TradeStability passes the 192-man roster map. Replicating the curve on the cached feed: Bo Nix at r15 (pick 17
- [medium] TradeStability builds the availability curve from rostered men only - the exact fault KeeperDriftCheck documents as fixed — `src/main/java/TradeStability.java:116`: The deepest rostered quarterback has ADP 164.3 (full pool: 999), so for any QB priced at r15 or r16 (Justin's picks 175/186) ceilingEntry returns null, the replacement is 0 and surplus is the whole projection. Nix reads about 303 here again
- [medium] TradeStability builds the replacement curve from rostered men only - the 'missing denominator' 81e616c fixed in its three siblings — `src/main/java/TradeStability.java:116`: KeeperDriftCheck.java:65-71 documents exactly this construction: 'Built from positionOf - which came from LeagueOwners and therefore holds only the 192 men on rosters - the deepest quarterback it knew had an ADP under 175, so ceilingEntry(1
- [medium] KeeperAuditSmokeTest cannot fail — `src/test/java/KeeperAuditSmokeTest.java:33`: `disagreements` is a final field initialised to `new ArrayList<>()` in KeeperAudit.Report (line 49), so the assertion is a tautology; the only test of the live audit passes on any board, including one with 24 disagreements or one the audit 
- [medium] KeeperAuditSmokeTest's live check asserts non-null on a final field initialised inline - it cannot fail — `src/test/java/KeeperAuditSmokeTest.java:33`: The runbook says 'If KeeperAudit reports anything other than 24 matching the rules, STOP'. The smoke test that runs the audit against the live board passes whether it reports 24/24, 0/24 or an empty board, because the only assertion is on a
- [medium] KeeperAuditSmokeTest cannot fail - it asserts a final, never-null field — `src/test/java/KeeperAuditSmokeTest.java:33`: KeeperAudit.Report declares `public final List<Finding> disagreements = new ArrayList<>();` so the assertion holds for every possible report, including the one the tool now produces on the cleared rosters feed (agreed 0, disagreements 0, no
- [medium] KeeperChooserSmokeTest's eligibility assertion is a tautology — `src/test/java/KeeperChooserSmokeTest.java:50`: `round > 2 || round >= 1` is true for every round >= 1, and KeeperPricing removes rounds < 1 before returning, so this passes for any candidate at any price - including a man priced at round 1 or 2 by an escalation bug. The test name promis
- [medium] KeeperDriftCheckTest checks the newest committed artifact, so a code regression cannot fail it — `src/test/java/KeeperDriftCheckTest.java:22`: Both tests parse data/keeper-drift-2026-09-07.txt, which was written before ac62628 and 4b51580 changed driftAware's signature and pick arithmetic. Flip the sign in driftAware today and `./gradlew check` stays green until somebody regenerat
- [medium] KeeperDriftCheckTest tests the newest data/keeper-drift-*.txt artifact, not the code — `src/test/java/KeeperDriftCheckTest.java:22`: Both tests parse whatever file was last written by a manual `run -Pmain=KeeperDriftCheck` (data/keeper-drift-2026-09-07.txt, built on the roster before Dalton Schultz replaced Tre' Harris). Flip the sign in driftAware (`pick + drift`), drop
- [medium] exactlyTheBestTwoKeepersAreMarked asserts at most two, so a page marking nobody passes — `src/test/java/LeagueConsoleTest.java:689`: The docstring says 'Two keepers, not three, and the two with the most surplus'. The assertion accepts 0 or 1 marked rows, does not check that the marked rows are the two highest surpluses, and does not check the one-starter rule keeperValue
- [medium] NextYearKeepersTest's 'edge' assertion cannot fail; the below-first-round refusal has no test — `src/test/java/NextYearKeepersTest.java:65`: The 'edge' input is a round-3 keeper again (cost 2), so the assertion is the first assertion repeated; and for a refused man NextYearKeepers.from stores the ORIGINAL round (`new Cost(id, name, round, "would cost better than a first-round pi
- [low] README recommends keeper tools that read the pre-draft rosters' keepers field or the previous-draft basis; in season they print blanks or answer last August's question — `README.md:40`: KeeperEligibility.standings reads `roster.get("keepers")` and `continue`s when null (KeeperEligibility.java:96-99), so since 2026-09-02 it prints '-- has not declared --' for all twelve managers; KeeperChooser/KeeperValuation/KeeperPlan pri
- [low] RUNBOOK quotes 654 tests; 36 @Test methods have been added since that line was written — `RUNBOOK.md:133`: The number was written in 0a93dba; `git diff 0a93dba..HEAD -- src/test/java | grep -c '^+.*@Test'` is 36 (SeasonOutlook, ConsoleRenders, FaabTie, NextYearKeepers, ProjectionDrift, WireSurvival, KeeperDriftCheck, KeeperBasis, SearchWidth and
- [low] League-id comment labels the 2023 league as 2024 — `src/main/java/AAAConfigurationSleeperLeague.java:10`: The cached league JSON previousLeagueSettings9808897320349941762026-09-08.txt says season 2023, draft 980889732034994177; the 2024 league is 1115743859591434240 (draft 1115743859591434241). Anyone hand-checking keeper history from this comm
- [low] KeeperAudit is owner-blind: it keys entered rounds by player only — `src/main/java/KeeperAudit.java:90`: A keeper entered at the right round on the wrong manager's slot, or an is_keeper pick for a man nobody declared, both pass 24/24 - the audit never looks at picked_by/roster_id and never counts board keepers against declared ones. keeperOccu
- [low] KeeperDriftCheck's 11.7-pick default has no committed provenance, and three documents give three different ranges for the same measurement — `src/main/java/KeeperDriftCheck.java:43`: QbMarketGap prints one drone gap per season (QbMarketGap.java:119-121) and never a mean; no data/ file records its output; so the 11.7 that halves Bo Nix (RUNBOOK 140-141) is a hand-averaged literal the 'numbers from repo code' rule forbids
- [low] driftAware re-implements keeperPoints instead of calling it with a shifted pick — `src/main/java/KeeperDriftCheck.java:147`: AS PRICED comes from TradeMarket.keeperPoints and DRIFT-AWARE from this copy. The next change to keeperPoints (a positional adjustment, a different floor, a curve keyed on something other than ADP) moves one column and not the other, and th
- [low] KeeperEligibility is a third copy of the ruleset and reads declarations that are null all season — `src/main/java/KeeperEligibility.java:111`: Escalation, the cap, the round-2 rule and the undrafted price are re-derived here independently of KeeperPricing (the audited copy) and NextYearKeepers (the forward copy); the round-2 test is applied to last season's escalated round, diverg
- [low] KeeperOrigin.captureRate silently reverts to 1.0 once the rosters feed stops carrying declared keepers, so StashValue's 0.88 discount vanishes in season — `src/main/java/KeeperOrigin.java:34`: count() reads getTodaysKeepers(), empty since 2026-09-02, so total()==0 and the rate is 1.0 rather than the measured 21/24 = 0.88 (data/keeper-origin-2026-08-29.txt) that RUNBOOK lines 401-412 quote and StashValue.java:44/116 multiplies the
- [low] In season every tool reading getTodaysKeepers sees zero keepers; KeeperOrigin.captureRate then silently returns 1.0 and KeeperAudit says the board is empty — `src/main/java/KeeperOrigin.java:37`: Sleeper cleared every roster's keepers field on 2026-09-02 (build.gradle comment), and AAAConfiguration.priceTodaysKeepers (line 263) reads that field. Live today: KeeperAudit reports 'nothing to check' although the 2026 board carries 24 is
- [low] Stray keeper tools: Keepers.java, WhoIsKept, QBMarket, KeeperLedger and the RunDraftWithKeepersTask chain are unreferenced or superseded — `src/main/java/Keepers.java:33`: `grep -rn '\bKeepers\.' src` finds no caller of the pre-2026 Keepers class (its own draftedPlayersLastYearAndRound throws "aaaaa" on the fun league). WhoIsKept is named in no source, test or document and, since the rosters feed cleared `kee
- [low] 'Two keepers' is a literal in keeperValue and the console while the league setting is read elsewhere — `src/main/java/LeagueConsole.java:939`: KeeperChooser.rank (line 120) and KeeperValuation (194) read configuration.getMaxKeepers() (2 in leagueSettings...2026-09-11); keeperValue's pair search and the panel's `< 2` hard-code it. Consistent today; a commissioner change to max_keep
- [low] The first-two-rounds rule is applied to the current pick's round in NextYearKeepers and KeeperEligibility, but to the original draft round in KeeperPricing — `src/main/java/NextYearKeepers.java:123`: KeeperPricing.cost walks back to the round the man was actually drafted (originalDraftRound, lines 178-181 and 219-230) and lets an escalated keeper sit at round 2 or 1; NextYearKeepers refuses any pick sitting in round <= 2, keeper or not,
- [low] NextYearKeepers' 'below a first-round pick' branch is unreachable and the test that names it asserts a value that cannot be otherwise — `src/main/java/NextYearKeepers.java:141`: Line 123 refuses every round <= 2 before the cost is computed, so round >= 3 and cost = round - 1 >= 2; cost < 1 never happens. The 'edge' case in the test is the same r3-kept pick as the case above it, and Cost.round is the raw draft round
- [low] Next-year pricing never applies the same-round bump KeeperPricing applies this year — `src/main/java/NextYearKeepers.java:145`: Two 2027 candidates on one roster that share a round are both priced at that round, though the ruleset moves the lower-ADP man a round dearer. On Justin's roster Tuten and Shakir are both r11 (Shakir's surplus is 0, so no effect today); a r
- [low] TradeMarket.main carries the same explanatory comment twice, one paragraph now stale — `src/main/java/TradeMarket.java:645`: Two near-identical paragraphs (645-659) describe the keeper-basis fix; the first still says the bug was 'left standing here' although line 660 now calls NextYearKeepers. A reader auditing the sibling sweep is told the terminal board is unfi
- [low] TradeMarket carries two overlapping paragraphs describing a divergence that has been fixed — `src/main/java/TradeMarket.java:645`: The first paragraph states, in the present tense, that this tool is 'left standing' on the old basis; the line below it calls NextYearKeepers.roundsForThisLeague. A reader auditing the sibling sweep (TRAPS #124) takes the comment at its wor
- [low] A 2027 keeper's pick number is read off the 2026 draft order — `src/main/java/TradeMarket.java:996`: draftSlotOfPlayer (967) joins each owner to configuration.getDraftSlots(), the 2026 draft_order, and prices next March's keeper pick there. The 2027 order is not set; a slot-7 r11 keeper priced at pick 127 could be anywhere from 121 to 132,
- [low] The same-round bump is not applied when the 2027 pair is chosen, although KeeperChooser.priceHypothetical promises it is — `src/main/java/TradeMarket.java:1116`: KeeperPricing.resolveSameRoundCosts moves one of two keepers sharing a round up a round (priceHypothetical's javadoc: 'so eligibility and the same-round bump are applied exactly as they would be on the board'); NextYearKeepers prices men on
- [low] TradeStability swallows any keeper-pricing failure and ships an unmarked report — `src/main/java/TradeStability.java:128`: A missing draft_order, a null round on a pick, or NextYearKeepers throwing produces one console line and a report with no [KEEPER] markers at all. TradeStabilityTest.theReportsSpreadsAreItsOwnNumbers reads the report and passes with zero ma
- [low] TradeStability's [KEEPER] marker fires on a bare literal 25, not on 'worth keeping' — `src/main/java/TradeStability.java:243`: The report text says 'Rows that send one are marked'. Brock Purdy - a declared 2026 keeper, keepable at r12 for 2027 with a page surplus of 19.7 - goes out unmarked in 'Brock Purdy + Derrick Henry -> Josh Allen + T...' on trade-stability-20
- [low] TradeStability's keeper threshold is a second copy of TradeMarket.HIS_KEEPER_POINTS — `src/main/java/TradeStability.java:243`: Tune HIS_KEEPER_POINTS to 20 and the console and terminal board start tagging Purdy (19.7-24.9) while the stability report stays silent, with nothing failing - the same shape as the pool 6-vs-8 bug fixed on 2026-09-09. `give.contains(name)`
- [low] KeeperBasisTest's forward-looking list omits KeeperDriftCheck — `src/test/java/KeeperBasisTest.java:27`: KeeperDriftCheck is the fourth tool that prices 2027 keepers (RUNBOOK 129) and today correctly calls NextYearKeepers.roundsForThisLeague, but the test written because 'the same bug landed five times' would not notice it drifting back to Kee
- [low] KeeperBasisTest guards three files but not KeeperDriftCheck, and its cap check scans comments — `src/test/java/KeeperBasisTest.java:27`: KeeperDriftCheck is a live, forward-looking (2027) tool that prices keepers; a regression to KeeperChooser.eligibleCandidates there would pass every test. onlyNextYearKeepersAssemblesTheCap reads the raw file although the class defines code
- [low] KeeperDriftCheckTest tests whatever artifact happens to be newest, and skips when there is none — `src/test/java/KeeperDriftCheckTest.java:27`: Both tests parse data/keeper-drift-<date>.txt rather than calling driftAware, so a sign inversion in the code passes until somebody regenerates the report, and on a machine without the file the suite reports a skip - the '0 skipped matters 
- [low] KeeperDriftCheckTest checks the committed report, skips when it is absent, and cannot see a code regression until the tool is re-run — `src/test/java/KeeperDriftCheckTest.java:27`: Both tests parse data/keeper-drift-<newest date>.txt. Any change to driftAware, keeperPoints or the sign of the drift keeps passing against the 2026-09-07 file until someone runs the tool and commits a new report; on a fresh clone with no d
- [low] KeeperHistorySmokeTest settles historical same-round clashes with today's ADP and sits exactly at its tolerance — `src/test/java/KeeperHistorySmokeTest.java:70`: SleeperProjections.adpOf is the 2026 feed (Double.MAX_VALUE for anyone not in it), so a 2022 clash is settled by 2026 ADP; the comment says the one real clash happens to resolve the same way in both seasons, so the count holds by luck. KNOW
- [low] NextYearKeepersTest's 'edge case' never reaches the cost<1 branch — `src/test/java/NextYearKeepersTest.java:65`: A man kept at r3 escalates to r2; the branch at NextYearKeepers.java:141 (`if(cost < 1)` -> 'would cost better than a first-round pick') needs a kept man at r1 and is exercised by nothing, so the refusal string and keepable()==false there a


## 5. League analyzer
confirmed 11 · refuted 1 · unverified 44

### Confirmed

**[high/small] SeasonOutlook banks head-to-head wins only; the median game added today was never swept into the completed-weeks loop** — `fantasyFootball/src/main/java/SeasonOutlook.java:132`
```
if(week < thisWeek && (pa > 0 || pb > 0)){
    wins.merge(managerOf.getOrDefault(a, "?"), pa > pb ? 1 : 0, Integer::sum);
    wins.merge(managerOf.getOrDefault(b, "?"), pb > pa ? 1 : 0, Integer::sum);
}
...
// lines 180-189, simulated weeks only:
if(medianGame && scored.size() > 1){ ... w.merge(entry.getKey(), entry.getValue() > median ? 1 : 0, Integer::sum); }
```
- scenario: Commit 81e616c (today) made every REMAINING week two results (head-to-head plus league median, league_average_match=1 in the fixture) but left the banked loop at lines 132-135 crediting only pa > pb. The moment Sleeper's state flips to week 2 the real standings read 2-0 / 1-1 / 0-2 while every manager is seeded with 0 or 1; the six who beat the week-1 median get nothing. By the halfway call after week 7 - the point SeasonOutlook.call() fires the sell-for-keepers rule - up to 7 of each team's 14 banked results are dropped while the simulated half plays two a week: a manager at a real 8-6 built on 4 h2h + 4 median wins is seeded at 4, a rival at 8-6 on 7 h2h + 1 median at 7, and Justin's printed WINS and PLAYOFFS % (the 20%/55% thresholds) are read off a record that is not his. The WINS column will also visibly disagree with the Sleeper standings page.
- fix: Inside the per-week byMatchup pass, collect every finished week's scores per manager and award score > median exactly as the sim loop does (gated on medianGame, computed before the loop). Extract the banking into a static method taking the week's (roster,points) rows and unit-test it with twelve known scores: banked wins must equal h2h wins plus median wins.

**[high/small] LeagueTransactions caches the CURRENT league's meta forever with status pre_draft, so 2026 can never become a completed season for TradePartners and every LeagueTransactions consumer** — `src/main/java/LeagueTransactions.java:61`
```
static String leagueRaw(String leagueID){
    return InOutUtilities.getCachedForever(
            "https://api.sleeper.app/v1/league/" + leagueID,
            "sleeperLeagueMeta" + leagueID);
}
...
public boolean complete(){ return "complete".equals(status); }
```
- scenario: chain() starts at the live league id and reads it through the same forever cache as finished seasons. The file sleeperLeagueMeta1390416723210952704.txt on disk reads `season 2026, status pre_draft` and will read that in January 2027. completedSeasons() filters on that frozen status, so next year TradePartners.records, FaabBid's harvest, WireSurvival and everything else built on LeagueTransactions.everySeason will silently report '5 finished seasons' and omit all of 2026's trades and claims until somebody knows to delete the file. PlayoffOdds.history has the same shape with leagueChain<id>/rostersChain<id>. Pattern 8 from the brief: a forever cache on a field (status) that moves.
- fix: Read the head of the chain (the configured league) through getTodaysWebPage / configuration.getLeagueJson() and only fall to getCachedForever once the league object says status == complete; or key the forever file by season+status. Add a test that chain() never serves a non-complete league from a dateless cache.
- siblings: src/main/java/PlayoffOdds.java, src/main/java/TradePartners.java

**[high/small] The 24.9 weekly spread is a typed literal, no repo tool measures it, and it is the pooled SD across teams rather than the within-team noise the simulation applies it as** — `src/main/java/SeasonOutlook.java:56`
```
/** ... Read back from the report that measures it rather than typed - see #115. */
static double measuredSpread(){
    return Double.parseDouble(System.getProperty("teamSpread", "24.9"));
}
```
- scenario: grep finds no tool, test or data file in the repo that produces 24.9 or '840 team-weeks' (TRAPS #115 is about a hardcoded 480 in a different test), so the javadoc's 'read back from the report that measures it' is false - the number was measured in a scratch script and typed, against the 'numbers from repo code' rule. Recomputing it from the 70 cached sleeperMatchups files (5 seasons x 14 weeks x 12 teams = 840 team-weeks): pooled SD = 24.95, but the SD of each team-season's weekly mean is 10.16 and the within-team residual SD (df-corrected) is 23.67. The sim draws each team's week as `mean + N(0, 24.9)` (line 171) where `mean` already carries the between-team differences, so between-team variance is counted twice. It is a 5% overstatement of noise that pushes every odds toward 50%, and more importantly nothing would notice if the literal were 34.9.
- fix: Add a committed tool (e.g. `TeamWeekSpread`) that walks LineupPromotion.matchupsRaw over completedSeasons, prints pooled SD, within-team residual SD and the count, and writes data/team-week-spread-<date>.txt; have measuredSpread() default to the within-team figure parsed from that report (the #115 pattern), and a test that fails if the report's count is not the count the tool computes.
- siblings: RUNBOOK.md

**[high/trivial] SeasonOutlook joins roster_id to manager through a forever cache of the LIVE league's users and rosters while its means come from the 20-minute path** — `src/main/java/SeasonOutlook.java:99`
```
Map<Integer, String> managerOf = TradePartners.managersOf(leagueID);
...
double sa = mean.getOrDefault(a, 100.0) + random.nextGaussian() * spread;   // line 171
```
- scenario: TradePartners.managersOf (lines 63-91) is documented 'for ONE league year' and reads both /users and /rosters via getCachedForever; called with the current league it froze sleeperUsers1390416723210952704.txt and sleeperRosters1390416723210952704.txt at 2026-09-07 07:35, forever. `mean` and `wins` are keyed by display names from LeagueOwners.today (20-minute rosters, day users). Verified identical today, but the first display-name edit or owner swap in the league splits the join silently: the renamed team's weekly mean becomes the 100.0 default at line 171, its wins accrue under a name absent from mean.keySet(), so it can never qualify - and the 600% test still passes (see next finding). SeasonLedger.managerByRoster (SeasonLedger.java:254-270) is the same join on the fresh path, one class away.
- fix: Replace the call with SeasonLedger.managerByRoster(configuration) (or move that method to LeagueOwners and have both use it) so the join and the means read the same rosters; assert in main that managerOf.values() equals mean.keySet() and fail loudly otherwise.
- siblings: src/main/java/TradePartners.java:63, src/main/java/SeasonLedger.java:254

**[high/small] SeasonOutlook banks only the head-to-head result for played weeks; the median game the 81e616c fix added is simulated for future weeks only** — `src/main/java/SeasonOutlook.java:132`
```
if(week < thisWeek && (pa > 0 || pb > 0)){
    wins.merge(managerOf.getOrDefault(a, "?"), pa > pb ? 1 : 0, Integer::sum);
    wins.merge(managerOf.getOrDefault(b, "?"), pb > pa ? 1 : 0, Integer::sum);
}
...
if(medianGame && scored.size() > 1){   // line 180 - simulated weeks only
    ...
    w.merge(entry.getKey(), entry.getValue() > median ? 1 : 0, Integer::sum);
```
- scenario: league_average_match is 1 in the live league JSON (verified in sleeperLeagueOutlook…2026-09-08.txt). Commit 81e616c made every SIMULATED week two results (H2H + median) but left the banked loop at one. Run after week 7 - the week the RUNBOOK says the buy/sell rule fires - each team's banked total is missing up to 7 median results while its remaining 7 weeks get 14, so a team that has been beating the median all half-season is under-credited by up to a quarter of its 28 results and the WINS and PLAYOFFS% columns and the call() verdict are computed on a win total Sleeper's own standings will not show. The same loop discards the actual points: `season` (line 164) is reset per sim and only receives simulated scores (175-176), so the 'points scored breaks the tie' sort at 193-194 ignores every real point banked so far.
- fix: In the played-week branch collect every roster's `points` for the week (all 12 rows are in byMatchup), compute the median exactly as the sim does and merge a second result per team; seed a `bankedPoints` map with the actual points and initialise `season` from it in each sim. Add a unit test on a synthetic two-week schedule: after one played week under the median rule the banked wins must sum to 12 (6 H2H + 6 median), not 6.
- siblings: src/main/java/SeasonOutlook.java:164, src/main/java/SeasonOutlook.java:193

**[high/small] SeasonOutlook banks only head-to-head results from finished weeks; the median game it simulates for future weeks is never credited for played weeks** — `src/main/java/SeasonOutlook.java:132`
```
if(week < thisWeek && (pa > 0 || pb > 0)){
    wins.merge(managerOf.getOrDefault(a, "?"), pa > pb ? 1 : 0, Integer::sum);
    wins.merge(managerOf.getOrDefault(b, "?"), pb > pa ? 1 : 0, Integer::sum);
}
...
if(medianGame && scored.size() > 1){   // line 180 - simulated weeks only
    ...
    w.merge(entry.getKey(), entry.getValue() > median ? 1 : 0, Integer::sum);
```
- scenario: Commit 81e616c ('fourteen games in a fourteen-week season that has twenty-eight') added the league_average_match result to the simulation loop (lines 180-189) and not to the banked loop (lines 132-135). Sleeper's own standings for this league count the median game: the cached 2025 rosters show every team at 28 games in 14 weeks (itsabust 25-3, tommyrads 2-26). From week 2 on, every played week enters the sim with one result per team instead of two. At week 7 - the halfway point Justin's rule fires on - a team that went 3-4 head-to-head but beat the median all seven weeks enters the sim at 3 wins where Sleeper has it at 10; a 4-3 team that lost to the median every week enters at 4 where Sleeper has 4. The banked half of the season is systematically mis-ranked and the simulated half doubles the noise it is measured against, so the 55%/20% buy/sell verdict fires on the wrong number. The sum-to-600% test cannot see this (it holds whatever the banked wins are). Same omission on the tiebreak: `season` (line 194) accumulates only simulated points, so banked points-for never break ties.
- fix: In the banked loop, collect every completed week's twelve `points` values (they are all in the same matchups response), compute the median exactly as lines 181-185 do, and `wins.merge(manager, points > median ? 1 : 0)`; also add banked points into a `bankedPoints` map that seeds `season` each sim. Alternatively read `settings.wins`/`fpts` off the rosters feed, which already includes the median results. Add a test that feeds one completed week with league_average_match=1 and asserts 24 results were banked, not 12.
- siblings: src/main/java/SeasonOutlook.java

**[medium/small] The 24.9 spread is a typed literal with no measuring tool, and it is the POOLED SD (between-team spread included), not the within-team noise the simulation draws** — `src/main/java/SeasonOutlook.java:56`
```
static double measuredSpread(){
    return Double.parseDouble(System.getProperty("teamSpread", "24.9"));
}
```
- scenario: Docstring: 'Read back from the report that measures it rather than typed - see #115' - but grep over src/, TRAPS.md, MODEL.md, DIAGNOSTIC.md finds 24.9 / '840 team-weeks' only in SeasonOutlook and RUNBOOK:112; no committed code computes it ('Numbers from repo code'). Recomputing from the cached sleeperMatchups<id>w1..14 files of the five completed seasons: pooled SD of all 840 team-week points = 24.95 (so that is what was typed), but the SD of each team-week around its own team-season mean = 23.7 (60 team-season means removed). The simulation already separates teams by `mean` and then adds a Gaussian of 24.9 on top, so between-team variance is counted twice: every probability is pushed toward 50% by roughly 5% too much noise, in a tool whose only output is a probability that fires a sell rule. The report prose at line 219 also hard-codes '840 real team-weeks' while `spread` is a property.
- fix: Add a `TeamSpread` main (or static method on SeasonOutlook with a test) that reads the completed seasons' matchup feeds through LineupPromotion.week, subtracts each team-season mean and prints the residual SD with its n; make SeasonOutlook read that number back (or a test assert the default equals it to 0.1) and print n and the population in the report instead of literals.
- siblings: RUNBOOK.md:112, src/main/java/SeasonOutlook.java:219

**[medium/small] SeasonOutlook joins matchups to rosters through a forever-cached users/rosters copy of the current league while the roster means come from today's copy; a display-name change silently gives a team a 100.0 default and zero banked wins** — `src/main/java/SeasonOutlook.java:99`
```
Map<Integer, String> managerOf = TradePartners.managersOf(leagueID);   // getCachedForever("sleeperUsers"+id), getCachedForever("sleeperRosters"+id)
...
Map<String, String> ownerOf = LeagueOwners.today(configuration);        // today's users + 20-minute rosters
...
double sa = mean.getOrDefault(a, 100.0) + random.nextGaussian() * spread;   // line 171
```
- scenario: TradePartners.managersOf (lines 65-67, 77-79) reads the CURRENT league's /users and /rosters through getCachedForever - files sleeperUsers1390416723210952704.txt and sleeperRosters1390416723210952704.txt written 2026-09-07 07:35 - while `mean` is keyed by LeagueOwners.today display names. The two are identical today (checked), so the join works by coincidence of cache age. If any manager edits his Sleeper display name mid-season, his banked wins accrue under the old name (line 133), the sim draws his score from the 100.0 default (line 171) under the old name, and under today's name he has 0 wins and 0 points and finishes last in every sim; the other eleven share six spots so the odds still sum to 600% and the test passes. The same forever copy feeds LeagueConsole's 'He trades' column via TradePartners.records - a renamed rival reads `tradesPerYear.getOrDefault(..., 0.0)` (LeagueConsole:808), i.e. 'will not answer your offer'. SeasonLedger's anchor TEN lines are keyed by name too, so a rename makes 100% of his points 'from bench'.
- fix: Give managersOf an overload that takes the rosters/users JSON, and have SeasonOutlook (and SeasonLedger.managerByRoster already does this) build roster_id -> name from configuration.getTodaysRosterWebPageSerious() and getUserIDToDisplayName(); keep the forever cache only for completed seasons. Replace `mean.getOrDefault(a, 100.0)` with a throw or a printed warning: an unknown manager in a matchup is a join failure, not a 100-point team.
- siblings: src/main/java/TradePartners.java, src/main/java/LeagueConsole.java, src/main/java/SeasonLedger.java

**[medium/small] TradePartners counts 50 of the league's 51 completed trades: a trade with empty adds/drops has no participants because Move drops roster_ids** — `src/main/java/TradePartners.java:94`
```
static Set<Integer> participants(LeagueTransactions.Move move){
    Set<Integer> rosters = new TreeSet<>();
    if(move.adds() != null){
        rosters.addAll(move.adds().values());
    }
    if(move.drops() != null){
        rosters.addAll(move.drops().values());
    }
    return rosters;
}
```
- scenario: The cached 2025 log (sleeperTxns1249220546315423744w11.txt) has a status=complete trade with adds {} drops {} roster_ids [4, 11] and a FAAB leg each way; rosters 4 and 11 in 2025 are itsabust and justinb314. LeagueTransactions.Move (lines 53-55) carries adds/drops/bid but not roster_ids, so participants() is empty and the deal vanishes. data/trade-partners-2026-09-07.txt prints '100 participations across 50 completed trades' against the 18+13+5+9+6 = 51 in the log (the class docstring itself says 'Fifty-one trades'); itsabust reads 15 trades / 3.00 per year instead of 16 / 3.20 and justinb314 8 / 1.60 instead of 9 / 1.80. LeagueConsole.java:585 builds the 'He trades' column - the column the RUNBOOK says outranks everything on the trades tab - from the same records(). No verdict flips today (KevinDA and BHier stay the market) but the number Justin reads is wrong.
- fix: Add `List<Integer> rosterIDs` to LeagueTransactions.Move, parsed from the row's roster_ids array, and union it into participants(); add a test with a picks-only/FAAB-only trade whose adds and drops are empty and roster_ids [4,11] asserting Set.of(4,11). Regenerate the report and assert its deal count equals the count of complete trades in the log.
- siblings: src/main/java/LeagueTransactions.java:53, src/main/java/LeagueConsole.java:585

**[medium/trivial] TradePartners.participants reads adds/drops instead of Sleeper's roster_ids, so a FAAB-only trade is credited to nobody: the log has 51 completed trades, the report says 50** — `src/main/java/TradePartners.java:94`
```
static Set<Integer> participants(LeagueTransactions.Move move){
    Set<Integer> rosters = new TreeSet<>();
    if(move.adds() != null){
        rosters.addAll(move.adds().values());
    }
    if(move.drops() != null){
        rosters.addAll(move.drops().values());
    }
    return rosters;
}
```
- scenario: Checked every completed trade in the cached sleeperTxns files for the five seasons: 51 trades, and exactly one has `adds: null, drops: null` with `roster_ids: [4, 11]` and a `waiver_budget` swap - 2025 week 11, itsabust (roster 4) with justinb314 (roster 11). participants() returns the empty set for it, so it counts for neither man and the pair. The report Justin reads (data/trade-partners-2026-09-07.txt) shows itsabust 15 trades / 3.00 per year and justinb314 8 / 1.60 where the log says 16 / 3.20 and 9 / 1.80, and '100 participations across 50 completed trades' where there are 51. This is the 'He trades' column the RUNBOOK says 'outranks everything else on the page'. A trade where one side gives only picks or FAAB would be credited to one manager only, and `totalTrades / 2` (line 182) would then floor an odd count. The Move record does not carry roster_ids at all, and TradePartnersTest asserts the adds/drops behaviour rather than the feed field.
- fix: Add `List<Integer> rosterIDs` to LeagueTransactions.Move (from the row's `roster_ids` array) and make participants() return it when present, falling back to adds/drops only when absent; add a test with a FAAB-only trade (adds null, drops null, roster_ids [4,11]) asserting both count. Regenerate the report.
- siblings: src/main/java/LeagueTransactions.java, src/test/java/TradePartnersTest.java

**[medium/small] SeasonOutlookTest's 600% check cannot fail on the failures it names, and it tests a committed artifact rather than the code** — `src/test/java/SeasonOutlookTest.java:24`
```
public void theOddsAcrossTheLeagueSumToTheNumberOfPlayoffSpots() throws Exception {
    ...
    assertEquals(600.0, total, 1.0,
            "playoff odds across the league sum to " + total + "%, but six spots means"
                    + " they must sum to 600% - the simulation has lost or double-counted"
                    + " somebody");
```
- scenario: In SeasonOutlook.main, `order` is built from mean.keySet() (line 191) and madeIt is incremented for exactly min(playoffTeams, 12) of them every sim (195-197), so the column sums to 600 by construction: a team lost to a name mismatch just sits at 0 wins and never qualifies, and the missing banked median wins (finding 1) change every row without changing the sum. The test also reads the newest data/season-outlook-*.txt, so a regression in the code cannot fail it until the tool is rerun by hand - both bugs above are live with this test green.
- fix: Extract the simulation into a static method taking (managerOf, rosters/means, schedule rows, medianGame, spread, sims) and test it on synthetic input: a manager present in managerOf but absent from means must throw; after one played week with the median rule the banked wins must sum to 12; and keep the artifact check only as a smoke test on the file's data stamp.
- siblings: src/test/java/TradePartnersTest.java:78

### Refuted
- **TeamRankings ranks the DRAFT (picks by draft_slot), while SeasonOutlook's 'A WEEK' ranks today's rosters - two 'every roster's best legal lineup' artifacts that diverge on the first waiver** — `src/main/java/TeamRankings.java:175`
  - refuted: Code and line numbers are accurate (line 175/192/207 quotes match verbatim), and TeamRankings genuinely builds rosters only from the draft-picks feed keyed by draft_slot, never touching live Sleeper rosters. But the finding's core claim — that this is a silent divergence between two tools meant to compute 'the same thing' — is refuted by the tool's own docstring and by DIAGNOSTIC.md. TeamRankings' docstring explicitly scopes it to 'Every roster in the league's draft, scored and ranked' (report-only, feeds no model), and DIAGNOSTIC.md's only two usages of it both treat it as a frozen draft-day snapshot: run once right after the 2026-09-01 draft to record projected-starters rankings, explicitly intended to be diffed later against real December standings to test whether the bench-heavy draft strategy paid off ('LeagueActuals and TeamRankings against December standings is the measurement'). That is a different job from SeasonOutlook's live weekly scoring, by design, not an accidental sibling that fell out of sync. There's no evidence in the runbook or docs that Justin runs TeamRankings expecting current post-waiver rosters, so no decision-facing number actually diverges from what the tool promises to compute. The 'justinb314' hardcoded default for -Pme (line 207) is real but is an overridable convenience default in an occasionally-run, report-only tool — not a wrong number reaching a decision.

### Unverified (44; found by a lens, not yet refuted — verify before acting)
- [medium] The test task declares the markdown files as inputs but not data/, so every artifact-reading test can be UP-TO-DATE after the weekly regeneration (TRAPS #45, half-swept) — `build.gradle:64`: SeasonOutlookTest, TradePartnersTest, ConsoleRendersTest, LeagueConsoleTest, KeeperDriftCheckTest, TradeStabilityTest, WireSurvivalTest and ProjectionDriftTest all read the newest file under data/ (grep 'Path.of("data"' in src/test). None o
- [medium] RUNBOOK says to read the test count and '0 skipped' from check-wire.log, but the unit test task has no testLogging and the log carries no per-test lines — `fantasyFootball/RUNBOOK.md:153`: build.gradle's `test {` block (line 28) configures inputs and forkEvery but no testLogging; the `testLogging { events 'passed', 'skipped', 'failed' }` block at lines 102-104 belongs to `smokeTest` (line 35). The check-wire.log on disk from 
- [medium] Console 'He trades' silently reads 0.00/yr for any manager absent from TradePartners.records, and the join is by per-season display name — `fantasyFootball/src/main/java/LeagueConsole.java:808`: TradePartners.records (lines 112-152) keys managers by the display name in each completed season's users feed (managersOf, forever-cached) and never sees the in-season league (completedSeasons filters status == complete). A rival who rename
- [medium] The 24.9 spread is described as MEASURED over 840 team-weeks, but nothing in the repo measures it — `fantasyFootball/src/main/java/SeasonOutlook.java:56`: The comment says the value is read back from a measuring report; it is a typed literal. grep of src/ and data/ finds no class that computes a standard deviation of weekly team scores over the five completed seasons' matchups and no data/*.t
- [medium] Manager names joined across two caches of the same users endpoint under different policies, with a silent 100.0 default; the sum-to-six test cannot see it — `fantasyFootball/src/main/java/SeasonOutlook.java:99`: `mean` is keyed by display names from the day-cached leagueUsers file; `managerOf` by display names from sleeperUsers<id>.txt, fetched once and kept forever (TradePartners.java:65-67, 77-79), for the LIVE league id. If a rival renames on Sl
- [medium] Playoff tie-break uses only simulated points; banked points from finished weeks are never seeded — `fantasyFootball/src/main/java/SeasonOutlook.java:164`: The banked loop (lines 128-135) reads pa/pb but never stores them; `season` starts empty every sim, so the 'points scored' tie-break is the remaining weeks' random draws only. With twelve teams on a 28-game record, ties on wins are the comm
- [medium] Ternary precedence drops the spread sentence and the 24.9 from the outlook header whenever the median game is on — `fantasyFootball/src/main/java/SeasonOutlook.java:214`: `+` binds tighter than `?:`, so the true branch is only the first three literals and the 'drawn around ... spread of %.1f - MEASURED over 840 real team-weeks' text belongs to the false branch alone. This league runs the median game, so data
- [medium] TeamRankings ranks the draft picks, not the rosters; README calls it 'every roster' and it is superseded in season — `fantasyFootball/src/main/java/TeamRankings.java:175`: README.md:44 sells it as 'every roster's best legal lineup scored and ranked' with no season caveat. The roster is rebuilt from draft picks by draft_slot, so Justin's 2026-09-07 waiver claim (InOutUtilities.java:109-112: dropped man gone, n
- [medium] SeasonOutlookTest and TradePartnersTest verify the committed artifact, not the code, and skip by design when it is absent — `fantasyFootball/src/test/java/SeasonOutlookTest.java:24`: The one test that 'exercises the whole loop end to end' reads data/season-outlook-2026-09-08.txt. The banked-median bug above changes every number in that table, yet the suite stays green until somebody reruns the tool and commits the new f
- [medium] LeagueTransactions caches the current league's metadata forever with status 'pre_draft', so the 2026 season will never count as completed for TradePartners and the console's He-trades column — `src/main/java/LeagueTransactions.java:62`: sleeperLeagueMeta1390416723210952704.txt (written 2026-08-31) carries "status": "pre_draft" and is never refetched; `chain()` reads `status` from it and `completedSeasons()` keeps only `complete`. When 2026 finishes and 2027's league points
- [medium] SeasonLedger's row arithmetic and verdict parsing - the numbers the pre-registered bench test is read from - have no test; a manager missing from the anchor silently reads as 100% promoted — `src/main/java/SeasonLedger.java:191`: SeasonLedgerTest covers weeksRecorded, rowsFor, rankOf and verdict() on hand-typed inputs only. Nothing exercises the week loop (lines 176-209) that writes scored/best_possible/from_bench/promoted, nor judge()'s `Integer.parseInt(line.split
- [medium] The 24.9 weekly spread that every playoff probability scales with is a typed literal; no tool in the repo measures it and the comment cites a read-back that does not exist — `src/main/java/SeasonOutlook.java:56`: grep for `teamSpread`, `840` or `team-weeks` across src/main, src/test, TRAPS and MODEL finds only this file and RUNBOOK.md:112. Commit 6357878, which introduced the number, added SeasonOutlook.java, its test, the artifact, RUNBOOK and buil
- [medium] SeasonOutlook joins matchups to managers through a users file cached FOREVER while its means are keyed by today's users; a rename silently zeroes a team and gives its opponents a phantom — `src/main/java/SeasonOutlook.java:169`: `managerOf` comes from TradePartners.managersOf(leagueID) (line 99), which reads `InOutUtilities.getCachedForever(".../users", "sleeperUsers" + leagueID)` and `getCachedForever(".../rosters", "sleeperRosters" + leagueID)` (TradePartners.jav
- [medium] LeagueWeekTest asserts two string literals typed in the test against each other, not the cache names LeagueWeek actually builds — `src/test/java/LeagueWeekTest.java:31`: The names LeagueWeek uses are built at LeagueWeek.java:74-75 (`"sleeperWeekProjection" + season + "w" + week`, `"sleeperLiveProjection" + season + "w" + week`). If either is edited - say the live name is shortened to "sleeperWeekProjectionL
- [medium] SeasonOutlookTest's only end-to-end check is a tautology: exactly six of mean.keySet() are credited every sim, so the odds sum to 600 whatever the games did — `src/test/java/SeasonOutlookTest.java:44`: In SeasonOutlook.java:191-197 `order = new ArrayList<>(mean.keySet())` is sorted and `for(int i = 0; i < Math.min(playoffTeams, order.size()); i++) madeIt.merge(order.get(i), 1, ...)` - six of the twelve names are credited per sim by constr
- [low] RUNBOOK says `check` is 654 tests; the last commit reports 692 — `RUNBOOK.md:133`: The same paragraph tells Justin to read the test count out of check-wire.log and treat '0 skipped' as load-bearing; the number he is told to expect has been stale since at least 548832e ('683 tests') and 18fd956 ('692 tests, none skipped').
- [low] The weekly loop never runs the live-API checks for this component: smoke-tagged tests are excluded from `check` and only recommended before the draft — `build.gradle:37`: LeagueSmokeTest.everyRosterIsAccountedFor, everyRosteredPlayerHasAProjection and theConfiguredLeagueIsThisSeason, and ManagerProfilesSmokeTest's pinned biases, are the only checks that look at the live league at all; `check` runs with fixtu
- [low] README's cache paragraph describes one day-cache policy; the code now has three and only one matches the prose — `fantasyFootball/README.md:92`: Rosters expire after rosterFreshnessMinutes (InOutUtilities.getRecentWebPage, lines 120-134); getCachedForever files (sleeperUsers<id>, sleeperRosters<id>, leagueChain<id>, rostersChain<id>, sleeperMatchups<id>w<n>) have no date suffix (lin
- [low] RUNBOOK quotes 654 tests; the suite has grown through 656, 660, 662, 678, 683 and 692 since — `fantasyFootball/RUNBOOK.md:133`: git blame puts the line at 0a93dba (2026-09-07); the commit messages since read '656 tests', '660', '662', '678', '683' and '692 tests, none skipped' (18fd956). A reader checking the log against the RUNBOOK's number would think 38 tests had
- [low] build.gradle says league-scored actuals are off by default; LeagueActuals.enabled() has defaulted to true since 2026-09-04 — `fantasyFootball/build.gradle:226`: LeagueActuals.java:61-63 returns !"false".equalsIgnoreCase(System.getProperty(FLAG, "true")) with a doc block 'ON since 2026-09-04, by Justin's decision'. A reader of build.gradle would pass -PleagueScoredActuals=true believing it changes s
- [low] DraftExpectation and OwnerLadder label their ACTUAL/DRAFTED rung 'the roster the manager holds today' but read draft picks — `fantasyFootball/src/main/java/DraftExpectation.java:279`: Both are report-only and their committed outputs are dated 2026-09-02/03, when the sentence was true. Rerun now (README.md:45-46 still lists both without a season caveat) and the 'today' roster is the August draft, while today's projections
- [low] README lists LeagueOutlook first under Running; in season Sleeper has emptied the keepers field so every seat comes back keeperless and starred — `fantasyFootball/src/main/java/LeagueOutlook.java:102`: InOutUtilities.java:97-100 records that on 2026-09-02 'Sleeper had emptied every roster's keepers field (two per roster the day before, zero after)'. README.md:31 makes this the first command under Running with no note that it is a pre-draf
- [low] LeagueRules and ManagerFeedTable are stray; the lineup shape TeamRankings hard-codes is never checked against roster_positions — `fantasyFootball/src/main/java/LeagueRules.java:16`: LeagueRules is cited only by a comment in RosterRules.java:27 and by no doc; its purpose (refuting a +11 claim) is finished and its printed claims about ShapeSensitivity are prose in a main, not a test. ManagerFeedTable has zero references 
- [low] PlayoffOdds walks the chain from the live league with forever caches and no status filter; only RiskDial reads it — `fantasyFootball/src/main/java/PlayoffOdds.java:75`: history() starts at configuration.getLeagueJson().league_id (the 2026 league) and never checks status. It is excluded today only because rostersChain1390416723210952704.txt was written pre-season with every fpts 0 and leagueChain...txt froz
- [low] SeasonOutlook.Team.rosterID is always 0 and never read — `fantasyFootball/src/main/java/SeasonOutlook.java:205`: No caller reads rosterID() (grep of src/main and src/test); the constant 0 suggests a join by roster id that was planned and abandoned in favour of display names - which is the join that the silent-default finding above shows is the weak on
- [low] Trade counts disagree across prose (51 vs 50), the deals figure assumes two-party trades, and the 'eleven moved four players' claim has no producer — `fantasyFootball/src/main/java/TradePartners.java:181`: The javadoc says fifty-one; data/trade-partners-2026-09-07.txt prints '100 participations across 50 completed trades'; TRAPS #109 says fifty; TradeMarket.java:411-412 justifies DEFAULT_POOL = 8 with 'eleven of this league's 51 completed tra
- [low] DraftExpectation says both sides use today's projections, but with the pre-draft fixture forced on it reads the fixture's draft-night feed — `src/main/java/DraftExpectation.java:109`: data/fixtures/2026-pre-draft contains sleeperProjections2026.txt, and InOutUtilities.getTodaysWebPage serves a fixture file whenever one exists for the name, so `points` is the draft-night feed for both EXPECTED and ACTUAL. The comparison i
- [low] DraftExpectation and OwnerLadder call the draft-picks feed 'the roster the manager holds today' — `src/main/java/DraftExpectation.java:279`: Both tools' AFTER/DRAFTED rung is built from /draft/{id}/picks, which is fixed at 192 picks; after the 2026-09-07 waiver the roster Justin 'holds today' differs from it (roster 11: +5001, -12509). The DIFF column is correctly 'his own picks
- [low] Stale-copy fallback matches by filename prefix, so week 1's cache name is a prefix of weeks 10-14 and a failed fetch of week 1 is served week 14's matchups — `src/main/java/InOutUtilities.java:202`: SeasonOutlook names its per-week caches "sleeperOutlook" + leagueID + "w" + week + date (line 117). On disk: sleeperOutlook...w12026-09-08.txt (week 1) and sleeperOutlook...w142026-09-08.txt (week 14). If Sleeper is unreachable when week 1 
- [low] LeagueOutlook reads keepers live (no fixture pin) while its siblings DraftExpectation/OwnerLadder pin the pre-draft fixture - run today it values every seat keeperless — `src/main/java/LeagueOutlook.java:102`: InOutUtilities.java:97-100 records that Sleeper emptied every roster's keepers field the morning after the draft; DraftExpectation (line 109-111) and OwnerLadder (line 194-196) therefore set fixtureDir to data/fixtures/2026-pre-draft. Leagu
- [low] The current league's status and rosters are read through forever caches in LeagueTransactions.chain and PlayoffOdds.history, so 2026 can never become a completed season without a manual delete — `src/main/java/LeagueTransactions.java:61`: sleeperLeagueMeta1390416723210952704.txt (2026-08-31) says status pre_draft while the live JSON already says in_season. chain() (90-101) and completedSeasons() read status from that file, so TradePartners, LineupPromotion and everything on 
- [low] Actuals and matchups are frozen forever the instant Sleeper's state week rolls, so stat corrections issued later in the week never reach the append-only ledger — `src/main/java/LeagueWeek.java:51`: SeasonLedger runs 'after the games' and writes rows it never rewrites. The first run after Sleeper advances `week` fetches that week's stats and matchups through getCachedForever; the NFL's Wednesday/Thursday stat corrections then change Sl
- [low] LineupPromotion's docstring says the matchup feed's players_points pays 4 per passing TD; it is league-scored, and SeasonLedger re-scores what the feed already carries — `src/main/java/LineupPromotion.java:54`: On the cached 2025 week-1 matchups, QB 6797 (3 pass TD) has players_points 33.92 = the repo's league re-scoring exactly, while the stats feed's pts_half_ppr is 27.92 (4/TD); all 12 rosters' `points` equal the sum of starters_points and equa
- [low] OwnerLadder defaults its ledger input to a dated artifact literal — `src/main/java/OwnerLadder.java:203`: The 'BEST PAIR' rung is searched over candidates parsed from a 2026-08-26 file; any later ledger run (KeeperLedger writes dated files) is ignored unless -Pledger is passed, and the docstring at lines 34/44 repeats the date twice more.
- [low] Three roster_id/owner_id to display-name joins with three cache policies and three treatments of an ownerless roster — `src/main/java/SeasonLedger.java:254`: SeasonLedger.managerByRoster skips an ownerless roster (20-min rosters, day users); TradePartners.managersOf names it 'roster N' (forever, forever); LeagueOwners.byPlayer drops its players entirely (20-min, day); TeamRankings.java:162-167 a
- [low] A banked head-to-head tie scores 0 for both teams where Sleeper records a tie; ties do occur in this league — `src/main/java/SeasonOutlook.java:133`: Sleeper keeps `ties` as its own column (2025: BHier 7-20-1, Hamrliks 11-16-1) and orders standings by wins then points; one of the 420 cached head-to-heads (2021-25) is an exact tie. Here both teams bank a loss, and `w` is an integer map so
- [low] SeasonOutlook's median-game prose branch drops the spread sentence: the ternary binds the concatenation to the else-branch only — `src/main/java/SeasonOutlook.java:214`: data/season-outlook-2026-09-08.txt reads 'Each team's weekly score is' followed directly by the MANAGER header: the one parameter every probability depends on (the spread) is no longer printed on the report Justin reads, and the 'Six of twe
- [low] The spread sentence is dropped from the printed outlook for this league: the ternary binds only the first string, so the median branch ends mid-sentence and never prints the 24.9 — `src/main/java/SeasonOutlook.java:214`: `+` binds tighter than `?:`, so the 'drawn around its best legal ten with a spread of %.1f ...' continuation belongs to the false branch only. This league has league_average_match=1, so data/season-outlook-2026-09-08.txt reads 'Each team's 
- [low] SeasonOutlook's report header drops the spread sentence on the real league: `+` binds before `? :`, so with a median game the format string ends at 'Each team's weekly score is' — `src/main/java/SeasonOutlook.java:214`: league_average_match is 1 for this league (sleeperLeagueOutlook…2026-09-08.txt), so the true branch is taken and the 'drawn around ... spread of %.1f - MEASURED' clause belongs only to the false branch; `spread` is passed and ignored. data/
- [low] TeamRankings claims its lineup is 'exactly as BoardValue.lineup' but the two score an empty slot differently — `src/main/java/TeamRankings.java:25`: BoardValue.lineup (BoardValue.java:1130-1153) fills an unfilled slot at replacement level ('An unfilled slot is NOT zero - the league still fields somebody there, off the wire'); TeamRankings.bestLineup (lines 83-85, 101-103) scores it 0 an
- [low] TeamRankings ranks the rosters as DRAFTED, not as held: in season it reads the draft picks feed, so waivers and trades never move the ranking — `src/main/java/TeamRankings.java:175`: The class comment and the README line ('every roster's best legal lineup scored and ranked') describe a roster ranking, and DIAGNOSTIC item 8 plans to set 'TeamRankings against December standings' as the bench measurement. But the roster is
- [low] TeamRankings ranks the DRAFT's rosters from the picks feed, not today's rosters, while DIAGNOSTIC tells Justin to run it against December standings — `src/main/java/TeamRankings.java:175`: Every roster the page ranks is `rosters.computeIfAbsent(slot, ...)` filled from draft picks (lines 182-197), so after the 2026-09-07 waiver claim and any trade the 'live' ranking is a picture of 2026-09-01. README.md:44 lists it without an 
- [low] EmptyPayloadPolicyTest guards the live-week cache policy by substring-matching the source, which a comment quoting the old call would satisfy — `src/test/java/EmptyPayloadPolicyTest.java:22`: Today the strings occur only at LeagueWeek.java:58-59. A future edit that swaps the live week onto getCachedForever and leaves a comment such as '// was getTodaysWebPage(url, liveName)' keeps both assertions green (pattern 6: a source test 
- [low] TradePartnersTest's artifact check verifies only that PER YEAR equals TRADES/SEASONS - the record's own arithmetic - and skips if no report exists — `src/test/java/TradePartnersTest.java:94`: `Record.rate()` is `trades / seasons` and main prints `record.rate()`, so the assertion restates the printf. It cannot catch the counting that produces the He-trades column: a season credited to a placeholder 'roster N' (managersOf line 87)


## 6. Trade finder
confirmed 12 · refuted 0 · unverified 52

### Confirmed

**[high/small] TradeMarket.main still prices the rival's keeper loss position-blind: the two-arg lossAverseOnKeepers overload survived the fix that claimed to delete it** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);

// and the overload it reaches, lines 375-378:
static Side lossAverseOnKeepers(java.util.function.ToDoubleFunction<List<String>> season,
                                Map<String, Double> surplus){
    return lossAverseOnKeepers(season, surplus, Map.of());
}

// which makes keeperValue's one-starter guard (line 1134) never fire:
if(first != null && first == second && ONE_STARTER.contains(first)){
```
- scenario: Commit ac62628 said "The overload is deleted rather than fixed, so nothing can reach it again" - but only keeperValue's two-arg overload was deleted; lossAverseOnKeepers kept one, main() calls it, and TradeMarketTest.java:201 pins it (`TradeMarket.lossAverseOnKeepers(flatSeason, surplus)`). With positionOf empty, `first` is null for every man, so a rival's keeper value is the two biggest surpluses regardless of position - exactly the Purdy+Nix pairing the console stopped doing at LeagueConsole.java:537-539 (`TradeMarket.lossAverseOnKeepers(seasonOnly, surplus, everyPosition)`). On the 2026-09-08 feeds (approximate league scoring, pts_half_ppr + 2 per pass TD) tommyrads holds Jared Goff r16 (~28.5), Caleb Williams r13 (~22.8) and Tyler Warren TE r7 (~21.3): the terminal board values his keepers as Goff+Caleb = 51.3 (two quarterbacks), the page as Goff+Warren = 49.8. Every `him` column against tommyrads therefore differs between the two tools - asking for Goff is charged 7.2 on the terminal and 5.7 on the page, asking for Caleb 1.5 versus 0 - and the set of trades that pass `mutual()` and feed the TRADING POWER chain differs with it. Any rival who acquires a second keepable QB/TE/DEF in-season widens the gap.
- fix: Delete the two-argument `lossAverseOnKeepers` overload (as the commit message says was done), pass `everyPosition` at line 672, and change TradeMarketTest.java:201 to the three-argument form with real positions so a test asserts the one-starter rule on the rival side.
- siblings: src/main/java/TradeMarket.java:375, src/test/java/TradeMarketTest.java:201, src/main/java/LeagueConsole.java:537

**[high/trivial] Terminal TradeMarket still prices the rival's keepers position-blind (2-arg lossAverseOnKeepers) while the page uses the position-aware overload** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);

// which resolves through line 377:
return lossAverseOnKeepers(season, surplus, Map.of());

// LeagueConsole.java:537-539, by contrast:
TradeMarket.Side theirSide = withKeepers
        ? TradeMarket.lossAverseOnKeepers(seasonOnly, surplus, everyPosition)
        : TradeMarket.scoring(seasonOnly);
```
- scenario: TRAPS #124 records the position-aware keeperValue sweep as done, and it was done for Justin's own side (line 669 passes everyPosition) but the rival side at line 672 still goes through the Map.of() overload, so keeperValue's ONE_STARTER rule never fires for any rival (positionOf.get(...) is null). On the 2026 rosters itsabust holds Herbert (r9) and Lawrence (r14), Renteez holds Jackson (r3) and Love (r16), jerem9604 holds Hurts (r5) and Mahomes (r16), tommyrads holds Williams (r14, kept) and Goff (r16), and BHier holds four keepable TEs. For any offer that takes the second QB from one of these managers, the terminal board charges him the full surplus of the man he sends (before = QB1+QB2, after = QB1+RB) while the page charges only the drop in his best LEGAL pair (often zero). His 'him' column is therefore lower in the terminal than on the page for the same trade, and trades that pass mutual() on the page fail it in ./gradlew run -Pmain=TradeMarket. The two live tools disagree about the same roster on the same afternoon, which is the exact failure #124 was written about.
- fix: Change line 672 to lossAverseOnKeepers(season, surplus, everyPosition) and delete the two-argument overload at lines 375-378 so the compiler flags any remaining caller (TradeMarketTest.theOtherManagerFeelsWhatHeLosesAndNotWhatHeGains can pass Map.of() explicitly). Extend KeeperBasisTest or SearchWidthTest with a source check that no main calls the two-argument form.
- siblings: src/test/java/TradeMarketTest.java:201

**[high/small] TradeMarket's rival side is still position-blind: the 2-arg lossAverseOnKeepers routes Map.of() into keeperValue, so the terminal 'him' column charges a rival two QB keepers the page charges one** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);

// and the overload it reaches, lines 375-378:
static Side lossAverseOnKeepers(java.util.function.ToDoubleFunction<List<String>> season,
                                Map<String, Double> surplus){
    return lossAverseOnKeepers(season, surplus, Map.of());
}
```
- scenario: Commit ac62628 says of the position-blind keeperValue 'The overload is deleted rather than fixed, so nothing can reach it again' - but only keeperValue(roster, surplus) was deleted; lossAverseOnKeepers(season, surplus) survived and passes an empty positionOf, so inside keeperValue `first != null && first == second && ONE_STARTER.contains(first)` is always false and two QBs (or two TEs, two DEFs) are summed as a legal pair. LeagueConsole.java:538 passes everyPosition; TradeMarket.main does not. On the 2026 board any rival whose two largest surpluses share a one-starter position (a manager holding two late-round QBs, e.g. Hamrliks with Maye r13 plus a second QB) has keeperValue(before)-keeperValue(keptBack) overstated in the terminal tool's 'him' column whenever a trade asks for one of them, so ./gradlew run -Pmain=TradeMarket and the console disagree about the same trade's acceptability. The only test of lossAverseOnKeepers (TradeMarketTest.java:201) calls the 2-arg form and so pins the blind path.
- fix: Delete the 2-arg lossAverseOnKeepers overload (as ac62628 did for keeperValue) and pass everyPosition at TradeMarket.java:672; change TradeMarketTest.theOtherManagerFeelsWhatHeLosesAndNotWhatHeGains to the 3-arg form and add an assertion that a rival holding two positive-surplus QBs is charged only the larger when one leaves.
- siblings: src/test/java/TradeMarketTest.java:201

**[high/small] TradeMarket terminal board still prices the rival's keepers position-blind (2-arg lossAverseOnKeepers); the console uses the position-aware one** — `src/main/java/TradeMarket.java:672`
```
Side theirSide = withKeepers ? lossAverseOnKeepers(season, surplus) : scoring(season);
// line 375-378:
static Side lossAverseOnKeepers(java.util.function.ToDoubleFunction<List<String>> season,
                                Map<String, Double> surplus){
    return lossAverseOnKeepers(season, surplus, Map.of());
}
// sibling, LeagueConsole.java:538:
? TradeMarket.lossAverseOnKeepers(seasonOnly, surplus, everyPosition)
// keeperValue's guard, TradeMarket.java:1134, never fires with Map.of():
if(first != null && first == second && ONE_STARTER.contains(first)){
```
- scenario: Commit ac62628 deleted the 2-arg keeperValue overload so 'nothing can reach it again', but left the 2-arg lossAverseOnKeepers, which feeds Map.of() as positionOf, so the rival side of the terminal board still treats two quarterbacks as a legal keeper pair. On the 2026-09-08 rosters (script reproduces the console's own Justin numbers exactly: Nix 32.8, Purdy 19.7, Tuten 33.8, Ravens 3.0) tommyrads holds Caleb Williams r13 (+22.8) and Jared Goff r16 (+28.5) with Tyler Warren TE (+21.3) as his best non-QB. Console: his pair is Goff+Warren = 49.8, so sending Williams costs him 0.0. Terminal TradeMarket: pair = Goff+Williams = 51.3, sending Williams costs 1.5, sending Goff costs 7.2 instead of 5.7. A Williams trade the page prices +1.0 for him prints -0.5 in the terminal and is dropped by mutual() (line 569 requires theirGain > 0), so the two tools disagree on which trades exist, in the column that decides acceptance.
- fix: Delete the 2-arg overload (the same move ac62628 made for keeperValue) and call lossAverseOnKeepers(season, surplus, everyPosition) at line 672; update the one other caller, TradeMarketTest.java:201, to pass Map.of() explicitly. Add a source assertion (KeeperBasisTest style) that no main calls the 2-arg form.
- siblings: src/main/java/LeagueConsole.java:538, src/test/java/TradeMarketTest.java:201

**[medium/small] Console computes his 'season/simple/hole' numbers for uneven trades on a roster that was never cut (or that received Justin's cut man)** — `src/main/java/LeagueConsole.java:681`
```
List<String> hisAfter = TradeMarket.swap(hisRoster, trade.get(), trade.give());
double himSeason = seasonOnly.applyAsDouble(hisAfter)
        - hisSeasonBase.get(trade.withManager());
double himSimple = TradeMarket.simpleStarters(hisAfter, points, positionOf)
        - hisSimpleBase.get(trade.withManager());
boolean himHole = TradeMarket.slotsFilled(hisAfter, points, positionOf)
        < hisSlotsBase.get(trade.withManager());
```
- scenario: TradeMarket.unbalanced prices the receiving side's forced cut inside theirGain (hisOut = [get, cut] at line 495-498) but the Trade record carries no cut, and for the I-receive-two shape the cut is folded into trade.give() (line 511). So for the 28 uneven rows on data/console-2026-w1.html, hisAfter is either a 17-man roster nobody may hold (I send two, receive one) or a roster that includes Justin's dropped man as though he were part of the deal (I receive two). The 'him' and 'himSeason' columns on the same row are therefore computed on different rosters: 'Brock Purdy + Rhamondre Stevenson -> Josh Allen' ships him +19.7 (cut applied) beside himSeason +20.1 (JFMarino at 17 men). The console test at LeagueConsoleTest.java:588 only asserts him <= himSeason, which an extra man can never violate, so it cannot see this.
- fix: Record the cut on the Trade (e.g. a hisCut/myCut field or a Trade.receiverDrops() list) and have the console, TradeStability (line 170) and chainAfter apply it: hisAfter = swap(hisRoster, get + hisCut, give minus myCut). Alternatively have unbalanced return, alongside each Trade, the exact out/in lists it priced each side on.
- siblings: src/main/java/TradeStability.java:170, src/main/java/TradeMarket.java:1191

**[medium/medium] Unbalanced trades encode the forced cut inconsistently, so every consumer that rebuilds rosters with swap(give, get) gets one shape wrong - the page's himSeason and the chain board included** — `src/main/java/TradeMarket.java:495`
```
// I send two, receive one: he cuts, but the Trade carries only `in`
List<String> hisOut = new ArrayList<>(in);
hisOut.add(cut);
trades.add(new Trade(them, give, in,
        myValue.gain(mine, give, in), theirValue.gain(theirs, hisOut, give)));
...
// I receive two, send one: MY cut is folded into `give`  (lines 509-512)
List<String> myOut = new ArrayList<>(out);
myOut.add(cut);
trades.add(new Trade(them, myOut, get,
        myValue.gain(mine, myOut, get), theirValue.gain(theirs, get, out)));

// consumers then do this (LeagueConsole.java:681; chainAfter 1190-1191; TradeStability 169-171):
Lis
```
- scenario: For "I send two, receive one" the rival's `theirGain` is priced with his worst man cut, but the record's `get()` is just the man he sends, so `swap(hisRoster, get, give)` leaves him at seventeen. On data/console-2026-w1.html the row "Brock Purdy + Rhamondre Stevenson -> Josh Allen" (JFMarino) ships him +19.74 and himSeason +20.06; Josh Allen was a 2026 round-2 pick and carries no keeper loss, so the 0.32 between the two columns is the uncut seventeenth man - two numbers the page presents as the same transaction. `chainAfter` (1191) re-searches every later step from that 17-man board, and TradeStability:170-171 re-values `theirs` on it. For "I receive two, send one" the opposite happens: `give()` contains my cut, so `label(trade.give())` on the page and the terminal reads "Give X + <cut man>" as though both are sent, `chainAfter` hands the rival my dropped man, and TradeStability values his side as receiving him. No row of that shape survives `mutual()` on today's page (no give side contains a bench man), so that half is latent; the first half is live.
- fix: Carry the cut explicitly: add `myCut`/`hisCut` (nullable) to `Trade`, keep `give`/`get` as what actually changes hands, and route every post-trade roster through one `TradeMarket.after(roster, trade, side)` that applies the cut. Then `label`, `hisAfter`, `chainAfter`, and TradeStability all agree with the gain that was priced.
- siblings: src/main/java/LeagueConsole.java:681, src/main/java/TradeMarket.java:1190, src/main/java/TradeStability.java:169, src/main/java/TradeMarket.java:509

**[medium/small] The chain re-searches on an unsorted roster, so after step one the multi-man pool is the OLD top few and the man just acquired is excluded** — `src/main/java/TradeMarket.java:1190`
```
board.put(me, swap(board.get(me), first.give(), first.get()));
board.put(first.withManager(), swap(board.get(first.withManager()), first.get(), first.give()));
...
for(Step step : chain(me, board, myValue, theirValue, maxSteps - 1, chainPool, floor)){

// swap appends the incoming men at the END (lines 558-563):
List<String> after = new ArrayList<>(roster);
after.removeAll(out);
after.addAll(in);

// and between() takes the pool from list position (lines 435-436):
List<String> myTop = mine.subList(0, Math.min(pool, mine.size()));
List<String> theirTop = theirs.subList(0, Math.min(pool, theirs.
```
- scenario: main() sorts rosters by projection once (line 631); nothing re-sorts inside `chain`/`chainAfter`. The javadoc at 1178-1179 promises "the chain still sees every side's best few men, which is where chains come from", and the page ships chainPool = 4. On today's page chain 1 sends Evans + Skattebo + Stevenson for Allen + Lamb + Hubbard; step 2's four-man pool is then [Purdy, Nix, Nabers, Henry] by the pre-trade order while Josh Allen - now the highest-projected man on the roster - sits at the tail, outside every two-for-two and three-for-three, and Henry keeps a pool slot he would lose on a re-sort. The rival's roster is unsorted the same way. Consistent with this, none of the eight shipped chains ever moves an acquired man in a multi-man step. The `reach` column (LeagueConsole.java:826) and TradeMarket's TRADING POWER section (767-768) are computed on that mis-pooled search, and `reach` is what ranks "what a trade opens up".
- fix: Sort by projection wherever the chain mutates a board - e.g. give `chain`/`chainAfter` the `points` map and `board.get(x).sort(byPoints)` after each `put` - or make `between` sort its two inputs before taking `subList`. A test: after a forced first trade that brings in the top-projected man, the next step's candidate multi-man deals must be able to include him.
- siblings: src/main/java/TradeMarket.java:1234, src/main/java/LeagueConsole.java:624

**[medium/small] Chain lookahead ('Opens up') searches an unsorted pool after step one and mutates the rival's board wrongly after an uneven first trade** — `src/main/java/TradeMarket.java:1190`
```
board.put(me, swap(board.get(me), first.give(), first.get()));
board.put(first.withManager(), swap(board.get(first.withManager()), first.get(), first.give()));
...
// between(), lines 435-436, assumes the list is sorted by projection:
List<String> myTop = mine.subList(0, Math.min(pool, mine.size()));
List<String> theirTop = theirs.subList(0, Math.min(pool, theirs.size()));
```
- scenario: swap() appends arriving men at the END of the list and nothing re-sorts, so from step two onward the 'top chainPool' men offered into two-for-twos and three-for-threes are the pre-trade top men minus whoever left - never the men just acquired. On the shipped page (chainPool 4) the row 'Brock Purdy + Rhamondre Stevenson -> Josh Allen' reaches 34.38 via step two 'Derrick Henry + Mike Evans -> Chase Brown + Ashton Jeanty': Allen, now Justin's highest-projected man, sits at index 14 and is outside every multi-man pool for the rest of the chain, while JFMarino's board roster is 17 men because the cut priced into the first trade is never applied (and for an I-receive-two first trade his roster would gain Justin's cut man). The javadoc's claim that 'the chain still sees every side's best few men' is false after step one. The same unsorted-pool defect affects chain() at lines 1234-1235, which feeds TradeMarket's TRADING POWER section.
- fix: After each swap in chain() and chainAfter(), re-sort both rosters by -points (pass the points map in, or sort by the objective's own expected value), and apply the forced cut when the first trade is uneven (see the Trade-cut finding).
- siblings: src/main/java/TradeMarket.java:1234, src/main/java/LeagueConsole.java:624

**[medium/trivial] TradeStability searches its own pool of 6 while the board it audits searches DEFAULT_POOL = 8; SearchWidthTest does not read this file** — `src/main/java/TradeStability.java:80`
```
int pool = Integer.getInteger("pool", 6);
...
// the board under the first seed, which is the board the page shows   (line 131)
WeeklyStarterValue first = WeeklyStarterValue.forCurrentBoard(
        configuration, points, scenarios, seedValues[0]);
TradeMarket.Side mySideOne = TradeMarket.scoring(ids -> first.of(ids));
...
    board.addAll(TradeMarket.between(me, entry.getKey(), rosters.get(me), entry.getValue(),
            mySideOne, mySideOne, pool));
```
- scenario: Commit 18fd956 moved the console to `TradeMarket.DEFAULT_POOL` (8) and added SearchWidthTest, but that test's list at src/test/java/SearchWidthTest.java:33 is `List.of("LeagueConsole.java", "TradeMarket.java")` - TradeStability kept the literal 6 the page was just cured of. The RUNBOOK (line 132) runs it weekly as "are the board's numbers bigger than its noise", yet the board it re-values is a narrower one than the page: data/trade-stability-2026-09-08.txt lists eight rows and none involves Rhamondre Stevenson (Justin's seventh/eighth man), while the page's top eight lookahead chains and the SEND-tier three-for-three the commit message cites ("Mike Evans + Cam Skattebo + Rhamondre Stevenson -> Josh Allen + CeeDee Lamb + Chuba Hubbard", reach 42.9) are built on him. The report's headline "worst seed-to-seed spread of a TRADE gain: 19.7" is measured over trades the page does not rank and misses the ones it does. The symmetric season-only scoring (both sides `mySideOne`) is disclosed in the output, but the comment at line 131 still claims it is "the board the page shows".
- fix: `int pool = Integer.getInteger("pool", TradeMarket.DEFAULT_POOL);` and add "TradeStability.java" to SearchWidthTest's file list so the next literal fails the suite. Fix the line-131 comment to say the population is season-only symmetric at the same width, not the page's board.
- siblings: src/test/java/SearchWidthTest.java:33, src/main/java/LeagueConsole.java:234, src/main/java/TradeMarket.java:418

**[medium/trivial] TradeStability searches its board at pool 6 while the page and TradeMarket now share DEFAULT_POOL = 8, so it measures the noise of trades the page does not show** — `src/main/java/TradeStability.java:80`
```
int pool = Integer.getInteger("pool", 6);
...
board.addAll(TradeMarket.between(me, entry.getKey(), rosters.get(me), entry.getValue(),
        mySideOne, mySideOne, pool));
board.addAll(TradeMarket.unbalanced(me, entry.getKey(), rosters.get(me),
        entry.getValue(), mySideOne, mySideOne, pool, points));
```
- scenario: Commit 18fd956 moved LeagueConsole and TradeMarket onto TradeMarket.DEFAULT_POOL (8) and added SearchWidthTest, but that test only inspects LeagueConsole.java and TradeMarket.java (SearchWidthTest.java:33), so TradeStability kept its own literal 6. The runbook says to run TradeStability weekly to ask 'are the board's numbers bigger than its noise', yet its top-8 population is drawn from a pool-6 board: the page's own #1 row on 2026-09-08 (Mike Evans + Cam Skattebo + Rhamondre Stevenson -> Josh Allen + CeeDee Lamb + Chuba Hubbard, +29.3) is built on Stevenson, Justin's 7th/8th man, and cannot appear in data/trade-stability-2026-09-08.txt at all - and it does not. The stability verdicts are about a different set of trades than the ones being recommended.
- fix: Replace the literal with Integer.getInteger("pool", TradeMarket.DEFAULT_POOL) and add TradeStability.java to SearchWidthTest's list of mains that must read the constant.
- siblings: src/test/java/SearchWidthTest.java:33

**[medium/small] TradeStability builds the keeper replacement curve from rostered men only - the population KeeperDriftCheck documents as producing a 347.7-point Bo Nix** — `src/main/java/TradeStability.java:115`
```
Map<Position, java.util.TreeMap<Double, Double>> bestByAdp =
        TradeMarket.bestStillAvailable(points, positionOf);
// positionOf was filled at lines 92-98 from LeagueOwners.today(), i.e. the 192 rostered men only

// KeeperDriftCheck.java:65-71, the sibling that was fixed:
// THE CURVE MUST KNOW THE WHOLE PLAYER POOL, not just rostered men.
// Built from `positionOf` - which came from LeagueOwners and therefore
// holds only the 192 men on rosters - the deepest quarterback it knew
// had an ADP under 175, so ceilingEntry(175) returned nothing, the
// replacement was priced at zero, and B
```
- scenario: LeagueConsole.java:482-490, TradeMarket.java:636-644 and KeeperDriftCheck.java:72-80 all widen `positionOf` to `everyPosition` (every projected player) before calling `bestStillAvailable`; TradeStability never did. Because the running maximum over a subset can only be lower, its surplus is >= the console's for every man, so it can only over-mark. On the 2026-09-08 feeds the deepest rostered QB ADP is 164.9 and Bo Nix's r15 pick at slot 7 is 175, so `ceilingEntry(175)` is null and his surplus is his whole projection (about 296 on the proxy scoring) instead of the page's 32.8; the Ravens read about 10.0 instead of 3.0. Today the [KEEPER] marks happen to land on the right rows (Nix clears 25 either way, the Ravens do not), but the number behind the mark is not the page's number, and the first keepable man of Justin's priced past the deepest rostered ADP at his position - any late-round QB, TE or DEF - is marked at his entire projection.
- fix: Widen `positionOf` to `everyPosition` exactly as the three siblings do (or lift that loop into `TradeMarket.everyPosition(points, positionOf)` and call it from all four). Add a test that `bestStillAvailable` is never handed a map smaller than `points.keySet()` by any main.
- siblings: src/main/java/KeeperDriftCheck.java:72, src/main/java/LeagueConsole.java:482, src/main/java/TradeMarket.java:636

**[medium/small] Three different 'is it bigger than the noise' rules across the report, the page and the chain, all still hanging off the retired 6.8 literal** — `src/main/java/TradeStability.java:231`
```
out.append(String.format("   %6.1f  %s%s%n", line.mySpread(),
        line.survives(floor) ? "clears " + floor + " on every seed"
                : line.mySpread() > floor ? "SPREAD EXCEEDS THE FLOOR - not a number"
                : "inside the floor on at least one seed",
        keeper));
// with floor = Double.parseDouble(System.getProperty("tradeFloor", "6.8"))   (line 81)

// the page's rule, LeagueConsole.java:717-723:
double low = trade.myGain(), high = trade.myGain();
for(WeeklyStarterValue shake : shakes){ ... }
boolean noise = low <= 0;

// and its own verdict on the literal, League
```
- scenario: The runbook (line 132) sends Justin to TradeStability to learn whether "the board's numbers [are] bigger than its noise", and its VERDICT column answers against the 6.8 marginal floor that the console (628-633) and TradeStability's own footer (204-206) both say is the wrong yardstick. The page decides the same question with `low <= 0`. On the 2026-09-08 report "Malik Nabers + Mike Evans -> Jordan Love + Ja..." reads +20.7 / +1.0 / +3.9: the report stamps it "SPREAD EXCEEDS THE FLOOR - not a number"; the page, with low = 1.0 > 0, does not tag it noise and it is eligible for SEND. Meanwhile both chain searches still accept steps against the same 6.8 (TradeMarket.java:766, LeagueConsole.java:619 `System.getProperty("tradeFloor", "6.8")`), so one page uses per-trade error bars for the headline and the retired constant for the chain beneath it. The report header (line 195, "The trades board applies no noise floor - it keeps anything above +0.05") describes a page that no longer exists.
- fix: Pick one rule and share it: expose `TradeMarket.insideItsOwnNoise(double low)` (the page's rule) and have TradeStability print that verdict beside the spread, dropping the 6.8 comparison; move the chain floor to a named constant with a comment saying it is a step-acceptance threshold, not a noise test; rewrite the line-195 prose.
- siblings: src/main/java/TradeStability.java:81, src/main/java/TradeStability.java:195, src/main/java/TradeMarket.java:766, src/main/java/LeagueConsole.java:619, src/main/java/LeagueConsole.java:723

### Unverified (52; found by a lens, not yet refuted — verify before acting)
- [medium] RUNBOOK and the page's own prose say the Trades tab defaults to SEND-only; the control defaults to 'send + worth asking' and the default screen leads with long shots — `RUNBOOK.md:62`: In the shipped data/console-2026-w1.html the default view (tier != 'no', 2026-positive, sorted by 'your full', capped at SHOWN=40) is 103 rows: 4 SEND and 99 ASK. The first four rows on screen are all ASK long shots (Evans+Skattebo+Stevenso
- [medium] The test task declares the .md files as inputs but none of the data/ artifacts the artifact-reading tests parse, so a regenerated console or stability report leaves `test` UP-TO-DATE and unverified — `build.gradle:58`: TradeStabilityTest.theReportsSpreadsAreItsOwnNumbers (line 51), LeagueConsoleTest (lines 39, 322, 382 - every trade-tab check: tiers, noise, hisEdge, keepers2027, chains), TradePartnersTest (line 80), KeeperDriftCheckTest, SeasonOutlookTest
- [medium] The retired 6.8 marginal floor still gates every chain step ('Opens up' and TRADING POWER), and the page prints swapFloor for it — `src/main/java/LeagueConsole.java:619`: The trades table was moved to per-trade error bars precisely because 6.8 (ObjectiveStability, a one-man marginal at 480 scenarios) is not the noise of a trade gain at 240 scenarios (TradeStability measured 0.4-19.7 on 2026-09-08). But chain
- [medium] On uneven trades the page's 'him' numbers are computed on two different rosters: himSeason/himSimple/himHole ignore the forced cut that theirGain already paid for, and 1-for-2 rows label the cut man as sent to the rival — `src/main/java/LeagueConsole.java:681`: For 'I send two, receive one' Trade.get holds only the man received, so hisAfter is his 16 minus 1 plus 2 = 17 men and himSeason/himSimple/himHole are priced on a roster nobody may hold while `him` (theirGain) priced his cut; the 09-08 page
- [medium] 'What you can sell' panel marks the top two surpluses as KEEP with no one-starter rule, so it will name Nix + Purdy the moment Tuten moves — `src/main/java/LeagueConsole.java:939`: TradeMarket.keeperValue learned (lines 1116-1142) that two men at a ONE_STARTER position are one keeper, and RUNBOOK.md states 'Without Tuten the fallback is Nix plus the Ravens, not Nix plus Purdy.' The panel that RUNBOOK says 'names your 
- [medium] unbalanced() passes the wrong side's men as the cut-exclusion list, and the test named for it cannot see that — `src/main/java/TradeMarket.java:491`: worstOther(roster, exclude, points) is documented as 'the lowest-projected man on a roster who is not part of the deal', but both call sites exclude the OTHER roster's men (give when scanning theirs, get when scanning mine), which are never
- [medium] Undrafted men get no keeper round in TradeMarket, TradeStability and KeeperDriftCheck; only the console prices them at the r10 default — `src/main/java/TradeMarket.java:660`: roundsForThisLeague only returns men in this season's draft, and NextYearKeepers.undrafted() exists but is called by nobody. Seven rostered men are undrafted today (Schultz on Justin's roster; Johnson and Vele for Renteez; Hill; Holani; Say
- [medium] TradeMarket.main and TradeStability skip the undrafted-costs-a-tenth rule the console applies, so a waiver pickup carries keeper surplus on the page and zero in both terminal tools — `src/main/java/TradeMarket.java:660`: roundsForThisLeague returns only men in the 2026 draft, so keeperPoints returns 0 (round == null) for every waiver pickup in TradeMarket and TradeStability (line 114), while the console prices him at round 10. On the 09-08 rosters seven ros
- [medium] After step one a chain's multi-player pool is list order, not the best men: swap appends received players at the end and between() takes subList(0, pool) as 'top' — `src/main/java/TradeMarket.java:1190`: main sorts every roster by projection (lines 630-632) so subList(0, pool) means 'best pool men' on the first search only. In chain() and chainAfter() the men just received sit at the END of the list and the men below the cut slide up into t
- [medium] chain/chainAfter re-search an unsorted roster after step one, so the pool window no longer holds each side's best men and 'Opens up' under-searches — `src/main/java/TradeMarket.java:1234`: The console's 'Opens up' column is chainAfter at chainPool 4. For the shipped row 'Brock Purdy + Rhamondre Stevenson -> Josh Allen' (reach 34.38) Allen is appended at index 15 of Justin's list after step one, outside the 4-man window, so no
- [medium] TradeStability keeps its own pool literal (6) while the board it re-values searches DEFAULT_POOL (8); SearchWidthTest only sweeps the two mains, so the 'same trades' it measures are a different population — `src/main/java/TradeStability.java:80`: Commit 18fd956 raised the board to pool 8 and added SearchWidthTest so 'neither main carries' a literal - but TradeStability (blame b46ba0b, 09-07) still searches 6. The class doc says it takes 'the board's best trades, re-value those SAME 
- [medium] TradeStability searches at pool 6 while the board it audits searches at DEFAULT_POOL 8; SearchWidthTest does not cover it — `src/main/java/TradeStability.java:80`: Commit 18fd956 made DEFAULT_POOL 'one constant both mains read, and a test that fails if either grows its own default again', but the third main that builds the same board was not swept. TradeStability's header says it takes 'the board's be
- [medium] TradeStability builds the keeper curve from rostered men only - the exact bug KeeperDriftCheck documents and fixed - so its [KEEPER] marks price a late QB at his whole projection — `src/main/java/TradeStability.java:116`: bestStillAvailable skips every id whose positionOf is null (TradeMarket.java:1064-1065), so free agents never enter the curve and ceilingEntry(pick) runs off the end for any keeper whose pick is later than the last ROSTERED man at his posit
- [medium] TradeStability swallows a keeper-pricing failure to stdout while the written report still claims keeper rows are marked — `src/main/java/TradeStability.java:127`: A missing draft cache, a feed hiccup or an NPE inside NextYearKeepers.roundsForThisLeague is caught, the notice goes to the terminal only, and the committed report file says 'Rows that send one are marked' above a table with no [KEEPER] on 
- [medium] TradeStability judges trades against the 6.8 floor the console has retired; the two weekly tools give opposite verdicts on the same trade — `src/main/java/TradeStability.java:231`: RUNBOOK tells Justin to run both in the same week. For any trade whose three seeds all sit in (0, 6.8] - the JFMarino Purdy+Evans+Skattebo -> Allen+Olave+Jones SEND reads +6.86 on the page's seed - the console tags it not-noise and SEND whi
- [medium] No test exercises the ONE_STARTER pair rule in keeperValue: TradeMarketTest passes Map.of() for positions, so the two-QB fix and the keeperCost column rest on untested code — `src/test/java/TradeMarketTest.java:143`: With an empty positionOf, `first != null && first == second && ONE_STARTER.contains(first)` (TradeMarket.java:1134) is never true, so deleting that branch - or reverting to 'sort and take the top two' - leaves every test green. The RUNBOOK 
- [low] README lists TradeFinder as the trade tool and build.gradle makes it the default main, without the 'superseded' note given to other retired tools — `README.md:37`: A bare ./gradlew run, or a reader following README's order, runs the projected-starters enumerator that knows nothing about keepers, bench value, supply or acceptance, and writes eleven twoTeamTradet*.txt files to the project root; RUNBOOK 
- [low] RUNBOOK's 'read the columns right to left' order does not match the Trades tab's header order — `RUNBOOK.md:63`: Reading right to left on the real page gives With, He trades, ADP out/in, Opens up, vs selling them, vs elsewhere, HE 2026, HE full, HE simple, keeper cost, YOU 2026, YOU full - 'Your full' sits left of 'He gains, simple', and three columns
- [low] RUNBOOK quotes 654 tests; the unit run is 688 by annotation count and 692 by the suite's own last commit — `RUNBOOK.md:133`: src/test/java carries 741 @Test annotations, 53 of them in the 11 smoke-tagged classes that `test` excludes (build.gradle:30 excludeTags 'smoke'), and commit 18fd956 reports 692. A reader checking the log against 654 either thinks tests van
- [low] TradeFinder is still the default main class and prices trades on a starters-only model whose keeper inputs read the 2025 draft — `build.gradle:115`: A bare `./gradlew run` writes eleven twoTeamTrade*.txt files ranked by `improvementT2` on ScoredRoster's best-ten starters - no bench, wire, injury, keeper, outside-option or error-bar term - so its tiers disagree with the console on the sa
- [low] GivenTradeAnalyzer hard-codes the tier file suffix TradeFinder computes, and splices player names into a regex unescaped — `src/main/java/GivenTradeAnalyzer.java:49`: Run TradeFinder with one name in givenPlayersToIgnore and it writes twoTeamTradet0Xignoring1Xreq0.txt; GivenTradeAnalyzer then opens the stale Xignoring0Xreq0 files from a previous run and reports trades from a different search with no erro
- [low] GivenTradeAnalyzer is unreferenced and miscounts players whose names contain 'and' — `src/main/java/GivenTradeAnalyzer.java:89`: No source, test, README or RUNBOOK line references the class (grep finds only its own file and a gitignored worktree copy). If run, --give "Miles Sanders" --take X counts one extra 'and' from 'Sanders' (also Chandler, Alexander, Andrews) an
- [low] LeagueConsole javadoc still advertises -Ppool=6, and README's 'reused for the rest of the day' cache claim predates the 20-minute roster rule — `src/main/java/LeagueConsole.java:50`: A reader of the javadoc believes the page searches at 6 - the exact number commit 18fd956 was about - and a reader of README believes a waiver clearing at noon is invisible until midnight, which TRAPS #127 says was fixed.
- [low] Stale 'size-balanced' prose in the page, the terminal header, the javadoc and the README after unbalanced() joined the search — `src/main/java/LeagueConsole.java:1227`: The page tells Justin the VS ELSEWHERE column is optimistic for a reason that no longer holds, and TRAPS notes the uneven search is what flipped the KevinDA recommendation from send to do-not-send; a reader discounting the column by the sta
- [low] TradeFinder's dedupe and round-filter passes drop the last trade (loop bound size() - 1); both behind flags that default off — `src/main/java/TradeFinder.java:207`: With onlyOne=true or roundFilter=true the final element of xyz2Arr is never re-added to tradePreviews, so the worst-ranked (last polled) trade silently vanishes from the tier files; roundFilter also depends on Keeper.allPotentialKeepers(), 
- [low] TradeFinder carries hardcoded-off switches, a 2024-era ignore list, and an off-by-one that drops the last trade when a switch is on — `src/main/java/TradeFinder.java:207`: Every switch is a compile-time false, so the dedupe and round-filter branches never run; if either is flipped on, the last trade in the queue is silently discarded by the < size()-1 bound, and the ignore list names user ids by 2024 handles.
- [low] TradeFinder keeps a second cache file of the same roster feed, keyed by Justin's league name rather than the shared filepathStartSeriousRosters — `src/main/java/TradeFinder.java:493`: Both caches expire after 20 minutes, but they are two files fetched at two moments, so a waiver processed between the two fetches gives TradeFinder (the build's default mainClass) and LeagueConsole different rosters inside the same session 
- [low] TradeFinderThreeTeams is unreferenced, hardcodes a player name and a loop count, and re-adds the null-player bug TradeFinder fixed — `src/main/java/TradeFinderThreeTeams.java:70`: Nothing in src, tests, README or RUNBOOK names the class (grep hits only its own file and the worktree copy), so it is compiled and never run; if it were, an unknown Sleeper id lands a null Player on the roster and the scorer NPEs several f
- [low] Dead and stale pieces in the live trade tools: realisedTrades has no caller, TradeStability computes 'theirs' it never prints, and the terminal header still says 'size-balanced' — `src/main/java/TradeMarket.java:140`: realisedTrades is referenced nowhere (TradePartners.records supersedes it). TradeStability fills a theirs[] array per trade under every seed - a full objective evaluation each - and row() prints only line.mine(), so the cost is paid and the
- [low] TradeMarket.realisedTrades has no callers; TradePartners.records is what the page uses — `src/main/java/TradeMarket.java:140`: grep finds no call site in src/main or src/test. Its 40-line javadoc describes the 'He trades' signal, which LeagueConsole.java:585 actually takes from TradePartners.records - a reader of TradeMarket is told the rate lives here. Two computa
- [low] worstOther is handed the other side's men as its exclusion list, so the man leaving is never excluded from the cut — `src/main/java/TradeMarket.java:491`: The exclusion is a no-op on both branches; what should be excluded is the man being sent (`get` on his roster, `give` on mine). It cannot bite while rosters are longer than the pool - the candidate sent man is drawn from the top-`pool` by p
- [low] Only the console prices undrafted rostered men at the tenth-round keeper default; TradeMarket and TradeStability leave them at zero — `src/main/java/TradeMarket.java:660`: `NextYearKeepers.undrafted()` exists (line 192) and nothing in src/main calls it; the console re-implements the rule inline and the other two mains omit it. On the 2026-09-08 rosters there are seven undrafted men (Roschon Johnson, Devaughn 
- [low] TradeMarket's report header still says it searched only size-balanced swaps — `src/main/java/TradeMarket.java:676`: data/trades-<date>.txt opens by describing a search it no longer runs; the class javadoc (line 36-38) makes the same claim. A reader checking the file against the page will think the two-for-ones on the page came from a different tool.
- [low] TradeMarket's own terminal table carries none of the guards the console added: no empties-a-slot flag on either side, no error bar, no thins check, so a trade the page tags 'hole' or 'noise' prints as a plain positive row — `src/main/java/TradeMarket.java:734`: LeagueConsole.java:671-687 learned that a SIMPLE of -95 on a trade sending the only defence means 'you would pick one up' and tags it hole on both sides; lines 717-723 re-value under other seeds and tag noise; line 757 checks thinned positi
- [low] The starting-slot table (QB1 RB2 WR3 TE1 DEF1 + 2 FLEX) is written out in five places across the trade tools' dependencies — `src/main/java/TradeMarket.java:746`: All five agree today, so nothing is wrong on the 2026 data. A league setting change (Sleeper `roster_positions`) has to be found and edited in five files, two of which (`alternativeSellers` inputs and the supply panel) are the numbers Justi
- [low] Supply thresholds duplicated as literals in TradeMarket, and 'carries a spare' ignores the two FLEX slots — `src/main/java/TradeMarket.java:759`: The console's comment says the page used to re-decide this in JS 'where nothing can test them'; the same two numbers now live untested in TradeMarket.main. Separately, alternativeSellers counts a rival as a seller when held > startersNeeded
- [low] TradeMarket's 'can I just keep trading?' paragraph describes an unfloored chain the code caps at the same depth as the floored one — `src/main/java/TradeMarket.java:768`: With the default -PchainDepth=2 the 'unfloored' chain can never run more than two steps, so the sentence 'the chain runs 2 trades ... and it starts churning: it reacquires men it gave away earlier' is printed about a run that was never made
- [low] outsideOption and realisedTrades have no callers; the number realisedTrades would compute is hardcoded as 51 while TradePartners' report says 50 — `src/main/java/TradeMarket.java:806`: realisedTrades exists 'so the caller can divide by the league size itself rather than being handed a rate whose denominator is invisible', but no caller exists and the terminal report, the javadoc (lines 45, 411, 432), SearchWidthTest's com
- [low] optics() saturates to Double.MAX_VALUE for a man with no ADP, turning the verdict and the SEND gate on a missing feed field; the path is untested and dormant on today's rosters — `src/main/java/TradeMarket.java:886`: If every man Justin sends lacks adp_half_ppr, gap() is ~1.8e308 and the row reads 'expect a no on sight' and fails the SEND gate (LeagueConsole.java:793, gap <= OPTICS_GRAB); if every man received lacks it, gap is hugely negative and 'you h
- [low] 'He trades' rate counts participants from adds/drops only, so a manager whose side of a trade was draft picks is not credited — `src/main/java/TradePartners.java:94`: Sleeper trade transactions list players in adds/drops and picks in a separate draft_picks array, which LeagueTransactions.moves (line 124-129) never reads. A completed player-for-pick trade credits only the roster that moved a player, so th
- [low] TradeStability builds the keeper replacement curve from rostered men only, the defect KeeperDriftCheck already fixed for itself — `src/main/java/TradeStability.java:116`: bestStillAvailable only keys men whose position is in the map passed in, so TradeStability's curve for QB ends at Jordan Love (ADP 164.9), the latest-drafted rostered QB. Bo Nix costs r15 at Justin's slot 7 = pick 175, ceilingEntry(175) is 
- [low] TradeStability re-fetches the rosters feed for the draft-slot join instead of reusing the snapshot it built the board from — `src/main/java/TradeStability.java:117`: `LeagueOwners.today` goes through the 20-minute roster cache (AAAConfiguration.rosterFreshnessMinutes). The two calls are seconds apart today, but a run that starts inside the last seconds of a cache window prices keeper slots off a roster 
- [low] TradeStability's keeper marking is wrapped in a catch that prints to stdout only, so a report with no marks is indistinguishable from a report with no keepers — `src/main/java/TradeStability.java:127`: If `NextYearKeepers.roundsForThisLeague`, `draftSlotOfPlayer` or the draft feed throws (a draft_order missing, a picks file that fails to parse), every row is written unmarked and the only notice is a console line that is not in data/trade-
- [low] TradeStability computes the rival's per-seed gains and never prints them; unused nameOf param; duplicate LeagueOwners.today; seeds beyond five silently wrap — `src/main/java/TradeStability.java:170`: Two objective evaluations per trade per seed (the rival roster before and after) are paid and discarded; -Pseeds=6 reports a six-seed spread whose sixth column repeats the first, so the header's seed count overstates the evidence with nothi
- [low] TradeStability discards the rival-side spreads it computes and matches keepers by substring on display name — `src/main/java/TradeStability.java:228`: `theirs` costs a full re-valuation per seed per trade and nothing reads it, so the report cannot say whether the rival's side of a recommendation is stable - which is the half that decides acceptance. The keeper map is keyed by "first last"
- [low] TradeStability's [KEEPER] threshold is a bare 25 that must match TradeMarket.HIS_KEEPER_POINTS; the guard test covers only the console pair — `src/main/java/TradeStability.java:243`: TradeMarket's javadoc for HIS_KEEPER_POINTS says it was made a constant so "tuning the number here would have stopped the terminal flagging trades the page kept flagging"; LeagueConsoleTest.java:173-187 enforces that for the page against `T
- [low] TradeStability computes the rival's per-seed gains and never prints them; keeperWarning duplicates HIS_KEEPER_POINTS as a literal 25 and matches names by substring, untested — `src/main/java/TradeStability.java:243`: TradeMarket.java:835-840 says HIS_KEEPER_POINTS is 'one home for the rule' so tuning it does not desync the page and the terminal; this file re-decides it with a literal, so raising the constant to 30 leaves TradeStability marking rows at 2
- [low] TradeStability's keeper marker uses a literal 25 instead of TradeMarket.HIS_KEEPER_POINTS — `src/main/java/TradeStability.java:243`: LeagueConsoleTest:187 pins the page's tag to asksForAKeeper; nothing pins this one. Tune HIS_KEEPER_POINTS to 20 and the page flags Purdy (19.7 -> no, 24 -> yes) while TradeStability keeps the old threshold, with nothing failing - the same 
- [low] onlyNextYearKeepersAssemblesTheCap scans raw source rather than codeOnly, so a comment mentioning consecutiveYears fails it - the very trap commentsAreNotCode in the same file exists to prevent — `src/test/java/KeeperBasisTest.java:105`: The class's own codeOnly() helper (line 45) exists because 'the first version of this test read the raw file and failed on all three tools - every one of which had a comment EXPLAINING that it no longer calls the old entry point'. This thir
- [low] whatATradeOpensUpIsTheChainItShips reads swapFloor as the chain bar, but the chain cleared tradeFloor, which the page never ships — `src/test/java/LeagueConsoleTest.java:512`: Two knobs, both defaulting to 6.8 and both forwarded by build.gradle. Run the console with -PswapFloor=3 and the test passes vacuously while the prose at LeagueConsole.java:1229 ('every step clearing the ${D.swapFloor.toFixed(1)}-point floo
- [low] TradeMarketTest asserts a stale claim: 'only balanced ones are searched' — `src/test/java/TradeMarketTest.java:37`: unbalanced() has been searched by both mains since 81e616c (TradeMarket.java:695, LeagueConsole.java:550); the message a future failure would print explains a design that no longer exists.
- [low] theReportsSpreadsAreItsOwnNumbers asserts at exactly the rounding bound (0.15 = three half-digits), the coin-flip the console test's own comment warns against — `src/test/java/TradeStabilityTest.java:68`: Each seed column and the spread are printed to one decimal, so the printed range can honestly differ from the printed spread by up to 0.05+0.05+0.05 = 0.15. LeagueConsoleTest.java:481-487 records what happens at the bound ('Skattebo came in


## 7. Waiver finder & weekly lineup
confirmed 9 · refuted 3 · unverified 57

### Confirmed

**[high/small] The page bids on the 17-week worth; the report scales it to weeks left; FaabBid tells Justin to type 'that number'** — `src/main/java/LeagueConsole.java:440`
```
int bid = FaabBid.bestBid(allBand, worth, 1.5, budgetLeft);
```
- scenario: TuesdaySwap.java:270-280 computes `int weeksLeft = Math.max(1, 15 - week);` and prints "scale by %.2f for what a move is worth from here" beside every row (week 1: x0.82), while the console ships the raw `worth` and computes the bid from it, and FaabBid.java:252-253 says "TuesdaySwap prints exactly that number for every add it considers" without naming which of the two columns. On today's data the feed is frozen (0 of 586 season projections moved between the 09-08 and 09-11 caches, after Thursday's game), so V(R) is a full-season unit and TuesdaySwap's scaling is the right one: reproducing bestBid on the committed price list, a man worth 20 (17-week) in week 8 bids $3 on the page against $1 at the 8.2 the report calls collectable; worth 30 in week 11 bids $4 against $1. The divergence grows every week and neither tool consults ProjectionDrift, whose answer decides which of them is right (if the feed ever becomes rest-of-season, TuesdaySwap double-discounts instead).
- fix: One function (e.g. TuesdaySwap.fromHere(gain, week, regime)) that both callers run; ship `worthFromHere` in the wire row and bid on it; have FaabBid's -Pvalue prompt name the column; make the scaling policy read ProjectionDrift's verdict rather than assume a regime.
- siblings: src/main/java/TuesdaySwap.java:270, src/main/java/TuesdaySwap.java:279, src/main/java/FaabBid.java:252

**[high/small] IR men are indistinguishable from active men in every roster join** — `src/main/java/LeagueOwners.java:31`
```
for(JsonElement p : roster.getAsJsonArray("players")){
    owner.put(p.getAsString(), nameByUser.getOrDefault(by, by));
}
```
- scenario: The league runs `reserve_slots: 2` and Sleeper's `players` array includes the `reserve` list; LeagueOwners.byPlayer, LeagueWeek.rostered (LeagueWeek.java:118-123), LeagueConsole's rosters (246-251), TuesdaySwap's roster (228-232) and StartSit's `mine` (183-194) all read `players` and never `reserve`. On the 09-08 rosters four rivals carry IR men with live season projections - Henderson 153.5 (d0ddi), Charbonnet 61.7, Pacheco 48.6, Dell 43.4 - counted as 17th men. In this component the exposure is Justin's own IR use: TuesdaySwap would search "add X, drop the IR man" as a like-for-like pair although the add needs an active spot the roster does not have (the plan is not executable), and the console/StartSit will START an IR man the week Sleeper publishes a row for him (it does for designated-to-return men). Today it changes the rivals' hisSlotsBase/simpleStarters and offers their IR men on the trade board.
- fix: Have LeagueOwners return active and reserve separately (or add LeagueOwners.reserve(configuration)); exclude reserve from the lineup and from the swap search's droppable roster; let TradeMarket choose its own policy explicitly.
- siblings: src/main/java/LeagueWeek.java:118, src/main/java/LeagueConsole.java:246, src/main/java/TuesdaySwap.java:228, src/main/java/StartSit.java:183

**[high/large] Outcome pool keyed by Sleeper's season-END feed - the objective's cells already know who got hurt** — `src/main/java/OutcomeDistributions.java:102`
```
Map<String, Double> projected = HistoricalProjections.leaguePointsBySleeperID(configuration, season);
...
// HistoricalProjections.java:83-86
String url = "https://api.sleeper.app/projections/nfl/" + season
        + "?season_type=regular&position[]=DEF&position[]=QB&position[]=RB&position[]=TE&position[]=WR"
        + "&order_by=pts_half_ppr";
String data = InOutUtilities.getCachedForever(url, "sleeperProjectionsFinal" + season);
```
- scenario: Since 57e6559 (2026-09-04) every cell WeeklyStarterValue draws from is keyed by the rank a man holds in sleeperProjectionsFinal{2021..2025}.txt, files fetched 2026-08-25 from the full-season endpoint that MODEL.md:2246-2252 itself declares 'UNUSABLE for training ... rest-of-season values frozen at season end - a projection that already knows who got hurt'. Checked against the committed real August snapshot (data/sleeper-projections-dated-2021-08-09.json.gz): of the top 200 men by August projection only 25 match the Final file exactly and 172 differ (Fitzpatrick 323.7 -> -5.3 after 1 game, Watson 329.4 -> 38.2 after 0, J.Taylor 157.8 -> 257.3, Callaway 114.5 -> 176.2), and the Final feed correlates with the realised season far better than the true August one at every position (QB 0.842 vs 0.715, RB 0.553 vs 0.405, WR 0.513 vs 0.446, TE 0.746 vs 0.602). So a 2026 man tiered by his real preseason projection draws his availability and boom/bust from a cell whose historical members were sorted partly by outcome: top tiers are survivors, P(up) is overstated, the bust tail is thinned, and every bench/insurance marginal is understated. That number is the 'worth' on the console wire tab, TuesdaySwap's gain, and both sides of the trade board. RankKeyChoice's '6.2 +/- 0.8 better' (TRAPS #82) measured this leak as a virtue, because its held-out season is keyed by the same contaminated file; the HistoricalProjections javadoc ('the ADP everyone drafted against that year') contradicts MODEL.md about the same endpoint.
- fix: Key the historical cells by a right-vintage order: the week-1 projection endpoint the repo already found usable (MODEL.md 'USABLE, and it is the find'), or the dated sleeper-adp/fp-ecr snapshots in data/. Re-run RankKeyChoice with that key and retract the #82 conclusion in a commit; fix the HistoricalProjections javadoc to say what the file is.
- siblings: src/main/java/RankKeyChoice.java:170, src/main/java/PlanBacktest.java:187, src/main/java/HistoricalProjections.java:11-13

**[high/small] The flip curve is calibrated on half-PPR gaps and applied to league-scored gaps** — `src/main/java/StartSit.java:310`
```
Map<String, Double> projected = WeeklyProjections.pointsBySleeperID(season, week);
Map<String, Double> actual = LeagueActuals.weeklyPoints(season, week);
```
- scenario: -Pcalibrate bins pairs by the gap in WeeklyProjections.pointsBySleeperID, which returns the feed's `pts_half_ppr` untouched (WeeklyProjections.java:39-41; 4-pt passing TD, no fumble), and grades them on LeagueActuals.weeklyPoints, which is league-scored. The live report (StartSit.java:180) and the console (LeagueConsole.java:239, 324-325 `StartSit.flipRate(curve, gap)`) feed the curve gaps from LeagueWeek.projected, which is league-scored (6-pt TD). On the 09-08 week-1 feed Purdy/Nix are 18.77/17.89 in the curve's units (gap 0.88, bin 0-1, 47.5%) and 21.84/20.71 league-scored; the console ships gap 1.05 (bin 1-2) and prints "beats him 43% of the time" for a bench call the calibration never measured at that gap. Only quarterback pairs move (RB/WR/TE/DEF differ by under 0.2), but Purdy-vs-Nix is the one genuine weekly start/sit on this roster and the odds column is the tool's answer to whether it is a real call.
- fix: Calibrate on the same scorer the live path uses (SleeperProjections.scoreStatLine over the week feed, i.e. LeagueWeek.projected's scoring) or apply the curve to feed-unit gaps; print the unit in the start-sit-flip header so StartSitTest can assert it.
- siblings: src/main/java/StartSit.java:180, src/main/java/LeagueConsole.java:325, src/main/java/WeeklyProjections.java:39

**[high/medium] The 6.8 'noise floor' was measured on a different quantity and population than the wire gains it judges** — `src/main/java/TuesdaySwap.java:208`
```
return priced.isEmpty() || priced.get(0).worth() < floor ? null : priced.get(0);
// TuesdaySwap.java:217
double floor = Double.parseDouble(System.getProperty("swapFloor", "6.8"));
// ObjectiveStability.java:47,61 - what 6.8 was measured on
System.setProperty("fixtureDir", Path.of("data", "fixtures", "2026-pre-draft").toString());
for(Keeper k : KeeperChooser.eligibleCandidates(configuration, user)){
// LeagueConsole.java:471
worth < swapFloor, hole,
```
- scenario: data/objective-stability-2026-09-04.txt says what 6.8 is: the worst seed-to-seed spread of a STARTER'S marginal (Courtland Sutton, +57) on a 13-man 2025 keeper-eligible roster read off the pre-draft fixture, 3 seeds. TuesdaySwap.recommend and the console's 'inside the noise' tag apply it to an (add, drop) pair gain on the 16-man 2026 roster - a bench-level difference of two men. The same report shows bench-level marginals wobble 0.6-2.4 (Mitchell 2.4, Texans 1.2), so the floor is almost certainly too high for the wire: a real +4 claim reads DO NOTHING all season, while nothing measures the pair gain's own wobble. The trades tab already learned this exact lesson (b46ba0b: 'one global floor was the wrong instrument ... came from a roster MARGINAL at 480 scenarios, a different quantity') and re-values each trade under other seeds; the wire tab and TuesdaySwap were never swept. LeagueConsoleTest.theWireIsJudgedAgainstItsOwnNoiseFloor only checks the scenario count (480 = 480), so it cannot see this.
- fix: Do for the wire what the trades tab does: re-value the top priced rows under the other seeds (shakes already built in LeagueConsole) and tag noise when the gain goes negative on any seed; have TuesdaySwap print that range and decide CLAIM/DO NOTHING on it. Remove the 6.8 literal from both callers.
- siblings: src/main/java/LeagueConsole.java:418, src/main/java/LeagueConsole.java:471, src/test/java/LeagueConsoleTest.java:307-334

**[medium/small] DefenceThisWeek can tell Justin to claim and start a defence that is on bye this week** — `src/main/java/DefenceThisWeek.java:86`
```
for(Defence defence : startable){
    // ADP order breaks ties because the list is already sorted by it
    if(defence.form() != null && defence.form() > bestForm){
        bestForm = defence.form();
        best = defence;
    }
}
...
out.append(String.format("THE POLICY SAYS: claim and start %s (free%s).%n", policy.name(),
```
- scenario: The pick is points-per-game over finished weeks and preseason ADP; nothing consults this week's projection feed, which is how StartSit/LeagueConsole detect a bye ('a man ABSENT from the map is not playing'). From week 5 the form leader among free defences is on bye roughly one week in six, and the report prints 'THE POLICY SAYS: claim and start X' for a defence that will score 0. WireRateStress.form (WireRateStress.java:353-354 `Double scored = pick.weekly()[week]; total += scored == null ? 0 : scored;`) scores those weeks as zero too, so the 8.03 quietly contains the cost - but a live instruction to start a bye defence is a wrong verdict, not a reproduced policy.
- fix: Load LeagueWeek.projected(season, week) and exclude defences with no row (bye/unpublished) from `startable`, printing that the choice departs from the backtested policy for that week; optionally add the same check to WireRateStress.form so the measured rate matches what a manager would do.
- siblings: src/main/java/WireRateStress.java:339-357

**[medium/small] The page bids FAAB on the unscaled seventeen-week worth; TuesdaySwap tells him to scale it** — `src/main/java/LeagueConsole.java:440`
```
int bid = FaabBid.bestBid(allBand, worth, 1.5, budgetLeft);
// TuesdaySwap.java:270,279-280
int weeksLeft = Math.max(1, 15 - week);   // the regular season runs to week 14
out.append(String.format("Gains are in the objective's seventeen-week units; about %d of those weeks are left, so%n"
        + "scale by %.2f for what a move is worth from here.%n", weeksLeft, weeksLeft / 17.0));
// FaabBid.java:201 - a dollar is priced as a point
double net = band.winChance(bid) * (worth - bid * dollarCost);
```
- scenario: FaabBid maximises P(win) x (worth - bid x cost) with worth in points and a dollar costing `dollarCost` points, so the bid is a direct function of the worth unit. The console feeds `row.worth()` in 17-week units while TuesdaySwap prints the same number and says to multiply by weeksLeft/17 (0.82 in week 1, 0.41 by week 8) for what the move is actually worth. By mid-season a man worth 20 on the page is worth about 8 from here, and bestBid(20) at 1.5x can return $3-4 where bestBid(8) returns $0-1 - an overbid printed as the recommended number, with no test comparing the two units. The tool's own javadoc (TuesdaySwap.java:36-40) asserts the feed becomes rest-of-season in-season, under which the scaling would instead be a double discount; the two claims cannot both hold, and ProjectionDrift's own archive shows 0 of 586 moved from 09-08 to 09-11 after the Thursday game.
- fix: Decide the unit once: pass worth x weeksLeft/17 into bestBid on the page (and print 'from here' beside worth), and state the FAAB exchange rate (1 dollar = 1 objective point at 1.0x) in FaabBid rather than 'the budget is free'. Once ProjectionDrift settles whether the feed moves, drop whichever of the two scalings is wrong.
- siblings: src/main/java/TuesdaySwap.java:270, src/main/java/FaabBid.java:191-193, src/main/java/LeagueConsole.java:1109 (page note)

**[medium/medium] Wire rows are judged by the single-marginal 6.8 floor while trades on the same page got per-trade seed bars** — `src/main/java/LeagueConsole.java:471`
```
worth < swapFloor, hole,
```
- scenario: TRAPS #112 established that a two-man move has its own spread (0.4 to 18.0 across sixty trades) and LeagueConsole.java:638-644 builds `shakes` under other seeds to bracket every trade; the wire rows (LeagueConsole.java:418 `"swapFloor", "6.8"`, TuesdaySwap.java:217 same literal, recommend() at 207-209) still use the constant ObjectiveStability measured for ONE man in/out (ObjectiveStability.java:61 `KeeperChooser.eligibleCandidates(configuration, user)` on the 2026-pre-draft fixture, line 47) - a 13-man roster of Flowers, White, Sutton, Kelce that Justin no longer holds. A swap is add-minus-drop on the current 16-man roster against deep free agents: a row reading +8.0 is named CLAIM and untagged although its own seed spread can exceed 8, and the "inside the noise" tag is asserted rather than measured. Today only Mayfield +1.19 is on the board so no verdict changed, which is the after-the-fact discovery #101 warns about.
- fix: Re-value each Priced row under the seeds already built (at the wire's 480), ship low/high per row as the trades do, tag noise when low <= 0, and make TuesdaySwap.recommend require the low bound to clear zero; retire the borrowed constant from both callers.
- siblings: src/main/java/TuesdaySwap.java:217, src/main/java/LeagueConsole.java:638, src/main/java/ObjectiveStability.java:61

**[medium/small] ProjectionDriftTest cannot fail on the question the tool exists for, and the tool reads the ephemeral archive** — `src/test/java/ProjectionDriftTest.java:59`
```
assertTrue(whole.moved() > 0,
        "NOT ONE of " + whole.shared() + " season projections has changed across the"
                + " whole archive. The feed is static, ...
```
- scenario: `whole` is oldest-vs-newest over the whole archive, whose oldest file is 2026-08-24; thirty men moved before 09-07, so the assertion is permanently true whatever the feed does after kickoff. Between the 09-08 and 09-11 caches - after Thursday's game - 0 of 586 shared projections moved, which is exactly the condition the assertion text describes, and the test stays green. The archive itself is ProjectionDrift.java:65-68's cwd `sleeperProjections\d{4}\d{4}-..` files, which .gitignore drops (`/*.txt`), while AdpSnapshot.java:125 maintains the committed data/projection-snapshots.csv (last row 2026-09-02); on any other machine the test skips on "need two archived snapshots".
- fix: Measure drift from the last snapshot before the first kickoff (or since the last finished week) to the newest, read the committed CSV, and assert on that window; have the tool print a per-game-week verdict rather than raw counts.
- siblings: src/main/java/ProjectionDrift.java:65, src/main/java/AdpSnapshot.java:125

### Refuted
- **The immutable week-projection cache was populated five days before kickoff by a reader that never checks finished()** — `src/main/java/WeeklyProjections.java:31`
  - stands: Quoted code is accurate at both cited locations. WeeklyProjections.java:31 calls InOutUtilities.getCachedForever unconditionally with cache key "sleeperWeekProjection"+season+"w"+week and no call to LeagueWeek.finished(). LeagueWeek.java:74-75 uses the byte-identical key ("sleeperWeekProjection"+season+"w"+week) as its immutable/finished-week cache name in feed(). InOutUtilities.getCachedForever (lines 233-253) only refuses to write on an empty payload -- it has no notion of live-vs-finished and will silently read back whatever file already sits at that path. Confirmed on disk: sleeperWeekProjection2026w1.txt exists, mtime 2026-09-05 10:49, five days before the 09-10 kickoff -- written while week 1 was still live/future, not after it finished. None of the current committed callers of WeeklyProjections.pointsBySleeperID reach live season-2026 week 1 (FaabBid's fit path requires year.complete(); StartSit.calibrate() loops year < current; AccuracyShootout hardcodes seasons 2021-2025), so this exact write likely predates or bypassed the current LeagueWeek split rather than being reproduced by a currently-live call graph -- but that history doesn't neutralize the forward risk. The file already sits under the exact name LeagueWeek's finished-week path will read the moment Sleeper's state advances past week 1, WeeklyProjections still has zero guard against writing under that same immutable name during any future live week (including ad hoc/manual invocations), and LeagueConsole.java (grep-confirmed) calls LeagueWeek.projected/feed -- LeagueConsole is the weekly page Justin actually reads. A pre-existing stale file silently poisoning a decision-surface cache with no error and no diff visibility (file is gitignored) meets the rubric's high bar for silent-corruption reaching a decision Justin acts on.
  - refuted: The code matches the quote (WeeklyProjections.pointsBySleeperID at line 31 does call InOutUtilities.getCachedForever unconditionally, with the same file-name pattern ("sleeperWeekProjection"+season+"w"+week) that LeagueWeek.feed uses only for finished weeks), and the file sleeperWeekProjection2026w1.txt does exist on disk, dated Sep 5 10:49 (553351 bytes) — so a live-week snapshot was indeed frozen under the immutable name. That part is accurate.

But the finding's consequence chain is false. I traced every caller of WeeklyProjections.pointsBySleeperID in the repo (grep across src/main and src/test): only three exist —
1. AccuracyShootout.java:57 — inside a loop `for(String season : {"2021","2022","2023","2024","2025"})`, never 2026.
2. StartSit.java:310 — inside `calibrate()`, called only when `-Pcalibrate` is set (StartSit.java:160-163), and calibrate's own loop is `for(int year = current - 5; year < current; year++)` (line 303) — current season is explicitly EXCLUDED (`year < current`, not `<=`). StartSit's live per-week lineup path (main(), line ~164 onward) uses `LeagueWeek.projected()`, not `WeeklyProjections`, and is unaffected.
3. FaabBid.java:223 — inside the `-Pfit` historical-calibration branch, gated by `if(!year.complete()){ continue; }` (line 218) — the in-progress season is explicitly skipped, so 2026 week 1 is never requested here either.

The finding also names "LineupPromotion/InSeasonLearning backtests" as consumers of the stale data — I checked both files directly and neither calls WeeklyProjections at all; that claim is simply fabricated/untraced.

So while WeeklyProjections.java does have the described missing-finished()-check defect and did write a premature immutable file, no code path Justin currently runs reads that file for the live 2026 season — StartSit's live lineup calls, FaabBid's live banding, and the console/report paths all get week-1 2026 projections through LeagueWeek.projected(), which correctly routes live weeks to the day cache. The specific "StartSit -Pweek=1 FINISHED-week path... reads a stale week-1 projection" and "-Pcalibrate... reads it" claims cannot occur on this code as written. No number Justin acts on today is affected by the stale sleeperWeekProjection2026w1.txt file.
- **Sunday's lineup tab reads a day-cached injury feed (and, per the runbook, Tuesday's page)** — `src/main/java/LeagueConsole.java:326`
  - refuted: Half (a) holds: LeagueConsole.java:326 is `String status = SleeperProjections.injuryStatusOf(man.id());`, and injuryStatusOf (SleeperProjections.java:256-269) populates cachedInjury from getTodaysProjections(), which calls InOutUtilities.getTodaysWebPage — confirmed to cache to a file keyed by today's date (InOutUtilities.java:136-150) and not refetch again same-day, unlike getRecentWebPage's 20-minute window used for rosters (AAAConfiguration.java:101-103). The per-day-cache mechanism is real.

But half (b) fails on the finding's own load-bearing premise: "RUNBOOK's Sunday section names no regenerate step, so the tags and break-evens Justin reads are Tuesday's." RUNBOOK.md has a "## Regenerate BOTH" section (lines 7-21) instructing `./gradlew run -Pmain=TuesdaySwap` then `./gradlew run -Pmain=LeagueConsole` back to back, then "Then open `data/console-<season>-w<week>.html`. Everything below is on that page." This precedes both the "## Sunday, ninety minutes before kickoff" and "## Tuesday" sections — it is the entry step for viewing the console page on either day, not Tuesday-only. Following the runbook, Justin regenerates LeagueConsole Sunday morning before reading the lineup tab, fetching that day's (not Tuesday's) file since the cache filename is keyed by DateUtility.getTodaysDate(). So the headline claim that the tags read are "Tuesday's" is false under normal runbook use.

The narrower real edge case — a second same-day regen (08:00 then 11:30) is a no-op for injury status since the day-file already exists — is real per the code, but the runbook already tells Justin the status column isn't the final word: "The page cannot know the actual probability. That arrives in the inactive report about ninety minutes before kickoff... Read the bar, read the report, decide." The workflow already routes last-mile freshness through an external source, not the page, undercutting the claim that a stale tag silently drives the START verdict Justin acts on. The finding's central factual claim about the runbook is contradicted by the file itself, and the residual scenario is explicitly hedged against by design.
- **Season-ending losses are dropped from the outcome cells: games == 0 and nulled projections exclude the very seasons insurance is for** — `src/main/java/OutcomeDistributions.java:137`
  - refuted: Quoted code is accurate at OutcomeDistributions.java:114 and :137, but the finding's mechanism for "door 2" is wrong. Line 114 (`entry.getValue() == null || entry.getValue() <= 0`) filters `projected.entrySet()` — i.e. HistoricalProjections.leaguePointsBySleeperID's preseason projections — not the "Final files" / pts_half_ppr actuals data as claimed. pts_half_ppr null-handling lives entirely in LeagueActuals.java (line ~119/270), a different code path never touched by this loop. So the specific claim that Chubb 2023, Rodgers 2023, Javonte Williams 2022, Penny 2022, Lance 2022, Rice 2024, Gus Edwards 2021 etc. get dropped via null Final points at this line is false — these are all skill players who had ordinary positive preseason ADP/projections, so entry.getValue() would be well above 0 for them.
Checking door 1 (games==0) against the same list: all of the named players except Dobbins 2021 (and arguably Dobbins 2023, ambiguous) actually recorded multiple games before their season-ending injury (Chubb played 2, Lance played 2, Rice/Edwards/Penny/J.Williams played several), so games>0 and they are NOT excluded by the `games==0` continue either — they enter the pool correctly with a low games count and a depressed per-game rate, which is exactly the intended representation of an early-season-ending injury. The finding's central evidentiary claim — that these 8 named "heaviest tail" players are dropped from the outcome cells — does not hold for the code as written; only true zero-recorded-game seasons (a much rarer case, e.g. a pure preseason IR before Week 1) are actually skipped by line 137, and that is a narrower, lower-magnitude population-discipline nuance than described, not the broad survivorship claim made.
WeeklyStarterValue does reach live decision surfaces (LeagueConsole, TuesdaySwap, TradeMarket, LiveLateRounds all construct WeeklyStarterValue from this pool), so the underlying mechanism isn't inert, but the finding as written misattributes the null-filter to the wrong data source and misidentifies most of its own supporting examples, so its account of the bug and its scope is not accurate.

### Unverified (57; found by a lens, not yet refuted — verify before acting)
- [medium] RUNBOOK says the Trades tab defaults to the offers a rival would thank you for; the page defaults to 'send + worth asking' and its own prose says the strict filter is on by default — `RUNBOOK.md:62`: The in-season RUNBOOK section was written at 07:12 on 2026-09-07 (0a93dba); the tiers landed at 11:24 (ce5ecc1) and moved the select's first option to 'both'. Only the 'send' tier requires `!noise`; 'ask' rows may sit inside their own error
- [medium] RUNBOOK tells him to read the test count and '0 skipped' out of check-wire.log; the log carries neither, and two console tests skip on the exact regression they exist to catch — `RUNBOOK.md:153`: Gradle prints a count only on failure, and the `test` task has no testLogging, so the log the RUNBOOK names cannot show 692 tests or a skip. The '654' itself is stale (18fd956: '692 tests, none skipped'). The skip rule matters here because 
- [medium] DefenceThisWeek can tell Justin to claim and start a defence that is on bye — `src/main/java/DefenceThisWeek.java:70`: The Defence record (line 51-52) carries preseasonAdp, form, gamesSoFar, mine, free - nothing about whether the defence plays THIS week. pick() ranks by ADP or points-per-game over finished weeks, so from the first bye week a free defence wi
- [medium] RUNBOOK, TRAPS #116 and the code comment promise exclusive replacements and 'nobody healthy left'; the code rebuilds each doubtful starter's ten independently — `src/main/java/LeagueConsole.java:312`: 81e616c removed the `taken` set, the 'nobody healthy left' message and the untagged-only rule when it replaced named replacements with bestTenWithout. bestTenWithout(man, mine) includes every other man - the other Questionable starters, tag
- [medium] The Sunday break-even bar (bestTenWithout / breakEven) has no test that re-derives its number — `src/main/java/LeagueConsole.java:331`: This is the number RUNBOOK 'Sunday, ninety minutes before kickoff' tells Justin to act on ('needs 85%'). bestTenWithout (lines 165-176) rebuilds the ten via TeamRankings.bestLineup and the formula here is checked by nothing: LeagueConsoleTe
- [medium] FAAB left silently defaults to 100 if the owner or settings lookup misses; the test cannot tell — `src/main/java/LeagueConsole.java:347`: If `me` is the raw user id (display name missing), if owner_id-to-name differs from the -Pme spelling, or if `waiver_budget_used` is absent for a roster that has spent nothing, budgetLeft stays 100 with no message. Today the page shows $100
- [medium] The 6.8 wire floor is a typed literal in three places, measured over the 13-man pre-draft roster, and the test that claims to read the yardstick off its report reads only the scenario count — `src/main/java/LeagueConsole.java:418`: data/objective-stability-2026-09-04.txt is the worst of 13 marginals on Justin's 2025 end-of-season roster (Flowers, White, Sutton, Godwin, Johnston, Kelce, Andrews...) valued on the frozen pre-draft board with no picks made. The console ta
- [medium] A legitimately empty wire turns `check` red while the page correctly says 'Do not claim' — `src/main/java/LeagueConsole.java:436`: data/console-2026-w1.html shipped exactly ONE wire row (Mayfield, +1.2); every DEF row printed +0.0 and was filtered here. A week where nothing clears +0.05 in 17-week units is one row away and ordinary. When it happens the page prints 'Not
- [medium] The page's FAAB bid and TuesdaySwap's 'from here' column apply opposite horizon conventions to the same worth; FaabBid points him at an ambiguous number — `src/main/java/LeagueConsole.java:440`: The objective spreads whatever total the feed carries over 17 weeks (WeeklyStarterValue:365), so a swap's `worth` is in feed-total units. TuesdaySwap tells Justin the number to act on is worth x weeksLeft/17 (0.82 in week 1, 0.41 by week 8)
- [medium] nflState is day-cached: a fetch before Sleeper advances the week pins the old week for the whole Tuesday — `src/main/java/LeagueWeek.java:35`: Pattern #8. RUNBOOK's loop runs TuesdaySwap then LeagueConsole 'Tuesday morning'. If any tool touches nflState that day before Sleeper's weekly advance (the cache is keyed by calendar day; nflState2026-09-08.txt shows week:1/display_week:1)
- [medium] LeagueWeek.state() is day-cached, so a run made before Sleeper advances the week pins every later run that day to the finished week and overwrites the committed week artifacts — `src/main/java/LeagueWeek.java:37`: Tuesday 2026-09-15. Any tool touched after midnight but before Sleeper advances /state/nfl to week 2 (the same 00:35-style run that bit the roster on 2026-09-07) writes nflState2026-09-15.txt with week 1. Every LeagueConsole/TuesdaySwap/Def
- [medium] Flip curve is measured on same-position pairs but applied to cross-position flex gaps — `src/main/java/StartSit.java:61`: flipCurve pairs men only within a position (this grouping, then i<j inside `group`). closestStarter (line 271-299) deliberately measures a benched WR against the weakest flex-eligible starter he can legally displace, which can be an RB; Lea
- [medium] Flip curve is binned in the feed's half-PPR gaps but looked up with league-scored gaps — `src/main/java/StartSit.java:310`: calibrate builds data/start-sit-flip-2026-09-04.txt from raw pts_half_ppr week projections, while StartSit.main and LeagueConsole (lines 239, 324-325) compute the gap from LeagueWeek.projected, which re-scores the stat line at 6 per passing
- [medium] The 6.8 noise floor is a literal in five files and no test binds it to the report that measured it — `src/main/java/TuesdaySwap.java:217`: The same '6.8' default is typed in LeagueConsole:418 (swapFloor), LeagueConsole:619 (tradeFloor), TradeMarket:766 and TradeStability:81. ObjectiveStabilityTest binds only DIAGNOSTIC.md to the report's 'worst seed-to-seed spread' line; Leagu
- [medium] Ladder comparison picks the swap report by string order; silently skips from week 10 — `src/test/java/LeagueConsoleTest.java:383`: newestConsole() (lines 37-48) was rewritten to sort season*100+week numerically, with a comment explaining that string order puts console-2026-w9 above w10..w18. The sibling picker for the TuesdaySwap report at line 383 still sorts by filen
- [medium] WireRateStressTest.fallsBackToPreseasonRankWithNoEvidence cannot fail: the 'blank' fixture is all zeros, so any pick scores 0 — `src/test/java/WireRateStressTest.java:97`: def() at line 23-29 fills `series[week] = week < weekly.length ? weekly[week] : 0.0`, so a 'blank' defence has 0.0 in every week, not null. formThrough then returns 0.0 (played = WEEKS), never the null the docstring ('every candidate blank 
- [low] LateRoundTargets is superseded by LiveLateRounds but MODEL.md still says LiveCommittee routes rounds 10+ to it — `MODEL.md:2223`: `grep LateRoundTargets src/main/java/LiveCommittee.java` finds nothing; the only main-code mentions (DraftNight:258, DraftPlanner:133, LateSurvival:14) are warnings not to use it. README does not list it. MODEL.md's routing sentence describ
- [low] README's caching paragraph describes a policy the code no longer has — `README.md:92`: Rosters expire after 20 minutes, finished weeks are kept forever, player metadata for 7 days (PlayerRawData:129), and the in-season feeds are date-keyed. 'Delete them to force a refresh' is wrong for the forever-cached weeks (deleting re-fe
- [low] RUNBOOK's weekly loop names neither DefenceThisWeek nor StartSit, and its test count is stale — `RUNBOOK.md:133`: The latest commits report 692 tests, so the number Justin is told to look for in check-wire.log is 38 short. More consequentially, the in-season section (lines 1-157) never mentions DefenceThisWeek or StartSit: two [live] tools with committ
- [low] The committed FAAB report's win ladder predates the tie fix and contradicts the page's — `data/faab-contests-2026-09-05.txt:22`: FaabBid.Band.winChance (FaabBid.java:97-104) now credits half of every tie, so a $0 bid wins 27% of all claims and the console's win ladder prints that; the committed report FaabBid main and LeagueConsole read PRICES from still prints $0 ->
- [low] DefenceThisWeek ranks by a different ADP source than the policy it claims to reproduce, and its committed artifact ran at the wrong lag — `src/main/java/DefenceThisWeek.java:123`: The pre-lag rule ("best FREE defence by preseason ADP") reads Sleeper's adp_half_ppr from the frozen draft-night fixture (lines 108-110), while WireRateStress.loadOne (line 427, `fp-adp-halfppr-\d{4}-\d{8}\.csv`) measured the 8.03 on Fantas
- [low] DefenceThisWeek's 'YOU HOLD' is the alphabetically-first defence you own, not the best one — `src/main/java/DefenceThisWeek.java:159`: The pool is iterated in TreeMap order of player id (BAL, BUF, ...), so with two rostered defences `held` is whichever abbreviation sorts first. Holding BAL and DET after a mid-season claim, the report compares the policy pick to the Ravens 
- [low] FaabBid -Pfit walks weeks 1..18 by literal while WireSurvival and LeagueTransactions.moves use weeks(season) — `src/main/java/FaabBid.java:221`: The two harvesters over the same transaction log bound the season differently. All five completed seasons (2021-2025) are 18-week, so the 1,448-contest curve is unaffected today; the moment a pre-2021 league year enters the chain, or the NF
- [low] FaabBid's 'max' column is the 99.9th percentile, which for 1448 prices is the second-largest — `src/main/java/FaabBid.java:340`: (int)(0.999 * 1448) = 1446, the second-to-last index. data/faab-contests-2026-09-05.txt prints 'every claim ... max 75' while its own PRICES ALL block ends '...,71,75,76' - the headline table understates the league's record bid by one row. 
- [low] LeagueActuals' class javadoc and the build.gradle knob comment say league scoring is opt-in and off by default; enabled() defaults to true — `src/main/java/LeagueActuals.java:31`: DefenceThisWeek's form (LeagueWeek.actual -> LeagueActuals.weeklyPoints) and StartSit -Pcalibrate both run on this switch. A reader of the class header or build.gradle concludes the default run is feed-scored (4-point passing TDs) and eithe
- [low] LeagueConsole's javadoc says the page covers 'the defence'; it has no defence tab, DefenceThisWeek appears in no RUNBOOK step, and its committed artifact ran at the non-measured lag — `src/main/java/LeagueConsole.java:16`: RUNBOOK.md contains zero mentions of DefenceThisWeek (only README:54 does), and the console has no defence section, so the only implementation of the streaming policy the objective is built on (WeeklyStarterValue.DEF_STREAM_OVER_HOLD = 8.03
- [low] LeagueConsole's usage javadoc still advertises -Ppool=6 after DEFAULT_POOL moved to 8 — `src/main/java/LeagueConsole.java:50`: 18fd956 fixed the 6-vs-8 literal in code and TRAPS but left the usage line, which is what gets copied into a command; `-Ppool=6` typed from it re-creates the narrower board the commit describes (81 trades found where 182 exist). The same he
- [low] The lineup shape is a literal in five files while the league JSON carries roster_positions — `src/main/java/LeagueConsole.java:104`: The same 1/2/3/1/1 (+2 FLEX) appears at LeagueConsole.java:947-948 (`starts`), TradeMarket.java:746-747, TeamRankings.java:52-60 (FIXED/FLEX) and WeeklyStarterValue.java:267-278/284 (fill calls), and the league's `roster_positions` array is
- [low] A rostered man missing from the player index crashes LeagueConsole through StartSit.slot while StartSit itself silently drops him — `src/main/java/LeagueConsole.java:324`: positionOf.get(id) is null when Player.getPlayerFromSIDV2 returns null (LeagueConsole:248-250 explicitly allows it). If that man has a row in the week projection feed he is `playing`, cannot start (position '?'), so :324 calls closestStarte
- [low] FAAB budget is a literal 100 that must match the league's waiver_budget setting — `src/main/java/LeagueConsole.java:347`: The league JSON carries settings.waiver_budget (100 today, seriousLeagueSleeper...2026-09-11.txt). If the commissioner changes it next season, every precomputed bid and the '$X of $100 already spent' line are wrong with nothing saying so - 
- [low] The FAAB budget is the literal 100 in Java, in the page script and in FaabBid's default — `src/main/java/LeagueConsole.java:359`: `int budgetLeft = 100;` (line 347) is also the silent default when Justin's roster row is not matched by display name; the page prints `"$" + (100 - D.budget) + " of $100 already spent"` (PAGE line 1123) and FaabBid.java:250 defaults `-Pbud
- [low] Chains clear tradeFloor while the page and its test say swapFloor — `src/main/java/LeagueConsole.java:619`: The page text at lines 1208 and 1229 says every chain step 'has to clear the ${D.swapFloor.toFixed(1)}-point floor', and LeagueConsoleTest.whatATradeOpensUpIsTheChainItShips reads "swapFloor" (line 512-513) and asserts each step >= bar (lin
- [low] ObjectiveStability's -Pme takes a user id while every other in-season tool's -Pme takes a display name; a display name yields an empty roster and a 0.0 floor — `src/main/java/ObjectiveStability.java:56`: Passing `-Pme=justinb314` (the form StartSit, TuesdaySwap, LeagueConsole and DefenceThisWeek accept) makes KeeperChooser.eligibleCandidates return nothing, the roster loop adds no men, and the tool writes data/objective-stability-<date>.txt
- [low] ProjectionDrift and the RUNBOOK say AdpSnapshot archives the feed daily; the archive is the gitignored day-cache, written only on days a tool ran — `src/main/java/ProjectionDrift.java:32`: The root holds 15 sleeperProjections2026*.txt files with gaps on 09-03, 09-04, 09-09 and 09-10 - the days nothing was run - and none of them is committed. The 585/30/105.9 numbers RUNBOOK quotes are reproducible only on this laptop; a fresh
- [low] ProjectionDrift reads whatever day-cache files survive in the project root, not the committed snapshot archive its javadoc names — `src/main/java/ProjectionDrift.java:65`: The javadoc says 'AdpSnapshot archives the feed daily; after real games have been played, run this', but the archive scanned is the gitignored sleeperProjections2026<date>.txt day caches - present only for days a tool ran (09-02 -> 09-05 is
- [low] StartSit.readCurve's regex cannot parse the open-ended top bin that calibrate writes — `src/main/java/StartSit.java:132`: BINS ends {25, MAX_VALUE}; calibrate prints that bin as '25+  n  rate', which readCurve never matches, so the measured 25+ rate is written and then thrown away and any gap of 25 or more is answered with the 16-25 bin's rate via the fallback
- [low] StartSit's lineup goes through DraftExpectation.man while its own closestStarter stopped doing so, and the console uses the man's own position — `src/main/java/StartSit.java:200`: The lineup path re-derives each man's position from the player index a second time (DraftExpectation.man returns "?" when the index lacks him), after StartSit's own comment at 272-275 explains that this exact call "gave every man position '
- [low] Dead code in the live lineup path: StartSit.flexEligible exists only for its test, LeagueConsole.FLEX is never read — `src/main/java/StartSit.java:253`: closestStarter deliberately replaced the flex-eligibility rule with a legality check via TeamRankings.bestLineup (StartSit:257-269 explains why the old rule was wrong twice). The helper and its test survive, so a reader finds a tested flex 
- [low] A completion is searched only at the dropped man's position, so an emptied FLEX is refilled at the wrong price — `src/main/java/TuesdaySwap.java:107`: slotsFilled drops by one when the roster can no longer fill a slot, but the slot that went empty need not be the dropped man's fixed slot: drop a tight end who was starting at FLEX with no other flex-eligible bench man and the hole is a FLE
- [low] TuesdaySwap.price's hole-with-no-completion fall-through and complete()'s same-position rule are untested — `src/main/java/TuesdaySwap.java:185`: When a drop empties a slot and no candidate at that position exists (complete() requires `positionOf.get(replacement) == emptied`, line 111), worth silently becomes the RAW pair gain - the exact number the 2026-09-08 fix was written to stop
- [low] A row flagged as emptying a slot is priced at the raw half-plan when no completion is found — `src/main/java/TuesdaySwap.java:190`: complete() returns null when `Position emptied = positionOf.get(swap.dropId())` is null - TuesdaySwap.main line 263 stores `player == null ? null : player.position` for a roster man the player index lacks - or when no candidate at that posi
- [low] TuesdaySwap builds its candidate list in HashMap order while the console uses an EnumMap, so tied rows order differently between the two artifacts — `src/main/java/TuesdaySwap.java:238`: search() sorts by gain with a stable sort, so exact ties (the seven +0.0 DEF rows this week) keep candidate-list order, which on the report depends on HashMap iteration and on the page on enum order. The report and page can print the same b
- [low] TuesdaySwap hardcodes the regular season's length that SeasonOutlook and SeasonLedger read from the league — `src/main/java/TuesdaySwap.java:270`: SeasonOutlook.java:85 and SeasonLedger.java:390 read `playoff_week_start` (15 today) from the league settings; TuesdaySwap types 15. They agree now; a settings change moves the outlook tools and leaves the "from here" column (and finding 1'
- [low] weeksLeft hardcodes a 14-week regular season that SeasonOutlook reads from the league settings; the page does not scale at all — `src/main/java/TuesdaySwap.java:270`: SeasonOutlook (line 85) and SeasonLedger (line 390) read playoff_week_start from the league JSON (15 in data/fixtures/2026-pre-draft/seriousLeagueSleeper*.txt); this tool types 15. A commissioner change moves the outlook and not the swap re
- [low] TuesdaySwap hard-codes the regular season end at 15 while SeasonLedger reads playoff_week_start from the league — `src/main/java/TuesdaySwap.java:270`: Today the literal matches the setting. If the commissioner moves the playoff start to 16 (Sleeper permits it mid-season), SeasonLedger follows and TuesdaySwap's 'about N of those weeks are left, scale by X' line and every 'from here' number
- [low] Two live tools answer the defence question on different bases: the objective prices the held Ravens at exactly 0, DefenceThisWeek says START them — `src/main/java/WeeklyStarterValue.java:372`: On the 2026-09-11 feed the 13th defence projects 86.0, so the wire is 86 x 8.03/7.21 = 95.8, and the Ravens project 95.0 - below the wire - so fill() at line 300 never starts them and every DEF swap prints +0.0 (seven of the eight rows in d
- [low] WireSurvival is a stray: no document names it, no runbook step runs it, and the conditional it measured is not consumed by FaabBid — `src/main/java/WireSurvival.java:47`: `grep -il wiresurvival *.md` returns nothing - RUNBOOK, README, FAAB-PLAN and TRAPS are all silent, although ce5ecc1 relied on its sevenfold hazard decline to justify a $0 bid. FaabBid and the console's 5,124 precomputed bids still use the 
- [low] WireSurvival computes each season's end and then measures unclaimed spells against the whole record's end — `src/main/java/WireSurvival.java:111`: `endOfSeason` is never used; main (lines 182-183) uses one `endOfRecord` = the latest stamp across all completed seasons, so an unclaimed man dropped in week 16 of 2021 "lasted" four years and sits in the at-risk denominator of the 2-8-week
- [low] WireSurvival computes endOfSeason and never uses it; unclaimed men are censored at the end of the whole record — `src/main/java/WireSurvival.java:111`: main (line 182-183) measures every unclaimed spell to the latest stamp in ALL seasons, so a man dropped in December 2021 and never claimed 'sat' four years and counts at risk in every band. Reproducing spells()/hazard() in python over the c
- [low] ConsoleRendersTest's field guard does not see the `t.` reads in the trades filter — `src/test/java/ConsoleRendersTest.java:67`: LeagueConsole's trades() reads `t.season`, `t.with`, `t.give`, `t.get` (page lines 1176-1177) and the sort reads `a[key]`/`b[key]`. Rename `season` in the emitter and the 'Only trades that also help 2026' filter (default on) compares undefi
- [low] FaabBidTest median assertion cannot fail: the ALL median is 0 by construction — `src/test/java/FaabBidTest.java:84`: The committed report states 772 of 1448 claims (53%) cleared at 0, so quantile(0.5) of ALL is 0 and the assertion reduces to contested-median >= 0, which no list of non-negative bids can violate. The property it names (contested dearer than
- [low] altWorth assertion restates the emitter's own `better` condition — `src/test/java/LeagueConsoleTest.java:351`: LeagueConsole line 439 emits altDrop/altWorth only when `better = free != swap && altGain > worth + 0.05`, so every shipped altWorth exceeds worth by construction. The test's own comment at lines 355-358 records removing exactly this shape 
- [low] Two LeagueConsoleTest tests end in an assumption, so healthy weeks report them as skipped — `src/test/java/LeagueConsoleTest.java:776`: The assertions above it already ran, but a trailing assumeTrue marks the test aborted/skipped. Same shape at line 811 (`assumeTrue(flagged > 0, ...)`). RUNBOOK says '0 skipped matters as much as 0 failed'; on a week with no injury tags and 
- [low] LeagueWeekTest's cache-name test asserts on two literals typed in the test, never on LeagueWeek — `src/test/java/LeagueWeekTest.java:31`: LeagueWeek.projected builds its names at lines 74-75 ("sleeperWeekProjection" + season + "w" + week / "sleeperLiveProjection" ...). Rename either literal in LeagueWeek to collide (or to the getCachedForever name WeeklyProjections also uses)
- [low] ObjectiveStabilityTest types '480 scenarios' while insisting elsewhere the count be read from the report — `src/test/java/ObjectiveStabilityTest.java:42`: LeagueConsoleTest line 315-320 refuses a hardcoded 480 ('READ THE COUNT OFF THE REPORT THAT MEASURED IT, never type it here') and parses '(N scenarios,' from the same file. This test types it. Regenerating the stability report at 960 scenar
- [low] ProjectionDriftTest's 'feed moves' guard compares oldest to newest, so it can never detect the in-season freeze the tool exists to notice — `src/test/java/ProjectionDriftTest.java:54`: Thirty players moved between 08-24 and 09-07 in preseason, so oldest-vs-newest will show movers for the rest of the season whatever the feed does after week 1. RUNBOOK 'Run ProjectionDrift once real games are played' relies on Justin readin
- [low] WireStressRegressionTest pins the 8.03/7.21 constants to one dated file, and DefenceThisWeek prints the same numbers as prose literals — `src/test/java/WireStressRegressionTest.java:23`: Re-running WireRateStress (e.g. after another scoring or join fix, as happened twice between 08-31 and 09-04) writes wire-rate-stress-<newdate>.txt; this test keeps reading the 09-04 file and keeps passing, so WeeklyStarterValue.DEF_STREAM_
